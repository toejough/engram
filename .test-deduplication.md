# Test Deduplication Analysis

This document maps business properties to tests, ranks tests by coverage quality (no IO > IO, simple > complex), and identifies redundant tests.

## Property: "Refactor extract produces valid Go code that compiles"

### Tests validating this property (ranked):
1. ⭐ **REMOVED** - cmd/projctl/refactor_extract_integration_test.go (has IO: exec.Command, moved to integration)

**Recommendation:** Property only validated by integration test (already separated)

---

## Property: "Extracted function parameters are correctly detected"

### Tests validating this property (ranked):
1. ⭐ **REMOVED** - cmd/projctl/refactor_extract_integration_test.go (has IO, moved to integration)

**Recommendation:** Property only validated by integration test (already separated)

---

## Property: "Extracted function return values are correctly detected"

### Tests validating this property (ranked):
1. ⭐ **REMOVED** - cmd/projctl/refactor_extract_integration_test.go (has IO, moved to integration)

**Recommendation:** Property only validated by integration test (already separated)

---

## Property: "Extraction is atomic (rollback on failure)"

### Tests validating this property (ranked):
1. ⭐ **REMOVED** - cmd/projctl/refactor_extract_integration_test.go (has IO, moved to integration)

**Recommendation:** Property only validated by integration test (already separated)

---

## Property: "Valid line ranges produce valid extractions" (rapid)

### Tests validating this property (ranked):
1. ⭐ **REMOVED** - cmd/projctl/refactor_extract_integration_test.go (has IO, property-based, moved to integration)

**Recommendation:** Property only validated by integration test (already separated)

---

## Property: "Rename updates all references across files"

### Tests validating this property (ranked):
1. ⭐ **REMOVED** - cmd/projctl/refactor_integration_test.go (has IO, moved to integration)

**Recommendation:** Property only validated by integration test (already separated)

---

## Property: "Rename is atomic (no partial changes on failure)"

### Tests validating this property (ranked):
1. ⭐ **REMOVED** - cmd/projctl/refactor_integration_test.go (has IO, moved to integration)

**Recommendation:** Property only validated by integration test (already separated)

---

## Property: "Valid Go identifiers rename successfully" (rapid)

### Tests validating this property (ranked):
1. ⭐ **REMOVED** - cmd/projctl/refactor_integration_test.go (has IO, property-based, moved to integration)

**Recommendation:** Property only validated by integration test (already separated)

---

## Property: "RealFS correctly wraps real filesystem operations"

### Tests validating this property (ranked):
1. internal/parser/realfs_test.go (has IO: os.Stat, os.WriteFile, os.Mkdir, os.ReadFile, filepath.Walk)

**Recommendation:** Thin wrapper validation - consider removing if no unique business logic beyond stdlib calls

---

## Property: "SSIM of identical images is 1.0"

### Tests validating this property (ranked):
1. internal/screenshot/screenshot_test.go (has IO: image.NewRGBA, png.Encode, os.Create, os.ReadFile, os.Stat)

**Recommendation:** Keep - validates core SSIM algorithm behavior (image IO is minimal for test data)

---

## Property: "Different images have SSIM < 1.0"

### Tests validating this property (ranked):
1. internal/screenshot/screenshot_test.go (has IO: image.NewRGBA, png.Encode, os.Create, os.ReadFile, os.Stat)

**Recommendation:** Keep - validates core SSIM algorithm behavior

---

## Property: "Dimension mismatch returns error"

### Tests validating this property (ranked):
1. internal/screenshot/screenshot_test.go (has IO: image.NewRGBA, png.Encode, os.Create, os.ReadFile, os.Stat)

**Recommendation:** Keep - validates SSIM error handling

---

## Property: "Clusters identify regions of difference"

### Tests validating this property (ranked):
1. internal/screenshot/screenshot_test.go (has IO: image.NewRGBA, png.Encode, os.Create, os.ReadFile, os.Stat)

**Recommendation:** Keep - validates clustering algorithm

---

## Property: "Install creates symlinks for all skills"

### Tests validating this property (ranked):
1. internal/skills/skills_test.go (has IO: os.MkdirAll, os.WriteFile, os.ReadFile, os.Symlink, os.Lstat, os.Readlink)

**Recommendation:** Candidate for dependency injection - logic can be tested with mockFS

---

## Property: "Install with Force overwrites conflicts"

### Tests validating this property (ranked):
1. internal/skills/skills_test.go (has IO: filesystem operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Uninstall preserves non-symlink directories"

### Tests validating this property (ranked):
1. internal/skills/skills_test.go (has IO: filesystem operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Status detects linked/missing/local/stale skills"

### Tests validating this property (ranked):
1. internal/skills/skills_test.go (has IO: filesystem operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Valid TOML round-trips correctly" (rapid)

### Tests validating this property (ranked):
1. internal/result/result_test.go (has IO: os.MkdirAll, os.WriteFile, os.ReadFile)

**Recommendation:** Candidate for dependency injection - TOML round-trip is pure logic

---

## Property: "Missing required sections return errors"

### Tests validating this property (ranked):
1. internal/result/result_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Collect merges multiple result files"

### Tests validating this property (ranked):
1. internal/result/result_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Valid entries are always parseable" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/corrections/analyze_test.go (no IO, property-based)
2. internal/log/log_test.go (has IO: os.WriteFile, os.ReadFile, os.Stat, property-based)
3. internal/corrections/corrections_test.go (has IO: os.WriteFile, os.ReadFile, property-based)

**Recommendation:** Keep #1 (analyze_test.go). Consider removing similar validation from #2, #3 if they don't add unique value beyond #1.

---

## Property: "Token estimate calculated from message length"

### Tests validating this property (ranked):
1. internal/log/log_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection - token estimation is pure logic

---

## Property: "Multiple entries written as separate JSONL lines"

### Tests validating this property (ranked):
1. internal/log/log_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Read filters by model when requested"

### Tests validating this property (ranked):
1. internal/log/log_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Report sums tokens from all log entries"

### Tests validating this property (ranked):
1. internal/usage/usage_test.go (has IO: os.MkdirAll, os.WriteFile)

**Recommendation:** Candidate for dependency injection

---

## Property: "Report provides breakdown by model"

### Tests validating this property (ranked):
1. internal/usage/usage_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Total matches sum of by-model values" (rapid)

### Tests validating this property (ranked):
1. internal/usage/usage_test.go (has IO: file operations, property-based)

**Recommendation:** Candidate for dependency injection - arithmetic property is pure

---

## Property: "Check returns correct status based on thresholds"

### Tests validating this property (ranked):
1. internal/usage/usage_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection - threshold logic is pure

---

## Property: "Create auto-increments conflict IDs"

### Tests validating this property (ranked):
1. internal/conflict/conflict_test.go (has IO: os.WriteFile, os.Stat)

**Recommendation:** Candidate for dependency injection

---

## Property: "Check finds resolved conflicts"

### Tests validating this property (ranked):
1. internal/conflict/conflict_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "List filters by status"

### Tests validating this property (ranked):
1. internal/conflict/conflict_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Log appends correction entries to JSONL"

### Tests validating this property (ranked):
1. internal/corrections/corrections_test.go (has IO: os.WriteFile, os.ReadFile)

**Recommendation:** Candidate for dependency injection

---

## Property: "LogGlobal creates .claude directory if missing"

### Tests validating this property (ranked):
1. internal/corrections/corrections_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Analyze identifies repeated corrections (count >= 2)"

### Tests validating this property (ranked):
1. ⭐ internal/corrections/analyze_test.go (no IO, pure business logic)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Fuzzy matching groups similar corrections"

### Tests validating this property (ranked):
1. ⭐ internal/corrections/analyze_test.go (no IO, pure business logic)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Respects MinOccurrences threshold"

### Tests validating this property (ranked):
1. ⭐ internal/corrections/analyze_test.go (no IO, pure business logic)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Patterns sorted by count descending" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/corrections/analyze_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Keyword extraction for fuzzy matching"

### Tests validating this property (ranked):
1. ⭐ internal/corrections/analyze_test.go (no IO, pure business logic)

**Recommendation:** Keep - exemplary unit test

---

## Property: "SelectEditor uses $EDITOR env var"

### Tests validating this property (ranked):
1. ⭐ internal/escalation/editor_test.go (no IO, uses MockExecutor)

**Recommendation:** Keep - exemplary unit test with proper mocking

---

## Property: "SelectEditor falls back to vim"

### Tests validating this property (ranked):
1. ⭐ internal/escalation/editor_test.go (no IO, uses MockExecutor)

**Recommendation:** Keep - exemplary unit test

---

## Property: "ReviewEscalations opens editor and parses changes"

### Tests validating this property (ranked):
1. ⭐ internal/escalation/editor_test.go (no IO, uses MockExecutor)

**Recommendation:** Keep - exemplary unit test

---

## Property: "WriteEscalationFile creates markdown format"

### Tests validating this property (ranked):
1. ⭐ internal/escalation/escalation_test.go (no IO, uses mockFS)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Round-trip preserves escalation data"

### Tests validating this property (ranked):
1. ⭐ internal/escalation/escalation_test.go (no IO, uses mockFS)

**Recommendation:** Keep - exemplary unit test

---

## Property: "ParseEscalationFile captures user edits"

### Tests validating this property (ranked):
1. ⭐ internal/escalation/escalation_test.go (no IO, uses mockFS)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Invalid status values are rejected"

### Tests validating this property (ranked):
1. ⭐ internal/escalation/escalation_test.go (no IO, uses mockFS)

**Recommendation:** Keep - exemplary unit test

---

## Property: "ApplyResolutions applies resolved status decisions"

### Tests validating this property (ranked):
1. ⭐ internal/escalation/resolve_test.go (no IO, pure business logic)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Deferred status creates issue"

### Tests validating this property (ranked):
1. ⭐ internal/escalation/resolve_test.go (no IO, pure business logic)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Issue status creates issue with user description"

### Tests validating this property (ranked):
1. ⭐ internal/escalation/resolve_test.go (no IO, pure business logic)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Pending status is skipped"

### Tests validating this property (ranked):
1. ⭐ internal/escalation/resolve_test.go (no IO, pure business logic)

**Recommendation:** Keep - exemplary unit test

---

## Property: "CheckBudget returns OK when under warning threshold"

### Tests validating this property (ranked):
1. internal/context/budget_test.go (has IO: os.WriteFile)

**Recommendation:** Candidate for dependency injection

---

## Property: "Returns warning when over warning but under limit"

### Tests validating this property (ranked):
1. internal/context/budget_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Returns limit exceeded when over limit"

### Tests validating this property (ranked):
1. internal/context/budget_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Uses most recent context estimate from log"

### Tests validating this property (ranked):
1. internal/context/budget_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "DetectFormat identifies YAML by --- markers"

### Tests validating this property (ranked):
1. ⭐ internal/parser/format_test.go (no IO, pure format detection)

**Recommendation:** Keep - exemplary unit test

---

## Property: "DetectFormat identifies TOML by key=value"

### Tests validating this property (ranked):
1. ⭐ internal/parser/format_test.go (no IO, pure format detection)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Content starting with --- is YAML" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/parser/format_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "ParseTestFunctions finds all Test* and Benchmark* functions"

### Tests validating this property (ranked):
1. ⭐ internal/parser/gotest_test.go (no IO, pure AST parsing)

**Recommendation:** Keep - exemplary unit test

---

## Property: "N test functions yields N results" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/parser/gotest_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "ParseTraceComment extracts TEST-XXX traces: TARGET"

### Tests validating this property (ranked):
1. ⭐ internal/parser/gotest_test.go (no IO, pure parsing)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Valid format always parses" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/parser/gotest_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "ParseGoTestFile produces TraceItems with correct fields"

### Tests validating this property (ranked):
1. ⭐ internal/parser/gotest_test.go (no IO, pure parsing)

**Recommendation:** Keep - exemplary unit test

---

## Property: "ParseTOML handles valid TOML with required fields"

### Tests validating this property (ranked):
1. ⭐ internal/parser/toml_test.go (no IO, pure TOML parsing)

**Recommendation:** Keep - exemplary unit test

---

## Property: "ParseTOML handles multiple items"

### Tests validating this property (ranked):
1. ⭐ internal/parser/toml_test.go (no IO, pure parsing)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Missing required fields return errors"

### Tests validating this property (ranked):
1. ⭐ internal/parser/toml_test.go (no IO, pure validation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Result indicates deprecation (TOML is deprecated)"

### Tests validating this property (ranked):
1. ⭐ internal/parser/toml_test.go (no IO, pure parsing)

**Recommendation:** Keep - exemplary unit test

---

## Property: "SplitFrontmatter handles single item with frontmatter and body"

### Tests validating this property (ranked):
1. ⭐ internal/parser/yaml_test.go (no IO, pure YAML parsing)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Splits multiple items separated by ---"

### Tests validating this property (ranked):
1. ⭐ internal/parser/yaml_test.go (no IO, pure parsing)

**Recommendation:** Keep - exemplary unit test

---

## Property: "N items creates N results" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/parser/yaml_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "ParseFrontmatter validates required fields"

### Tests validating this property (ranked):
1. ⭐ internal/parser/yaml_test.go (no IO, pure validation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Valid frontmatter parses successfully" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/parser/yaml_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "ParseDocument continues on malformed item"

### Tests validating this property (ranked):
1. ⭐ internal/parser/yaml_test.go (no IO, error handling)

**Recommendation:** Keep - exemplary unit test

---

## Property: "DiscoverDocs finds files in docs/ directory"

### Tests validating this property (ranked):
1. ⭐ internal/parser/discovery_test.go (no IO, uses MockFS)

**Recommendation:** Keep - exemplary unit test with proper mocking

---

## Property: "Falls back to root when docs/ missing"

### Tests validating this property (ranked):
1. ⭐ internal/parser/discovery_test.go (no IO, uses MockFS)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Prefers docs/ even if root also has files"

### Tests validating this property (ranked):
1. ⭐ internal/parser/discovery_test.go (no IO, uses MockFS)

**Recommendation:** Keep - exemplary unit test

---

## Property: "docs/ takes priority over root" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/parser/discovery_test.go (no IO, property-based with MockFS)

**Recommendation:** Keep - exemplary unit test

---

## Property: "DiscoverTestFiles finds *_test.go files"

### Tests validating this property (ranked):
1. ⭐ internal/parser/discovery_test.go (no IO, uses MockFS)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Excludes vendor and .git directories"

### Tests validating this property (ranked):
1. ⭐ internal/parser/discovery_test.go (no IO, uses MockFS)

**Recommendation:** Keep - exemplary unit test

---

## Property: "CollectTraceItems finds docs and returns items"

### Tests validating this property (ranked):
1. ⭐ internal/parser/collect_test.go (no IO, uses mockCollectFS)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Finds test files and returns items"

### Tests validating this property (ranked):
1. ⭐ internal/parser/collect_test.go (no IO, uses mockCollectFS)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Combines docs and tests"

### Tests validating this property (ranked):
1. ⭐ internal/parser/collect_test.go (no IO, uses mockCollectFS)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Extracts project name for tests"

### Tests validating this property (ranked):
1. ⭐ internal/parser/collect_test.go (no IO, uses mockCollectFS)

**Recommendation:** Keep - exemplary unit test

---

## Property: "ParseDoc delegates to YAML or TOML parser"

### Tests validating this property (ranked):
1. ⭐ internal/parser/parser_test.go (no IO, pure delegation logic)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Returns error for unknown format"

### Tests validating this property (ranked):
1. ⭐ internal/parser/parser_test.go (no IO, error handling)

**Recommendation:** Keep - exemplary unit test

---

## Property: "BuildGraph creates nodes and edges from TraceItems"

### Tests validating this property (ranked):
1. ⭐ internal/trace/builder_test.go (no IO, pure graph construction)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Duplicate node ID returns error"

### Tests validating this property (ranked):
1. ⭐ internal/trace/builder_test.go (no IO, validation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Dangling edge creates warning"

### Tests validating this property (ranked):
1. ⭐ internal/trace/builder_test.go (no IO, validation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "N items creates N nodes" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/trace/builder_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "DetectCycle returns false for acyclic graph"

### Tests validating this property (ranked):
1. ⭐ internal/trace/cycle_test.go (no IO, pure cycle detection algorithm)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Detects simple cycle"

### Tests validating this property (ranked):
1. ⭐ internal/trace/cycle_test.go (no IO, cycle detection)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Detects self-loop"

### Tests validating this property (ranked):
1. ⭐ internal/trace/cycle_test.go (no IO, cycle detection)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Diamond dependency is not a cycle"

### Tests validating this property (ranked):
1. ⭐ internal/trace/cycle_test.go (no IO, cycle detection)

**Recommendation:** Keep - exemplary unit test

---

## Property: "AddNode adds node to graph"

### Tests validating this property (ranked):
1. ⭐ internal/trace/graph_test.go (no IO, pure graph data structure)

**Recommendation:** Keep - exemplary unit test

---

## Property: "AddNode returns error for duplicate ID"

### Tests validating this property (ranked):
1. ⭐ internal/trace/graph_test.go (no IO, validation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "AddNode validates ID format matches NodeType"

### Tests validating this property (ranked):
1. ⭐ internal/trace/graph_test.go (no IO, validation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Unique valid nodes always succeed" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/trace/graph_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "AddEdge creates bidirectional edges"

### Tests validating this property (ranked):
1. ⭐ internal/trace/graph_test.go (no IO, graph operations)

**Recommendation:** Keep - exemplary unit test

---

## Property: "AddEdge returns error when From/To node missing"

### Tests validating this property (ranked):
1. ⭐ internal/trace/graph_test.go (no IO, validation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Upstream returns transitive ancestors"

### Tests validating this property (ranked):
1. ⭐ internal/trace/graph_test.go (no IO, graph traversal)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Downstream returns transitive descendants"

### Tests validating this property (ranked):
1. ⭐ internal/trace/graph_test.go (no IO, graph traversal)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Orphans detects nodes with no edges in specified direction"

### Tests validating this property (ranked):
1. ⭐ internal/trace/graph_test.go (no IO, graph analysis)

**Recommendation:** Keep - exemplary unit test

---

## Property: "ValidateV2 passes with valid graph"

### Tests validating this property (ranked):
1. ⭐ internal/trace/validate_v2_test.go (no IO, pure validation logic)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Fails with cycle"

### Tests validating this property (ranked):
1. ⭐ internal/trace/validate_v2_test.go (no IO, validation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Fails with dangling reference"

### Tests validating this property (ranked):
1. ⭐ internal/trace/validate_v2_test.go (no IO, validation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Reports coverage warnings but still passes"

### Tests validating this property (ranked):
1. ⭐ internal/trace/validate_v2_test.go (no IO, validation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Tokenize splits on whitespace and punctuation"

### Tests validating this property (ranked):
1. ⭐ internal/memory/tokenizer_test.go (no IO, pure tokenization)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Handles empty string"

### Tests validating this property (ranked):
1. ⭐ internal/memory/tokenizer_test.go (no IO, edge case)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Token count <= word count" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/tokenizer_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Memory struct has required fields"

### Tests validating this property (ranked):
1. ⭐ internal/memory/types_test.go (no IO, struct validation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Validate rejects missing required fields"

### Tests validating this property (ranked):
1. ⭐ internal/memory/types_test.go (no IO, validation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Valid memory always validates" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/types_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "ParseMemoryLine extracts memory from formatted line"

### Tests validating this property (ranked):
1. ⭐ internal/memory/parse_test.go (no IO, pure parsing)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Handles different memory formats"

### Tests validating this property (ranked):
1. ⭐ internal/memory/parse_test.go (no IO, parsing)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Valid formats always parse" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/parse_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "FetchExternal retrieves URL content"

### Tests validating this property (ranked):
1. ⭐ internal/memory/external_test.go (no IO, mocked http.Get)

**Recommendation:** Keep - exemplary unit test with proper mocking

---

## Property: "Caches responses"

### Tests validating this property (ranked):
1. ⭐ internal/memory/external_test.go (no IO, mocked HTTP)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Cache hits don't refetch" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/external_test.go (no IO, property-based with mocks)

**Recommendation:** Keep - exemplary unit test

---

## Property: "ACT-R activation calculation based on recency and frequency"

### Tests validating this property (ranked):
1. ⭐ internal/memory/actr_test.go (no IO, pure activation calculation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "More recent access increases activation" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/actr_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "More frequent access increases activation" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/actr_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Hybrid search combines keyword and semantic results"

### Tests validating this property (ranked):
1. ⭐ internal/memory/hybrid_test.go (no IO, uses mocked Database)
2. **REMOVED** - internal/memory/hybrid_fts5_integration_test.go (has IO: real SQLite + FTS5, moved to integration)

**Recommendation:** Keep #1 (hybrid_test.go with mocks), integration test already separated

---

## Property: "Hybrid score >= max(keyword, semantic)" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/hybrid_test.go (no IO, property-based with mock DB)

**Recommendation:** Keep - exemplary unit test

---

## Property: "FTS5 search returns relevant results"

### Tests validating this property (ranked):
1. **REMOVED** - internal/memory/hybrid_fts5_integration_test.go (has IO, moved to integration)

**Recommendation:** Integration test already separated

---

## Property: "Semantic search with embeddings"

### Tests validating this property (ranked):
1. **REMOVED** - internal/memory/hybrid_fts5_integration_test.go (has IO, moved to integration)

**Recommendation:** Integration test already separated

---

## Property: "Combined results deduplicated" (rapid)

### Tests validating this property (ranked):
1. **REMOVED** - internal/memory/hybrid_fts5_integration_test.go (has IO, property-based, moved to integration)

**Recommendation:** Integration test already separated

---

## Property: "DetectContradiction finds opposing advice"

### Tests validating this property (ranked):
1. ⭐ internal/memory/contradiction_test.go (no IO, mocked LLM API)

**Recommendation:** Keep - exemplary unit test with proper mocking

---

## Property: "Contradictions have cosine similarity > threshold" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/contradiction_test.go (no IO, property-based with mock LLM)

**Recommendation:** Keep - exemplary unit test

---

## Property: "SemanticMatch compares embedding vectors"

### Tests validating this property (ranked):
1. ⭐ internal/memory/semantic_matcher_test.go (no IO, pure cosine similarity)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Identical text has similarity 1.0" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/semantic_matcher_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "DecideIngest filters based on confidence and recency"

### Tests validating this property (ranked):
1. ⭐ internal/memory/ingest_decision_test.go (no IO, pure filtering logic)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Confidence >= threshold ingested" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/ingest_decision_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "EnrichQuery expands query with synonyms"

### Tests validating this property (ranked):
1. ⭐ internal/memory/enriched_query_test.go (no IO, pure query expansion)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Enriched query contains original terms" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/enriched_query_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "FormatMemory produces consistent markdown"

### Tests validating this property (ranked):
1. ⭐ internal/memory/format_test.go (no IO, pure formatting)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Round-trip preserves content" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/format_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "ParseObservation validates schema"

### Tests validating this property (ranked):
1. ⭐ internal/memory/observation_schema_test.go (no IO, pure validation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Valid observations always parse" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/observation_schema_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Primacy effect: first item recalled more often"

### Tests validating this property (ranked):
1. ⭐ internal/memory/primacy_test.go (no IO, pure primacy calculation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Position 0 has highest recall" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/primacy_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "ParseHookInput extracts fields from JSON"

### Tests validating this property (ranked):
1. ⭐ internal/memory/hook_input_test.go (no IO, pure JSON parsing)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Valid JSON always parses" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/hook_input_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Synthesize combines related memories"

### Tests validating this property (ranked):
1. ⭐ internal/memory/synthesis_test.go (no IO, mock LLM API)
2. ⭐ internal/memory/llm_synthesis_test.go (no IO, mock HTTP API)

**Recommendation:** These may be duplicates - investigate if they test the same synthesis logic or different aspects

---

## Property: "Synthesis preserves key facts" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/synthesis_test.go (no IO, property-based with mock LLM)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Consolidate removes duplicates from CLAUDE.md"

### Tests validating this property (ranked):
1. internal/memory/consolidate_claudemd_test.go (has IO: os.ReadFile, os.WriteFile)

**Recommendation:** Candidate for dependency injection

---

## Property: "Consolidation is idempotent" (rapid)

### Tests validating this property (ranked):
1. internal/memory/consolidate_claudemd_test.go (has IO, property-based)

**Recommendation:** Candidate for dependency injection - idempotence is pure property

---

## Property: "AppendToCLAUDEmd adds entry to correct section"

### Tests validating this property (ranked):
1. internal/memory/append_claudemd_test.go (has IO: os.ReadFile, os.WriteFile)

**Recommendation:** Candidate for dependency injection

---

## Property: "Append preserves existing content" (rapid)

### Tests validating this property (ranked):
1. internal/memory/append_claudemd_test.go (has IO, property-based)

**Recommendation:** Candidate for dependency injection

---

## Property: "RemoveFromCLAUDEmd deletes matching entry"

### Tests validating this property (ranked):
1. internal/memory/remove_claudemd_test.go (has IO: os.ReadFile, os.WriteFile)

**Recommendation:** Candidate for dependency injection

---

## Property: "Removal is idempotent" (rapid)

### Tests validating this property (ranked):
1. internal/memory/remove_claudemd_test.go (has IO, property-based)

**Recommendation:** Candidate for dependency injection

---

## Property: "Reinforce increases activation for retrieved memories"

### Tests validating this property (ranked):
1. ⭐ internal/memory/reinforcement_test.go (no IO, pure reinforcement calculation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Reinforcement decays over time" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/reinforcement_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "ExtractMemories parses structured learning format"

### Tests validating this property (ranked):
1. ⭐ internal/memory/extract_test.go (no IO, pure extraction)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Valid format always extracts" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/extract_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "SessionCache stores and retrieves session data"

### Tests validating this property (ranked):
1. internal/memory/session_cache_test.go (has IO: os.WriteFile, os.ReadFile)

**Recommendation:** Candidate for dependency injection

---

## Property: "Cache hit returns stored value" (rapid)

### Tests validating this property (ranked):
1. internal/memory/session_cache_test.go (has IO, property-based)

**Recommendation:** Candidate for dependency injection

---

## Property: "ExtractLearnings uses LLM to extract lessons"

### Tests validating this property (ranked):
1. ⭐ internal/memory/learn_extraction_test.go (no IO, mocked LLM API)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Extraction preserves semantic meaning" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/learn_extraction_test.go (no IO, property-based with mock)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Hygiene detects stale memories below threshold"

### Tests validating this property (ranked):
1. ⭐ internal/memory/hygiene_test.go (no IO, pure hygiene calculation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Confidence decay is exponential" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/hygiene_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Promote moves high-confidence memories to CLAUDE.md"

### Tests validating this property (ranked):
1. internal/memory/promote_test.go (has IO: os.ReadFile, os.WriteFile)

**Recommendation:** Candidate for dependency injection

---

## Property: "Promotion threshold prevents noise" (rapid)

### Tests validating this property (ranked):
1. internal/memory/promote_test.go (has IO, property-based)

**Recommendation:** Candidate for dependency injection

---

## Property: "Metadata tracks source, confidence, timestamp"

### Tests validating this property (ranked):
1. ⭐ internal/memory/metadata_test.go (no IO, pure metadata handling)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Metadata serializes to JSON" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/metadata_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "LLM client sends requests and parses responses"

### Tests validating this property (ranked):
1. ⭐ internal/memory/llm_test.go (no IO, mocked HTTP API)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Retries on transient errors" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/llm_test.go (no IO, property-based with mock)

**Recommendation:** Keep - exemplary unit test

---

## Property: "LLM synthesis combines memories coherently"

### Tests validating this property (ranked):
1. ⭐ internal/memory/llm_synthesis_test.go (no IO, mocked HTTP API)

**Recommendation:** Keep - exemplary unit test (investigate possible overlap with synthesis_test.go)

---

## Property: "Synthesis respects context window" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/llm_synthesis_test.go (no IO, property-based with mock)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Curated memories have confidence 1.0"

### Tests validating this property (ranked):
1. ⭐ internal/memory/curated_test.go (no IO, pure curation logic)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Curated entries never decay" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/memory/curated_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Memory storage and retrieval via SQLite"

### Tests validating this property (ranked):
1. **REMOVED** - internal/memory/memory_integration_test.go (has IO: real SQLite database, moved to integration)

**Recommendation:** Integration test already separated

---

## Property: "End-to-end memory workflow"

### Tests validating this property (ranked):
1. **REMOVED** - internal/memory/memory_integration_test.go (has IO, moved to integration)

**Recommendation:** Integration test already separated

---

## Property: "Optimize removes low-confidence memories"

### Tests validating this property (ranked):
1. **REMOVED** - internal/memory/optimize_integration_test.go (has IO: exec.Command, moved to integration)

**Recommendation:** Integration test already separated

---

## Property: "TraceItem field validation"

### Tests validating this property (ranked):
1. ⭐ internal/trace/item_test.go (no IO, field validation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Valid items pass validation" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/trace/item_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Promote test IDs to documented status"

### Tests validating this property (ranked):
1. internal/trace/promote_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Validate enforces traceability rules"

### Tests validating this property (ranked):
1. ⭐ internal/trace/validate_test.go (no IO, pure validation)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Phase validation ensures workflow correctness"

### Tests validating this property (ranked):
1. internal/trace/validate_phase_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Task validation enforces structure"

### Tests validating this property (ranked):
1. internal/task/validate_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Territory mapping identifies ownership"

### Tests validating this property (ranked):
1. internal/territory/territory_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Retrospective generation from logs"

### Tests validating this property (ranked):
1. internal/retro/retro_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Interview collects user inputs"

### Tests validating this property (ranked):
1. ⭐ internal/interview/interview_test.go (no IO, uses mock prompter)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Validation enforces required fields" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/interview/interview_test.go (no IO, property-based with mocks)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Gap analysis identifies missing requirements"

### Tests validating this property (ranked):
1. ⭐ internal/interview/gap_test.go (no IO, pure analysis)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Gap detection is complete" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/interview/gap_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "ID parsing and validation"

### Tests validating this property (ranked):
1. ⭐ internal/id/id_test.go (no IO, pure parsing)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Valid IDs always parse" (rapid)

### Tests validating this property (ranked):
1. ⭐ internal/id/id_test.go (no IO, property-based)

**Recommendation:** Keep - exemplary unit test

---

## Property: "Merge combines documents without duplicates"

### Tests validating this property (ranked):
1. internal/integrate/merge_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Worktree management via git"

### Tests validating this property (ranked):
1. **REMOVED** - internal/worktree/worktree_integration_test.go (has IO: git operations, moved to integration)

**Recommendation:** Integration test already separated

---

## Property: "Coverage parsing from go test output"

### Tests validating this property (ranked):
1. internal/coverage/coverage_test.go (has IO: exec.Command, file operations)

**Recommendation:** Candidate for dependency injection - parsing is pure logic

---

## Property: "Config loading and validation"

### Tests validating this property (ranked):
1. internal/config/config_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "QA skill contract validation"

### Tests validating this property (ranked):
1. internal/skills/qa_skill_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Project skill orchestration"

### Tests validating this property (ranked):
1. internal/skills/project_skill_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Plan awareness skill validates plans"

### Tests validating this property (ranked):
1. internal/skills/plan_awareness_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Missing skills detection"

### Tests validating this property (ranked):
1. internal/skills/missing_skills_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Contract validation for skills"

### Tests validating this property (ranked):
1. internal/skills/contract_test.go (has IO: file operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Cleanup removes generated artifacts"

### Tests validating this property (ranked):
1. internal/skills/cleanup_test.go (has IO: file operations, directory operations)

**Recommendation:** Candidate for dependency injection

---

## Property: "Cleanup is idempotent" (rapid)

### Tests validating this property (ranked):
1. internal/skills/cleanup_test.go (has IO, property-based)

**Recommendation:** Candidate for dependency injection - idempotence is pure property

---

# Summary: Tests to Remove

## Fully Redundant Tests (rank #1 for zero properties)

**None found.** All tests validate at least one unique property or provide coverage for their property set.

## Potential Duplicates (further investigation needed)

### 1. Synthesis Logic Duplication
- **internal/memory/synthesis_test.go** - Property: "Synthesize combines related memories" (mock LLM)
- **internal/memory/llm_synthesis_test.go** - Property: "LLM synthesis combines memories coherently" (mock HTTP)

**Action:** Investigate if these test the same logic or different synthesis paths. If identical, keep llm_synthesis_test.go (more specific naming) and remove synthesis_test.go.

### 2. JSONL Parsing Property Overlap
- **internal/corrections/analyze_test.go** - Property: "Valid entries are always parseable" (rapid, no IO) ⭐
- **internal/log/log_test.go** - Property: "Valid entries are always parseable" (rapid, has IO)
- **internal/corrections/corrections_test.go** - Property: "Valid entries are always parseable" (rapid, has IO)

**Action:** Keep analyze_test.go (#1, no IO). Verify log_test.go and corrections_test.go don't redundantly test the same JSONL parsing - they should focus on their domain-specific properties.

### 3. Thin Wrapper (minimal business logic)
- **internal/parser/realfs_test.go** - Validates RealFS wraps stdlib filesystem operations

**Action:** Consider removing if RealFS is a transparent wrapper with no business logic beyond stdlib delegation. If it has error handling or transformation logic, keep the tests but focus on those aspects.

## Candidates for Dependency Injection (Step 8)

The following 40 test files use real IO but test pure business logic properties. These should be rewritten with dependency injection (mockFS/imptest):

1. internal/result/result_test.go (TOML round-trip, validation, merging)
2. internal/log/log_test.go (token estimation, JSONL formatting, filtering)
3. internal/usage/usage_test.go (token aggregation, threshold checking)
4. internal/conflict/conflict_test.go (ID incrementing, status filtering)
5. internal/corrections/corrections_test.go (JSONL append, directory creation)
6. internal/context/budget_test.go (threshold comparison, log parsing)
7. internal/skills/skills_test.go (symlink management, status detection)
8. internal/screenshot/screenshot_test.go (SSIM algorithm - image IO is minimal)
9. internal/memory/consolidate_claudemd_test.go (deduplication logic)
10. internal/memory/append_claudemd_test.go (section targeting, preservation)
11. internal/memory/remove_claudemd_test.go (deletion logic, idempotence)
12. internal/memory/session_cache_test.go (cache hit/miss logic)
13. internal/memory/promote_test.go (threshold filtering, promotion logic)
14. internal/trace/promote_test.go (ID promotion logic)
15. internal/trace/validate_phase_test.go (phase validation rules)
16. internal/task/validate_test.go (structure validation)
17. internal/territory/territory_test.go (ownership mapping)
18. internal/retro/retro_test.go (log analysis, retrospective generation)
19. internal/integrate/merge_test.go (deduplication, merging)
20. internal/coverage/coverage_test.go (go test output parsing)
21. internal/config/config_test.go (config validation)
22. internal/skills/qa_skill_test.go (contract validation)
23. internal/skills/project_skill_test.go (orchestration validation)
24. internal/skills/plan_awareness_test.go (plan validation)
25. internal/skills/missing_skills_test.go (detection logic)
26. internal/skills/contract_test.go (contract validation)
27. internal/skills/cleanup_test.go (cleanup logic, idempotence)

## Tests to Keep As-Is (exemplary unit tests, 47+ files)

All tests marked with ⭐ in the analysis above should be kept unchanged. They demonstrate:
- No real IO (use mocks/mockFS where needed)
- Property-based testing with rapid
- Clear business logic validation
- Simple, readable structure

Examples of exemplary tests to preserve:
- internal/parser/* (format, gotest, toml, yaml, discovery, collect, parser)
- internal/trace/* (builder, cycle, graph, validate_v2, item, validate)
- internal/memory/* (tokenizer, types, parse, external, actr, hybrid, contradiction, semantic_matcher, ingest_decision, enriched_query, format, observation_schema, primacy, hook_input, reinforcement, extract, learn_extraction, hygiene, metadata, llm, curated)
- internal/corrections/analyze_test.go
- internal/escalation/* (editor, escalation, resolve)
- internal/interview/* (interview, gap)
- internal/id/id_test.go

## Integration Tests (already separated in Step 5)

These tests were moved to separate files with build tags and are excluded from unit test runs:
- cmd/projctl/refactor_extract_integration_test.go
- cmd/projctl/refactor_integration_test.go
- internal/memory/memory_integration_test.go
- internal/memory/memory_extract_integration_test.go
- internal/trace/trace_integration_test.go
- internal/worktree/worktree_integration_test.go

---

# Next Steps

## Step 7: Delete Redundant Tests
1. Investigate synthesis test duplication (synthesis_test.go vs llm_synthesis_test.go)
2. Verify JSONL parsing coverage (analyze_test.go vs log_test.go vs corrections_test.go)
3. Evaluate realfs_test.go necessity (thin wrapper check)

## Step 8: Dependency Injection Rewrites
Focus on the 27 test files listed in "Candidates for Dependency Injection" section. Rewrite to use:
- mockFS for file operations
- imptest pattern for dependency injection
- rapid for property-based testing
- gomega for readable assertions

## Step 9: Parallelize Tests
Add `t.Parallel()` to all unit tests (already have no shared state after DI rewrites)

## Step 10: Final Performance Measurement
Compare baseline (71.81s) → after orchestration removal (66.14s) → after issue migration (64.27s) → after integration separation (69.54s) → after deduplication → after DI rewrites → after parallelization
