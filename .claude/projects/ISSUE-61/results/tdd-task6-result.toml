[status]
result = "complete"
timestamp = "2026-02-05T12:30:00Z"

[summary]
phase = "tdd-red"
task_id = "TASK-6"
artifact = "internal/interview/interview.go"
test_file = "internal/interview/interview_test.go"

[test_results]
total = 10
passing = 1
failing = 9
red_state_verified = true

[files_modified]
paths = [
  "internal/interview/interview.go",
  "internal/interview/interview_test.go",
]

[[decisions]]
decision = "Created InterviewQuestion type to represent questions with context"
rationale = "Questions need to carry context information from gathered sources. InterviewQuestion wraps KeyQuestion with context field."
alternatives = ["Reuse KeyQuestion", "Use map[string]string"]
chosen = "New InterviewQuestion type"

[[decisions]]
decision = "SelectQuestions accepts gathered context as map[string]string"
rationale = "Maps question ID to context string. Simple, flexible, and allows easy lookup of what context is available for each question."
alternatives = ["Structured context type", "Pass GapAnalysis only"]
chosen = "Map parameter for gathered context"

[[decisions]]
decision = "Tests cover all gap size tiers and priority combinations"
rationale = "Each gap size (small/medium/large) has different question count requirements. Tests verify correct behavior for each tier and priority ordering."
alternatives = ["Property-based tests only", "Single example per tier"]
chosen = "Multiple examples per tier"

[[decisions]]
decision = "Stub returns empty list to force failures"
rationale = "Tests must fail in red phase. Empty list violates all non-empty expectations, ensuring tests fail correctly."
alternatives = ["Return random questions", "Return all questions"]
chosen = "Empty list stub"

[coverage]
# Each acceptance criterion mapped to tests

[[coverage.criteria]]
id = "AC-1"
description = "Code added after gap assessment, before yield"
tests = [
  "TestSelectQuestions_SmallGap_OneCriticalUnanswered",
  "TestSelectQuestions_AllAnswered_EmptyResult"
]

[[coverage.criteria]]
id = "AC-2"
description = "Small gap (≥80%): yields 1-2 confirmation questions for critical unanswered items only"
tests = [
  "TestSelectQuestions_SmallGap_OneCriticalUnanswered",
  "TestSelectQuestions_SmallGap_NoCriticalUnanswered",
  "TestSelectQuestions_MediumGap_PriorityOrdering"
]

[[coverage.criteria]]
id = "AC-3"
description = "Medium gap (50-79%): yields 3-5 clarification questions prioritizing critical then important"
tests = [
  "TestSelectQuestions_MediumGap",
  "TestSelectQuestions_MediumGap_PriorityOrdering"
]

[[coverage.criteria]]
id = "AC-4"
description = "Large gap (<50%): yields full interview sequence covering all key questions"
tests = [
  "TestSelectQuestions_LargeGap",
  "TestSelectQuestions_LargeGap_AllPriorities"
]

[[coverage.criteria]]
id = "AC-5"
description = "Question text references gathered context where relevant"
tests = [
  "TestSelectQuestions_ReferencesGatheredContext",
  "TestSelectQuestions_IncludesContextInformation"
]

[[coverage.criteria]]
id = "AC-6"
description = "Questions skip topics fully answered by context"
tests = [
  "TestSelectQuestions_SkipsAnsweredTopics",
  "TestSelectQuestions_AllAnswered_EmptyResult"
]

[traceability]
task = "TASK-6"
upstream = ["ARCH-002", "ARCH-004", "REQ-003"]
artifacts = ["internal/interview/interview.go", "internal/interview/interview_test.go"]
