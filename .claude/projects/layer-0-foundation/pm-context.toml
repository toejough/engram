[project]
name = "layer-0-foundation"
dir = ".claude/projects/layer-0-foundation"
repo_dir = "/Users/joe/repos/personal/projctl"
phase = "pm"
workflow = "new"

[input]
issue = "ISSUE-045"
description = """
Build core projctl infrastructure without agent spawning, as specified in docs/orchestration-system.md Section 13.3 "Layer 0: Foundation".

Key components:
1. Semantic memory system (ONNX + SQLite-vec + e5-small)
2. Memory CLI commands (query, learn, grep, extract, session-end)
3. Trace repair command
4. Context write yield_path support for parallel execution
"""

[input.references]
spec = "docs/orchestration-system.md"
section = "Section 13.3 Layer 0: Foundation"

[output]
artifact_path = ".claude/projects/layer-0-foundation/requirements.md"
yield_path = ".claude/projects/layer-0-foundation/pm-yield.toml"
