# Yield for TASK-006: Add projctl trace show CLI command

type = "complete"
task_id = "TASK-006"

[summary]
description = "Implemented `projctl trace show` CLI command with ASCII tree (default) and JSON output formats"
files_changed = [
    "cmd/projctl/trace.go",
    "cmd/projctl/main.go",
    "cmd/projctl/trace_test.go"
]

[acceptance]
criteria_met = [
    "projctl trace show outputs ASCII tree by default",
    "projctl trace show --format json outputs JSON graph",
    "--dir flag specifies project directory",
    "Invalid format returns error with valid options"
]

[notes]
testing = "Manual testing passed; CLI tests written but blocked by pre-existing build errors in repo (internal/trace/promote.go, cmd/projctl/state.go)"
pre_existing_issues = [
    "internal/trace/promote.go:193 - type mismatch error (untracked file)",
    "cmd/projctl/state.go - uncommitted changes reference task package"
]
