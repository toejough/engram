## 1. Skill edits with TDD (superpowers:writing-skills)

Each of the three skill edits (route, please, learn) goes through the full RED → GREEN → REFACTOR cycle using the `superpowers:writing-skills` skill, per repo rule. Do not skip the RED baseline.

- [x] 1.1 **route skill edit**: Run RED pressure test (route-only flow with no LESSONS line instruction — expect subagent to miss the line or not include it). Edit `agent-instructions/skills/route/SKILL.md`: add a new bullet to the MUST-hand checklist in the **"The handoff is the unlock"** section (currently: exact files, acceptance checks, do-NOT-touch bounds, recall-first instruction) mandating the completion-report contract — the report MUST end with a `LESSONS:` line per design D-A's format — and state the orchestrator's single re-ask rule for a missing line (one targeted follow-up, never a second). Run GREEN behavioral test (same flow with updated route skill — expect subagent to include LESSONS line). Done when: superpowers:writing-skills confirms RED failed, GREEN passed.

- [x] 1.2 **please skill edit**: Run RED pressure test (orchestrator collecting work, run closing learn — expect LESSONS lines not to be visible in the closing learn's context). Edit `agent-instructions/skills/please/SKILL.md` in two places: (a) in **step 4 "Execute (TDD)"** of "The workflow (fixed, seven steps)" (which is where dispatched units resolve), instruct the orchestrator to record each returned LESSONS line at the moment the dispatch result is handled; (b) in **step 7 "Capture (close) — `/learn`"**, add the collected LESSONS lines to what that step hands the closing learn, alongside the existing lessons-audit list (design D-A, D-B). Run GREEN behavioral test (same orchestration flow — expect closing learn to receive collected LESSONS lines). Done when: superpowers:writing-skills confirms RED failed, GREEN passed.

- [x] 1.3 **learn skill edit**: Run RED pressure test (closing learn with sample LESSONS lines present, no kind-4 exemplars or completion anchor — expect kind-4 moments to fire rarely). Edit `agent-instructions/skills/learn/SKILL.md` **Step 2** ("Crystallize explicit lessons") to: (a) add curation logic — the closing learn's scan gains the session's collected LESSONS lines as an explicit input, each judged against Step 2's existing four kinds and quality bar (confirmed not hypothesized; general principle, retrieval-shaped situation), silently discarding lines that fail; (b) add these three audit-derived positive exemplars to the kind-4 block (from `dev/eval/audit/results/moments.jsonl`, real missed success moments): **(i)** validated a design assumption by targeted code search before finalizing the design (e5ccd18d#89), **(ii)** confirmed a suspected defect via a targeted reproduction test (agent-aa0a29568578223b9#51), **(iii)** systematically verified vendor capability claims with documented evidence instead of assuming (agent-a585df274fee7ca19#27); (c) anchor the kind-4 scan to the completion moment per design D-C — fire when a unit's outcome is confirmed (review verdict, passing check, explicit user confirmation), and at the closing learn scan completion reports + collected LESSONS lines. Run GREEN behavioral test (same input with edited learn skill — expect LESSONS lines to be filtered by the bar, and kind-4 moments to fire more reliably). Done when: superpowers:writing-skills confirms RED failed, GREEN passed.

## 2. Deployment and verification

- [x] 2.1 Deploy via `engram update`: run `engram update` to deploy all three edited SKILL.md files to their deployment paths — for Claude Code these are `~/.claude/skills/route/SKILL.md`, `~/.claude/skills/please/SKILL.md`, `~/.claude/skills/learn/SKILL.md` (verify the actual paths from the update command's own output rather than assuming; Pi equivalents apply only if `engram update` reports deploying them). Done when: the update command reports success and the deployed files exist at the paths it names.

- [x] 2.2 Verify deployed copies: check that the three deployed SKILL.md files (at the paths task 2.1's update command reported) are byte-identical to their source files in `agent-instructions/skills/{route,please,learn}/SKILL.md`. Spot-check the LESSONS mandate in route's deployment, the collection logic in please's, and the curation + kind-4 exemplars in learn's. Done when: the spot checks confirm all three are correctly deployed and contain the expected text.

## 3. Pre-registered re-measurement

This block is gated on v1 shipping (tasks 1-2 complete). The re-measure is scheduled for 2-3 weeks after natural usage begins, per design D-E.

- [x] 3.1 Schedule the re-measurement: calendar a date 2-3 weeks after v1 ships (tasks 1-2 complete, skills are deployed). Record the baseline instrument version (v2, with bootstrap CIs) and the baseline metric: W2 from the audit (5.6%, CI95 [1.4, 11.3]). Joe's bar is noted, but no green/yellow/red threshold is pre-committed (design D-E, D-F; ADR-0028 D-G). Done when: the date is calendared and the baseline values are documented in a `re-measure-plan.md` file under `dev/eval/audit/`.

- [ ] 3.2 Run the re-measure: execute `python dev/eval/audit/run_audit.py --measure` (or equivalent command; the exact form is TBD based on how the audit tool evolves) with instrument v2 (same as baseline). Run on Joe's machine with the real vault and real transcript history (tasks 1-2 involved md-only changes to skills, so the measurement infrastructure itself hasn't changed). Capture the raw results (JSONL records, per-moment rates) in `dev/eval/audit/results/re-measure-<date>/`. Done when: the tool completes without error and the results are saved.

- [ ] 3.3 Compute re-measure results and compare: extract W2 (% worth-learning moments where learn fired) from the re-measure; compute secondary metrics (lost-lessons total, reachable share) per design D-E; compare against baseline (5.6%, [1.4, 11.3]). Label all answers DERIVED/ESTIMATE per design D-C. Document the comparison in `dev/eval/audit/REPORT-re-measure-<date>.md` (one row table: baseline vs. re-measure W2, with CI and DERIVED/ESTIMATE labels). Done when: the comparison is documented and shows whether W2 moved.

- [ ] 3.4 Decide escalation per design D-F: if re-measure shows W2 still below Joe's bar, escalate to the parked directions (watcher/metacognition layer, mechanical validator, hooks/harness push). If W2 hits Joe's bar or improves significantly, park the escalations further (re-visit only on next measured regression). Record the decision in a `escalation-decision-<date>.md` file. Done when: Joe and the agent review the results together (critical joint review, per design D-F and ADR-0028 D-G's live judgment call approach) and document the outcome.
