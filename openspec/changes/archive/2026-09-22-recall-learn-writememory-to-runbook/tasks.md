## 1. Eval fixture and skill baseline

- [x] 1.1 Design the write-memory eval task per design.md D4: a `recall`- or `learn`-shaped task, both kept as real skills, that reaches a write site (at least one `recall` Step-4 handoff and one `learn` Step-2 handoff, covering both parents) — cheap enough for n=3 per arm
- [x] 1.2 Build the fixture repo/vault under `dev/eval/cumulative/runbook_vs_skill/phase2/fixtures/write-memory/` (task-prompt.txt, task.json, steps.json, done_when_checks.sh, init_fixture_repo.sh or vault-template, TASK-RATIONALE.md) following the please/curate task layout
- [x] 1.3 Design the validity-gate fixture detail per design.md's Risk section: the write-memory runbook's real content must differ subtly from what an agent would compose from memory alone, so a run that skips `engram show` and gets lucky is detectable and scored as a validity failure
- [x] 1.4 Estimate eval cost, get Joe's confirmation, then run the skill-only baseline (recall/learn/write-memory all real skills), n=3, record composition-correctness and `end_state` as the skill row

## 2. Conversion

- [x] 2.1 Enumerate `write-memory/SKILL.md` sections (handoff contract, per-kind compose blocks, execute/verify/report) and tag each carry/drop-as-shim-floor; write the conversion-fidelity report plan
- [x] 2.2 Write the write-memory runbook in the fixture vault from the existing eval note `dev/eval/cumulative/runbook_vs_skill/phase2/encodings/shim/recall-learn/vault/8.2026-09-14.write-memory-compose-execute-verify.md`: situation, done_when, body verbatim where possible, red_flags (flag-mixing across kinds, `--tags` vs `--tag`, qa-takes-no-tags, wikilink-not-plain-text citations), no `triggers` field (design D2)
- [x] 2.3 Verify red_flags fits under the 1200-byte cap (it should, per design D1 — confirm, don't assume)
- [x] 2.4 In the fixture's frozen `recall`/`learn` skill copies, update all nine write-memory call sites (recall/SKILL.md:192,284,307; learn/SKILL.md:119,127,137-138,189-190,199-200,225 — re-verify line numbers against the fixture copy, not the live file; `recall/SKILL.md:293,339` and `learn/SKILL.md:25,98,231-232` are prose mentions, not invocation sites, and are NOT touched) to name/wikilink the runbook's basename instead of "invoke the write-memory skill"
- [x] 2.5 Complete the conversion-fidelity report: every carried/dropped/reworded item with reason
- [x] 2.6 Fresh-context reviewer checks the fidelity report against the real SKILL.md for lost content (reviewed 2026-09-22, verdict PASS-WITH-FIXES, fixes applied)

## 3. Validation (gates retirement)

- [x] 3.1 In a trial vault, confirm `engram show <basename>` resolves the promoted runbook fixture and its content matches the fidelity report (no query-retrieval check needed — write-memory has no trigger)
- [x] 3.2 Run the shim-only arm (write-memory as runbook; recall/learn as real skills), n=3; validity gate: the transcript shows `engram show <basename>` before each write is composed (design D4 — a correct write without this is not a pass); record composition-correctness and `end_state` against the skill row — **ran 2026-09-22, results in `dev/eval/cumulative/runbook_vs_skill/phase2/results/3.2_write_memory_shim_only_sonnet5.md`: validity gate FAILED in 3/3 trials (0 `engram show` fetches)**
- [x] 3.3 D8 bar: within one trial of the skill row on both composition-correctness and `end_state`, AND the validity gate (runbook fetched) holds in all passing trials — **MET as of the 3.4b retry2 run, 2026-09-22: followed_all 3/3 (matches skill row's 3/3), end_state 2/3 (within one trial of skill row's 3/3), validity gate (runbook fetched before every write) holds in both trials reporting end_state=True. See `dev/eval/cumulative/runbook_vs_skill/phase2/results/3.4b_write_memory_shim_only_retry2_sonnet5.md`.**
- [x] 3.4 If D8 not met: record actuals, iterate on the runbook body or the call-site wording, re-run; do not proceed to section 4 until met or Joe redirects — **ran (not passed) 2026-09-22, results in `dev/eval/cumulative/runbook_vs_skill/phase2/results/3.4_write_memory_shim_only_retry_sonnet5.md`: D8 still NOT MET (validity gate 0/3, `end_state` 1/3 vs skill's 3/3). Root cause found: the reworded call-site fix was applied only to the frozen fixture copy (`encodings/taskWriteMemory/recall-learn/`), not to the live canonical skill source (`agent-instructions/skills/{recall,learn}/SKILL.md`) that `probe.py` also deploys into each trial's isolated CLI config pool (`cfg-N/skills/`) — confirmed by byte-identical md5 to the pre-fix text. This run tested the OLD wording by accident; the fix itself remains untested. Needs: apply the fix to `agent-instructions/skills/`, verify via `--setup-only` diff, then re-test — not attempted here per task scope. **Retry2 (3.4b) ran 2026-09-22 after applying the fix to the live `agent-instructions/skills/{recall,learn}/SKILL.md` files (confirmed via `git diff` and deployed-config check): validity gate 3/3, followed_all 3/3, end_state 2/3 — D8 MET. One trial (R-0) failed `end_state` for a real, reproducible reason unrelated to validity: it wrote an unrequested third note (`engram learn qa ...`, which creates a `.q.md`+`.a.md` pair) beyond the two notes the task required. Results in `dev/eval/cumulative/runbook_vs_skill/phase2/results/3.4b_write_memory_shim_only_retry2_sonnet5.md`. Cumulative eval cost across 3.2 + 3.4 + 3.4b: $4.73. Section 4 is now unblocked — see note there about the temporary fixture basename still hardcoded in the live skill files.**

## 4. Promotion

- [x] 4.1 Promote the write-memory runbook to the production vault via `engram learn runbook` (never a hand-edit or file copy); confirm `engram embed status` is clean — **done 2026-09-22: promoted via `engram learn runbook` (fields copied verbatim from the fixture note: situation, done_when, body, red_flags 1-4, no triggers per D2) → assigned basename `1053.2026-09-22.write-memory-compose-execute-verify`. The fixture's 5th red_flag is self-referential (names its own basename as the `engram show` target), which can't be known before creation, so it was added in a follow-up `engram amend --target 1053... --red-flag ...` (all 5, in order, replacing the placeholder-free 4) once the real basename existed — same CLI-only path, no hand-edit. A `repo:` field also required a second `engram amend` run from inside the repo (the first create+amend ran from the scratchpad dir, so `detectRepo` found no git remote and left `repo:` unset; amend's `--repo` flag is server/wire-only and does not stamp the local frontmatter — re-running plain `engram amend --target <basename>` from this repo's cwd re-stamped `repo: ssh://git@github.com/toejough/engram.git` via cwd auto-detection, red_flags/body/etc. all confirmed unchanged after). `engram embed status`: 987/987 with-embeddings, 0 without/stale/incompatible/broken.**
- [x] 4.2 Update the real `agent-instructions/skills/{recall,learn}/SKILL.md`'s nine call sites (recall/SKILL.md:192,284,307; learn/SKILL.md:119,127,138,191,203,231 — line numbers shifted from the plan's original list by the 3.4/3.4b wording-fix edits made earlier this session) to name/wikilink the promoted runbook's real basename; also apply the `**REQUIRED SUB-SKILL:**` → `**REQUIRED NEXT ACTION:**` relabel to learn's five bolded labels (kinds 1, 2, 3, runbook-branch, feedback-branch), matching what was done in the fixture copies — this relabel decision was correctly in-scope during the fixture conversion (task 2.4) but was never recorded in this plan, risking it being silently dropped at promotion time. **Note (from 3.4b validation, 2026-09-22): the live `agent-instructions/skills/{recall,learn}/SKILL.md` currently hardcode the temporary eval-fixture basename `6a.2026-09-22.write-memory-compose-execute-verify` (that's what the 3.4b validation run actually deployed and tested) — this task must swap it for the real production runbook basename produced by 4.1, not leave the fixture name in place.** — **done 2026-09-22: relabel was already applied to all 5 sites during the 3.4/3.4b session fix (confirmed present, unaltered here); this task's remaining work was a literal `sed` swap of `6a.2026-09-22.write-memory-compose-execute-verify` → `1053.2026-09-22.write-memory-compose-execute-verify` across all 9 call sites in both files (3 in recall/SKILL.md, 6 in learn/SKILL.md) — verified 0 stale references to the fixture basename remain in either file, and the unrelated Step 2.5 self-referential-QA fix (design.md D-fix, section 7) was left untouched.**
- [x] 4.3 Confirm `engram show <basename>` resolves against the real vault from a non-repo cwd — **done 2026-09-22: `cd ~ && engram show 1053.2026-09-22.write-memory-compose-execute-verify` printed the full note (situation/done_when/red_flags/body) from `~`.**
- [x] 4.4 Confirm `shim.md` is imported in the real `~/.claude/CLAUDE.md` (and `~/.pi/agent/AGENTS.md`) — expected already true; re-confirm, don't assume — **done 2026-09-22: re-confirmed both — `~/.claude/CLAUDE.md:27` has `@~/.claude/engram/shim.md`; `~/.pi/agent/AGENTS.md:6` has `@~/.pi/agent/guidance/shim.md`.**

## 5. Retirement and references

- [x] 5.1 (DONE 2026-09-22; evidence: `enumeration.md` verified by an independent fresh-context reviewer, whose findings — GLOSSARY.md's missed "three"→"two"/"three"→"four" lines, the write-memory-worker Purpose-line archive-time task, two missing MODIFIED-requirement delta specs (learn-runbook-capture, learn-branching-disposition), c2-containers.md carrier-language sequence-diagram labels, ROADMAP.md scope wording, and enumeration.md's own exclusions-list gap — were applied) Run the doc-surface enumeration grep for `write-memory` across docs, skills, guidance, specs, and code; write the per-file disposition list; have a fresh-context reviewer independently verify it and run its own discovery pass (per this session's established pattern for please/curate)
- [x] 5.2 (DONE 2026-09-22; evidence: CLAUDE.md, README.md, docs/GLOSSARY.md, docs/ROADMAP.md, docs/architecture/{adr,c1-system-context,c2-containers}.md, agent-instructions/guidance/shim.md all edited per the reviewed enumeration; `targ test` and `targ check-full` green except check-uncommitted (expected while dirty); `openspec validate` clean) Update live references: `CLAUDE.md`, `README.md`, `docs/GLOSSARY.md`, `docs/architecture/{c1-system-context,c2-containers,c3-components}.md`
- [x] 5.3 (DONE 2026-09-22; evidence: `git rm -r agent-instructions/skills/write-memory/`; `engram update --with-guidance` dry-run plan matched expectations (deletes only write-memory's deployed copies, refreshes recall/learn + guidance), then the real run deleted `skills/write-memory` from both `~/.claude/engram` and `~/.pi/agent/engram` roots, removed dangling links `~/.claude/skills/write-memory` and `~/.pi/agent/skills/write-memory`; `ls ~/.claude/skills/` and `ls ~/.pi/agent/skills/` confirm only `learn`/`recall` remain of engram's own skills) Delete `agent-instructions/skills/write-memory/`; run `engram update` and confirm the deployed Claude Code and Pi copies are removed
- [x] 5.4 (DONE 2026-09-22; evidence: `targ check-full` 8/9 PASS, check-uncommitted fails only because the tree is dirty pending commit (expected); `openspec validate recall-learn-writememory-to-runbook --strict` and `--all --strict` both valid, 43/43 specs) `targ check-full` passes; `openspec validate --all --strict` passes
- [x] 5.5 (DONE 2026-09-22, archive time) Edit `openspec/specs/write-memory-worker/spec.md` line 5 (Purpose): it said "The write-memory skill is a dedicated worker that executes vault-write commands handed off by parent skills (recall and learn)..."; reworded to name the write-memory runbook (basename `1053.2026-09-22.write-memory-compose-execute-verify`) instead of "skill" (a delta cannot edit Purpose)

## 7. Learn Step 2.5 self-capture over-write fix (found via 3.4b/R-0, design.md D-fix)

- [x] 7.1 RED: confirm the OLD Step 2.5 wording, read literally, sanctions the over-capture — (a) cite the already-kept 3.4b/R-0 transcript where the agent quoted the old text and justified the extra `engram learn qa` write via the "crystallized a new vault note (Step 2) as the answer" disjunct; (b) a small headless micro-test (`claude -p`, no spend beyond negligible, 3 reps) reproducing the same YES verdict under the OLD wording for an R-0-shaped scenario
- [x] 7.2 GREEN: extend the "Gate — do not duplicate" clause in `agent-instructions/skills/learn/SKILL.md` Step 2.5 to exclude a question answered by the agent's own just-written Step 2 note this turn; add a matching Red flags row
- [x] 7.3 GREEN verification: re-run the same micro-test harness against the NEW wording — confirm (1) the self-referential scenario now returns NO (3/3 reps) and (2) a legitimate wikilink-to-existing-note scenario (no new Step 2 note this turn) still returns YES (3/3 reps)
- [x] 7.4 Add delta spec `specs/learn-adhoc-qa-capture/spec.md` (ADDED Requirements) normatively stating the gate; record the decision as design.md D-fix
- [x] 7.5 Check the vault for an existing note covering this before writing one (`engram query --lazy-chunks`); none found — crystallize a new `learn`-mid-cycle-fast-path vault note capturing the fix as a self-discovered reversal

## 6. Close-out

- [ ] 6.1 Record the eval outcome in `dev/eval/LEDGER.md`
- [ ] 6.2 Decide (design.md's Open Question): does this commit reference #760 in prose only (issue stays open for the recall/learn slices), or does a fresh sub-issue get filed for tracking? Confirm with Joe before committing
- [ ] 6.3 Commit; archive this change once merged
