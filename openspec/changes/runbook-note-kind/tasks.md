## 1. Capture: `engram learn runbook` + frontmatter schema (learn-runbook-capture)

- [x] 1.1 Write RED tests for `type: runbook` frontmatter schema (fields: `type`, `date`, `luhmann`, `situation`, `done_when`, `source`, optional `contributors`; filename `<luhmann-id>.<YYYY-MM-DD>.<slug>.md`, identical scheme to `fact`/`feedback`)
- [x] 1.2 Implement `engram learn runbook` as a third subcommand in the `learn` targ.Group (`internal/cli/targets.go`), reusing the shared `fact`/`feedback` pipeline: `LearnRunbookArgs` embedding `CommonLearnArgs` (gets `--target`/`--position` disposition flags for free) plus `--situation`/`--done-when`/`--body`; a `learnArgsFromRunbook` mapper into the existing `LearnArgs`; a `runbook` case in `assembleLearnContent` (new `runbookFields`/`runbookFrontmatterDoc` + render functions in `internal/cli/learn.go`). `RunLearn`/`writeLearnUnderLock`/`nextLuhmannID`/`learnPath` are reused unchanged. Template: `internal/cli/learn.go`'s existing `fact`/`feedback` handling — NOT `qa.go` (design.md Context)
- [x] 1.3 Confirm embed-on-write and vocab term assignment apply to runbook notes automatically via the shared `autoEmbedNote`/`applyVocabAssignmentAfterLearn` pipeline — no new wiring needed, same path `fact`/`feedback` already use
- [x] 1.4 Add `--help` text for `engram learn runbook`
- [x] 1.5 Get capture tests to GREEN; run `targ check-full` (tests GREEN, `lint-full`/`reorder-decls`/`deadcode`/`check-thin-api`/`test-integration` clean; `check-coverage-for-fail` and `check-nils-for-fail` both fail on a clean baseline too — verified pre-existing/environmental, not introduced by this work — see memory `nilaway-oom-gomemlimit-fix`)

## 2. write-memory + learn skill wiring

- [x] 2.1 Using `superpowers:writing-skills` TDD: baseline behavior test (RED) for `write-memory` handling a runbook handoff — confirmed the skill correctly refuses to improvise an undocumented `kind=runbook` compose path (blocks the write, does not guess a command); a second RED baseline on `learn`'s Step 2 kind-4 confirmed it currently has no way to represent numbered steps or a `done_when` ending condition, only flat feedback sentence fields
- [x] 2.2 Update `write-memory` SKILL.md to compose/execute `engram learn runbook` from a `learn` handoff (slug, situation, done_when, body/steps, source, target/position disposition, contributors) — the same disposition-judgment handoff shape write-memory already composes for fact/feedback
- [x] 2.3 Update `learn` SKILL.md: when to capture a runbook vs a feedback note (pick by shape — a reusable multi-step procedure for a recurring task → runbook; a single behavioral tweak with no step structure → feedback, as before), the runbook handoff fields (situation/done_when/body), and that a runbook capture already is the "remember to do it this way" note — no redundant kind=fact save-request fires alongside it when the user says "do that going forward"
- [x] 2.4 Verify GREEN + pressure-test both skill edits per `superpowers:writing-skills` — re-ran both baseline scenarios against the updated skills: write-memory composed the exact documented `engram learn runbook` command; `learn` correctly routed a multi-step Go-release confirmation to runbook and a single-tweak confirmation to feedback, and correctly suppressed the double-fire. (Technique/reference-skill testing bar per the skill's own taxonomy — not the heavier discipline-skill pressure-test apparatus, which targets rule-violation-under-pressure, not applicable here.)

## 3. Recall/query surfacing (recall-runbook-surfacing)

- [x] 3.1 Write tests: runbook notes appear in `candidate_l2s` (no exclusion treatment), retrieval ranking is pure situation-similarity like fact/feedback — `TestIsQueryExcludedKind/runbook_competes` and `TestRunQuery_RunbookCompetesInMainMatchedSet` (internal/cli/qa_test.go, query_runbook_test.go)
- [x] 3.2 Both tests passed on first run with zero production-code changes — Decision 2's "no `isQueryExcludedKind` membership" is an omission, not an addition, so retrieval symmetry already existed once the capture path (section 1) landed; `targ test`/`lint-full` clean
- [x] 3.3 Update `recall` SKILL.md: `runbook` notes surface and rank exactly like `fact`/`feedback` in Step 2's clustering/coverage judgment — no new Step-1 phrase, no new query flag. Verified via one comprehension-check scenario (lighter than section 2's routing-logic RED/GREEN, proportionate to a pure enumeration/reference completeness fix — no new judgment logic added). Scoped to the READ side only, per design.md: recall's Step 2.5C/Step 4 *write* path (crystallizing new synthesis notes) still only offers kind=fact/feedback — recall doesn't gain a "write a runbook" judgment here, that's `learn`'s kind-4 job (section 2). Flagged to Joe as a possible follow-up, not filed as a tracked issue.

## 4. Close-out

- [x] 4.1 Update `docs/architecture/adr.md` with a new ADR recording the `runbook` kind decision (name + three-questions schema, provenance: Joe 2026-08-23, vault note 789), the retrieval-treatment decision (situation-similarity only; a `task_type` mechanism was considered and rejected — design.md Decision 3, Non-Goals), and the Luhmann-participation decision (runbooks get a Luhmann ID via the same disposition judgment as fact/feedback, not a kind-prefixed flat name — design.md Decision 1) — ADR-0026
- [x] 4.2 Ran full-repo `go build`/`go test` (all packages green), `lint-full` (0 issues), `reorder-decls-check` (258 files, all ordered), `check-thin-api` (pass), `deadcode` (pass), `openspec validate --strict` (valid). `check-coverage-for-fail`/`check-nils-for-fail` remain excluded — both verified pre-existing/environmental (baseline-broken and host-memory-pressure-blocked respectively; see task 1.5, memory `nilaway-oom-gomemlimit-fix`). All four touch points confirmed end-to-end via the installed binary from a non-vault, non-repo cwd (`~`): `engram learn runbook` wrote a correctly-shaped note (`luhmann:`/`situation:`/`done_when:` frontmatter, `<luhmann-id>.<date>.<slug>.md` filename) with a real embed sidecar; `engram query` returned it with `kind: runbook`, uncounted score 1.0, no exclusion. `write-memory`/`learn` skill touch points verified via the section-2 RED/GREEN subagent scenarios (skills are markdown, not directly executable).
- [ ] 4.3 Run `/opsx:archive` to archive this change and sync `openspec/specs/`
