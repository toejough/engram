## 1. Corpus discovery and preconditions

> Environment split (design D-K): tasks 1.x and 5.x run on the HOST (real corpus + git vault). Tasks 2.x-3.x build and fixture-test the instrument and may run anywhere; their real-data verification sub-steps (2.2, 3.2's live fingerprint, 3.3's hand-labels) also need the host corpus.

- [ ] 1.1 Verify transcript paths live (never assume): (a) Claude Code — main session `.jsonl` files under `~/.claude/projects/<mangled-path>/`, subagent transcripts under `<session-id>/subagents/**` INCLUDING `subagents/workflows/**`; (b) Pi — search `~/.pi/` for an analogous layout and document the actual structure, or document its absence and scope to Claude Code only (design D-A fallback). Verification: a checked-in corpus manifest (`dev/eval/audit/corpus.json`) listing every discovered root with per-repo counts of main and subagent transcripts.
- [ ] 1.2 Categorize and sample: bucket transcripts by repo × role (main/subagent) × dispatch type (fork/fresh/workflow, from the transcript's own dispatch metadata) × size; define the stratified single-read-sized sample and the windowing plan for sessions ≥ 500 KB (design D-A). Verification: manifest records total counts, sample counts per stratum, and the window boundaries chosen for each oversized session.
- [ ] 1.3 Verify oracle preconditions: (a) the vault is a git repo — record first/last commit dates and commits-per-week density, and from it the expected DERIVED coverage over the corpus's time range (moments before dense history get ESTIMATE); (b) confirm chunk records carry `IngestedAt` by citing the struct field in `internal/chunk/` at HEAD. Verification: an `oracle-preconditions` section in the manifest with the density table and the code citation.

## 2. Mechanical extractor (full corpus, no LLM)

- [ ] 2.1 Build `dev/eval/audit/extract.py` + unit tests on small checked-in synthetic fixture transcripts. Per transcript it emits structured events: recall invocations (Skill call + the `engram query` tool result parsed for phrases and items with path/kind/provenances/rank), dispatch events (Agent tool calls: prompt text, subagent_type, model), learn/write-memory Skill calls, `engram learn|amend|activate` Bash invocations, completion reports. Fixtures must cover: a main session with a recall + a dispatch, a fork subagent, a fresh subagent, a workflow agent. Verification: `pytest dev/eval/audit/` green (environment note: bootstrap `pytest` via `python3 -m ensurepip` + pip if absent; `go` may need an explicit PATH export before `go install ./cmd/engram` for task 3.2's oracle).
- [ ] 2.2 Hand-verify the extractor against ≥ 2 REAL transcripts (one main with ≥ 1 recall + ≥ 1 dispatch, one subagent): compare its event list to a hand-read of the same file; fix discrepancies; document the comparison. Hand-verify every dispatch type the host corpus actually contains; the workflow type may exist only as the synthetic fixture until the host run — note that in the comparison doc. A value must be measured against the artifact it describes (vault note 478). Verification: comparison doc checked in beside the tests.
- [ ] 2.3 Pi transcript support in the extractor, or the documented exclusion from 1.1(b) carried into the manifest.

## 3. Semantic auditor (sampled windows)

- [ ] 3.1 Build the auditor prompt + runner (`dev/eval/audit/audit_moments.py`): windowing per 1.2; detects moments of every type in design D-D (failure / correction / rework / SUCCESS / DISPATCH — successes are first-class, not optional); judges the funnel columns; over-match then prune to NOT-A-MOMENT in-agent; emits per-moment JSONL joined to the mechanical events on (transcript, anchor). Verification: runner executes end-to-end on one fixture window with a stubbed (canned-response, no-network) model; real runs use headless `claude -p` at the tiers pinned in design D-B (detection haiku; FN re-audit sonnet; judgment sonnet).
- [ ] 3.2 Oracle sub-step inside the runner: derive per-moment oracle phrases; build the as-of-T note set (read-only `git worktree add --detach` at last commit ≤ T; temp dir; ALWAYS cleaned up) and chunk set (`IngestedAt ≤ T`); run `engram query` under `dev/eval/isolation.py` isolation (#708 — assert_isolated before any invocation); judge exists/kind/generic-vs-idiosyncratic; stamp the record's oracle method DERIVED or ESTIMATE per design D-C. Verification: an isolation canary (empty scratch vault → zero results) recorded in the runner's tests; live vault fingerprint asserted unchanged after a trial run.
- [ ] 3.3 Calibrate BEFORE any full run: hand-label a stratified sample of 10–20 windows (both roles; ≥ 2 repos — a host-side requirement, see D-K; a single-repo dev-env corpus cannot satisfy it); measure the detector's false-negative (miss) rate against the hand labels; pre-register the miss-rate tolerance the full run must satisfy; run the FN check (a stricter model re-audits the low-yield windows, per the 2026-06-28 precedent). Then hand-read ≥ 3 full trial outputs against the scorer/join logic (vault note 505: calibration signal exists only before the numbers look like data). Verification: calibration CSV/JSON + the pre-registered tolerance committed before task 5 starts.

## 4. Pre-registration (all before the full run — order is load-bearing)

- [ ] 4.1 Pin fire-units per axis: primary per-moment; secondary per-dispatch (read-side gap) and per-transcript. Recorded in the report skeleton.
- [ ] 4.2 Pre-register the Phase 2 gate: the write-side threshold (% of lost lessons reachable by a completion-report LESSONS harvest), the read-side threshold (% of dispatch-moment losses reachable by dispatch injection), and the acceptable-outcomes list mapping results → GREEN / YELLOW / RED with the consequence of each (GREEN: file Phase 2; YELLOW/RED: named scope revision). Flow: the executor DRAFTS the thresholds with one-paragraph rationale each, presents them to Joe, and commits them only on approval — task 5 never runs on unapproved thresholds. Committed before task 5.
- [ ] 4.3 Estimate the full-run LLM cost from the calibration runs, confirm with Joe, then run without mid-run budget interrupts.

## 5. Full run

- [ ] 5.1 Mechanical extractor over the FULL corpus; semantic auditor over the pre-registered sample; join. Raw outputs under `dev/eval/audit/results/` (per-moment JSONL, per-transcript event JSONL).
- [ ] 5.2 Compute the funnel: per-stage loss rates at the pinned fire-units; klass split per runbook 824; kind and generic/idiosyncratic splits; per-repo spread (retention-bias check); every number labeled DERIVED or ESTIMATE. Compute the #739 slice: write-side lost-lesson counts and reachable fraction; read-side dispatch had-vs-injected gap (forks counted as injected, design D-E).
- [ ] 5.3 Emit the credit-record JSONL per design D-F, plus a schema doc (`dev/eval/audit/CREDIT_SCHEMA.md`) the credit-loop change will build against. NO vault writes.
- [ ] 5.4 Gap harvest: cluster residual moments (capture-ceiling and uncovered-cue classes) and draft remedies per the note-495 ladder — vault-note remedies by default; a repo-change proposal only where the note-803 test says a note cannot reach (procedure-execution bugs). Output: a proposals section in the report, not filed issues yet.

## 6. Report, LEDGER, gate, and issue trail

- [ ] 6.1 Write the report (`dev/eval/audit/REPORT-<date>.md`): labeled criteria table (stage × rate × fire-unit × label), one short narrative per major finding, honest-limits section (detector conservatism, oracle coverage, sample bounds).
- [ ] 6.2 GATE (user-facing durable records — present, then write): show Joe the funnel verdicts, the #739 slice, and the proposed gate decision; on approval write the `dev/eval/LEDGER.md` rows (one per claim, existing format, raw-data paths) and add the coverage-map index section (memory function × kind → evidencing rows + measured rate; every existing row and anchor untouched). Verification: `grep -c 'a id=' dev/eval/LEDGER.md` unchanged-or-grown, no anchor renamed.
- [ ] 6.3 Record the Phase 2 gate decision per the pre-registered mapping — never re-derived after seeing the numbers.
- [ ] 6.4 Issue trail per ADR-0028 Consequences (after 6.2's approval): rewrite #739's BODY as one self-contained current brief (shape per vault note 819, quoted: one coherent current narrative, never stacked correction blocks; include a 'State of the world: exists / does-not-exist' section verified against the repo at write time; the body records the decided path — Option A gated on this audit, Option B parked with its revisit conditions — while the historical A/B/C analysis stays in the comment trail); file the two follow-on issues (credit loop; replay + mine) citing ADR-0028; narrow #718 (retrieval-strength half → experience mechanism); annotate #742 (situation-conditioned demotion = soft-suppression evidence).
- [ ] 6.5 `openspec validate memory-loop-audit --strict` green, then sync/archive (`/opsx:sync` or `openspec archive memory-loop-audit`) to propagate the `memory-loop-audit` capability spec into `openspec/specs/`. Commit the instrument, results, and doc changes per the repo's normal workflow (review before merge; ff-only) before archiving.
