Conventions for every task: TDD (write the failing test, run `targ test`, see RED, implement, GREEN,
refactor). Test stack: gomega `NewWithT(t)`, `pgregory.net/rapid` for properties, `t.Parallel()` on
every test/subtest with no shared mutable state. No `os.*`/direct I/O in `internal/` — use the
injected `QueryDeps`/`LearnDeps` fakes the neighboring tests use. Reach unexported symbols from
`cli_test` via aliases in `internal/cli/export_test.go`. Run `targ check-full` (not `targ check`)
before ticking any task in section 4. All paths under `internal/cli/` unless stated.

## 1. Frontmatter field + learn/amend flags

- [x] 1.1 RED: in `learn_test.go` (mirror the runbook `--red-flag` cases ~L1190-1290) add tests: `engram learn runbook … --trigger "/please" --trigger "take this end-to-end"` writes `triggers:` with both entries in order (after `red_flags:` when present); no `--trigger` → no `triggers:` line; `--trigger "  "` and `--trigger "ab"` fail with an error naming the trigger and write nothing
- [x] 1.2 GREEN: add `Triggers []string` to `LearnArgs` (`learn.go:~66-70`, beside `RedFlags`), `runbookFields` (`~334`), `runbookFrontmatterDoc` (`~345`), `LearnRunbookArgs` (`targets.go:~60`, targ `name=trigger`, repeatable); thread through `learnArgsFromRunbook` (`learn.go:~550-570`), `assembleRunbookContent` (`~445-466`), `renderRunbookFrontmatter` (`~700-719`); add a `validateTriggers([]string) error` (trim, reject empty / < 3 chars) called before any write
- [x] 1.3 RED→GREEN: `learn_adapters_test.go` (mirror L313-340) proves repeated `--trigger` reaches `LearnArgs.Triggers` in order
- [x] 1.4 RED→GREEN: `amend_test.go` (mirror L555-600): `engram amend <runbook> --trigger X` replaces the whole list; note re-stamped; `Triggers []string` on `AmendArgs` (`amend.go:~43`), `applyRunbookAmend` (`~345-395`, `slices.Equal` change detection), `renderAmendedRunbook` (`~672-699`); same `validateTriggers`
- [x] 1.5 Property (rapid): for any list of 1–5 valid trigger strings (printable, may contain `:`/`"`/unicode), learn→read-back→parse yields the identical list; render is valid YAML (round-trip through the same YAML decoder the code uses)
- [x] 1.6 Verify sidecar invariance: test in `internal/embed/hash_test.go` (or nearest) that `ContentHash` is identical for a runbook with and without a `triggers:` block; and that `runbookFrontmatterDoc` decoding tolerates an unknown key (older binaries reading newer notes)

## 2. Query: `--text`, trigger index, surfacing

- [x] 2.1 RED: in `query_runbook_test.go` (uses `newInMemoryFS`/`plantNoteWithSidecar`/`newQueryDeps` from `query_helpers_test.go`), plant: a runbook R with `triggers: ["/please", "take this end-to-end"]` and a situation unrelated to the phrases; a feedback F that matches the phrases strongly. Assert for `RunQuery` with `Text: "/please fix the flaky login test"` + two phrases: `items[0].Path == R`, `items[0].Kind == "runbook"`, `"trigger" ∈ items[0].Provenances`, F follows
- [x] 2.2 RED (same file): no `--text` → payload byte-identical to a golden captured before the change for the same vault (capture the golden in the test by running RunQuery with `Text: ""` on a vault with no triggers and comparing to a vault where R has triggers)
- [x] 2.3 RED: case/whitespace insensitivity (`"PLEASE   take this end-to-end: x"` hits `take this end-to-end`); a `fact` note with a `triggers:` list never gets `trigger` provenance; `--text` with zero `--phrase` returns just the hit(s); `Limit: 1` returns the hit plus one similarity item
- [x] 2.4 GREEN: add `Text string` (targ `name=text`) to `QueryArgs` (`query.go:23-46`); add `provenanceRoleTrigger = "trigger"` and `provenanceRankTrigger = 4` beside the constants at `:150-157` and map it in `provenanceRankFor` (`:1557`); extend `loadAllVaultNotesMeta` (`query_vault_meta.go:151-203`) to parse `triggers:` into a `[]string` on `noteQueryFrontmatter` (string-scan idiom of `capRedFlagsForPreview` or the YAML struct) for `type: runbook` notes only; add `matchTriggers(text string, meta) []path` implementing `normalize = strings.ToLower + collapse whitespace`, `strings.Contains`; in `runQuery` (`:1833-1893`) after `assembleResolvedItems`, inject/merge hits as `resolvedItem`s with the `trigger` role (keep any existing similarity score/roles), bypassing `applyFloorAndCap` (`:572-599`) and `capWithNoteFloor` (`:894-916`); in `renderQueryPayload`/`capItemsToLimit` (`:880, :1719-1771`) exclude trigger hits from the limit count; hits render with the same `red_flags` preview cap as other runbooks
- [x] 2.5 Property (rapid): for random `text` and random trigger lists, `matchTriggers` hits iff normalized substring holds; ordering invariant — every item with `trigger` provenance precedes every item without it, for arbitrary score vectors (use `resolvedItemLess` via export alias)
- [x] 2.6 Relax `RunQuery`'s "at least one phrase" validation (`query.go:69-101`) to "at least one phrase or non-empty text"; test both error and success paths
- [x] 2.7 Served path: `buildQueryParams` (`serve_client.go:36-51`) and `serveQuery` (`serve.go:371-385`) carry `text`; RED→GREEN in `serve_client_test.go`: round-trip a 1 KB text with quotes, newlines, `&`, `=`, `%`, and non-ASCII yields the identical string; cap `text` at 2 KB (truncate, no error) and test the boundary

## 3. Quality gates on the binary

- [x] 3.1 `targ test` green; `targ check-full` green (lint, nilaway, coverage, thin-API); fix everything it reports — no suppressions (2026-09-20: only remaining failures are check-uncommitted (tree uncommitted) and the 4-5 dev/eval fixture lint findings awaiting Joe: gochecknoglobals + revive package-comments)
- [x] 3.2 `go install ./cmd/engram`; smoke with the real binary from a non-repo cwd against a scratch vault: `engram learn runbook … --trigger "/zz-smoke"`, then `engram query --text "/zz-smoke do a thing" --phrase "unrelated"` shows the note first with `provenances: [trigger]`; `engram show` renders `triggers:`; `engram embed status` shows 0 stale after the learn and after an `engram amend --trigger`

## 4. Specs, docs, ADR

- [x] 4.1 `openspec validate runbook-lexical-triggers --strict` passes; sync delta specs into `openspec/specs/` on archive
- [x] 4.2 `docs/architecture/adr.md`: amend ADR-0026's "no dedicated ranking mechanism" rationale with the measured please failure (parked change `please-skill-to-runbook`, results `3.3_*`/`3.3d_*`: paraphrase drops the cue; 0/95 lift from a literal semantic phrase) and the single lexical exception
- [x] 4.3 `docs/GLOSSARY.md` (entries at ~L273/337/372/382/391/397 naming items[]/provenance) and `docs/architecture/c3-components.md` (K6 row ~L104, payload shape ~L139-142, ~L191): document `--text`, `triggers`, provenance `trigger`
- [x] 4.4 Run the doc-surface enumeration grep for `provenance`, `--phrase`, `items[]`, `red_flag` across docs/, agent-instructions/, openspec/specs/; paste the per-file disposition list into this file under a "Enumeration" heading; update every "update" row
- [ ] 4.5 On archive/sync, edit the main spec `openspec/specs/recall-runbook-surfacing/spec.md` Purpose (deltas cannot modify Purpose): replace "no dedicated query flag, no dedicated ranking mechanism" with "no dedicated ranking mechanism beyond one author-declared literal-trigger exception (`--text` / `triggers`, capability `runbook-lexical-triggers`)"

## 5. Guidance + template

- [x] 5.1 `agent-instructions/guidance/shim.md`: add `--text "<the user's message, word for word>"` to the first-action query block and one sentence: pasted verbatim (first ~300 chars if long), never rewritten; record the byte delta vs HEAD in this file
  - Byte delta (2026-09-20): HEAD 7550 B -> working tree 8315 B = +765 B vs HEAD; of that, +559 B is the pre-existing uncommitted phrase-two re-check paragraph (working tree was 8109 B before this edit) and +206 B is this task's `--text` line + one sentence
- [x] 5.2 `agent-instructions/skills/write-memory/SKILL.md` (handoff contract ~L20-33, runbook template ~L64-76): add optional `triggers` → repeated `--trigger`; add the authoring rule (distinctive cue, never a lone common word; a hit is still a candidate). Use the `superpowers:writing-skills` TDD flow for this SKILL.md edit
  - STATUS: edit in place (contract field, `--trigger` template line after `--red-flag`, authoring rule). RED = no-spend read-through: the prior skill had no `triggers` field and its "never mix flags" rule would make a worker drop or improvise it. NOT ticked: the writing-skills GREEN gate (5+ reps vs a no-guidance control, fresh-context) needs paid runs, est. ~$1-3 (2 arms x 5 reps x ~$0.10-0.30, haiku/sonnet single-shot); awaiting Joe's go
  - GREEN (2026-09-20): 12 fresh `claude -p --model sonnet` runs per arm (6 scenarios x 2 reps; no tools, compose-only, nothing executed, no vault touched; `--system-prompt` = skill text only, `--setting-sources ""`, slash commands off; arms differ only by SKILL.md: HEAD vs edited). Scenarios: a1/a2 triggers listed; b none; c1 lone `please`, c2 lone `fix` + `/rewrite-history`; d red_flags + triggers. GREEN 12/12: a repeated `--trigger` exact strings (4/4), b no `--trigger` (2/2), c asked parent for a distinctive cue (4/4), d `--red-flag`s then `--trigger`s (2/2). Control (old skill) 2/12: only b passed; a and d silently dropped triggers (6/6 dropped, noted 'not in contract'), c silently dropped instead of flagging (0/4). Cost ~$0.27 total. No defect found; SKILL.md unchanged.
- [x] 5.3 Deploy: `engram update --with-guidance` (reinstalls the binary from this checkout — intended); verify `~/.claude/engram/guidance/shim.md` and the Pi copy contain the `--text` line and `~/.claude/CLAUDE.md` still imports the shim — DONE 2026-09-20: update ok (rev e0a85087+wt); shim byte-identical in ~/.claude/engram/guidance, ~/.pi/agent/guidance, ~/.pi/agent/engram/guidance; CLAUDE.md+AGENTS.md import shim; skills identical; embed status 979/0 stale/0 broken; --text smoke ok

## 6. Validation (paid; estimate and confirm cost with Joe first)

- [x] 6.1 (RAN 2026-09-20; original strict bar not met at 24/30; bar amended by Joe after the run — strict 24/30, whitespace-tolerant 29/30 vs bar 27/30; RED 0/30; contamination gate passed; residual risk: 1/30 dropped the opening prefix; see design D9 amendment and results/6.1_text_fidelity_shim_sonnet5.md) Headless RED/GREEN for `--text` fidelity (pattern: parked change's `results/3a3_3a4_shim_fix_validation_sonnet5.md`; each arm a fresh `claude -p` with the shim file as the only variable; RED = shim without the `--text` line; GREEN = current): 5 varied asks (one starting `/please`, one "take this end-to-end", three plain multi-step asks) × 3 trials × 2 arms. Score the first `engram query`: does `--text` contain the user's message verbatim (deterministic substring check)? Print every `--text` value. Bar: GREEN ≥ 27/30, and none equal to the shim's own example text. ~$4–8
- [x] 6.2 (RAN 2026-09-20; bar met: runbook surfaced with trigger provenance 3/3, --text in 3/3; results/6.2_please_triggers_sonnet5.md) Trial-vault please rerun: in the phase2 harness, amend the please top runbook fixture (`encodings/taskPlease/Please-R/vault/6.*.md`) with `triggers: ["/please", "take this end-to-end"]` via `engram amend` on the trial vault (or regenerate the fixture through `engram learn runbook`), rebuild sidecars (0 stale), then `probe_phase2.py --task please --arms R --shim-only --model sonnet5 --n 3 --keep`. Validity gate: `--text` present in each trial's first query. Bar: runbook surfaced 3/3 with `trigger` provenance. Record in `results/6.2_please_triggers_sonnet5.md`. ~$4 (2026-09-20 note: after 6.5, Joe added the plain word `please` as a third trigger, so the list is now `["/please", "take this end-to-end", "please"]`; this is a deliberate exception to the write-memory authoring rule, and over-firing is accepted because a trigger hit is only a candidate the agent judges against the runbook's own applicability guard. Real vault note 1045 and fixture note 6.* were both re-amended; sidecars 0 stale.)
- [x] 6.3 (RAN 2026-09-20; found 3/3, followed 10/10 x3, --text in 3/3; results/6.3_route_regression_triggers_sonnet5.md) Route regression: `probe_phase2.py --task route --arms R --shim-only --model sonnet5 --n 3`; bar: found 3/3 as before. ~$2
- [ ] 6.4 If 6.1 fails its bar: stop, report, and file the prompt-submit-hook follow-on as a new issue; do not tick 6.5
- [x] 6.5 (DONE 2026-09-20; show renders triggers, embed status 0 stale, `/please anything` from ~ returns 1045 as items[0] with trigger provenance, `please read this file` does not) Amend the promoted please runbook in the real vault: `engram amend 1045.2026-09-20.please-drive-ask-end-to-end --trigger "/please" --trigger "take this end-to-end"`; `engram query --text "/please anything" --phrase x` from a non-repo cwd shows it first; `engram embed status` 0 stale (2026-09-20 note: after 6.5, Joe added the plain word `please` as a third trigger, so the list is now `["/please", "take this end-to-end", "please"]`; this is a deliberate exception to the write-memory authoring rule, and over-firing is accepted because a trigger hit is only a candidate the agent judges against the runbook's own applicability guard. Real vault note 1045 and fixture note 6.* were both re-amended; sidecars 0 stale.)

## 7. Close-out

- [x] 7.1 (DONE 2026-09-20; six rows added after `shim-follow-frame-d8-bar` in `dev/eval/LEDGER.md`: `please-semantic-retrieval-unreliable` (refuted; 0/3 -> 2/3 -> D11 0/95), `runbook-lexical-triggers-text-fidelity` (strict 24/30, whitespace-tolerant 29/30 vs RED 0/30, bar amended), `runbook-lexical-triggers-please-surfacing` (3/3 surfaced, end_state 3/3), `runbook-lexical-triggers-please-followed-all` (unmeasured; 0/3 scorer artifact, 17-18/19 with `after` removed), `runbook-lexical-triggers-route-regression` (3/3), `write-memory-triggers-contract` (12/12 vs 2/12)) Record the eval outcomes in `dev/eval/LEDGER.md`
- [x] 7.2 (DONE 2026-09-20; PARKED block replaced with UNPARKED status citing 6.2: surfaced 3/3 with trigger provenance, end_state 3/3; R1 chosen by Joe) Update `openspec/changes/please-skill-to-runbook/tasks.md` PARKED block: unblock condition met (or not), with the 6.2 numbers
- [x] 7.3 (commits made: 00efc1c6 feat(query), 00c9299c docs(openspec), 8468823a feat(guidance), ec5f3735 chore(eval); verified with `git log`; the 7.1/7.2 close-out edits themselves are still uncommitted at this stage) Commit with `/commit` (trailer `AI-Used: [claude]`); reference #758 in prose only — this change does not close it

## Enumeration

Greps run over docs/, agent-instructions/, openspec/specs/, README.md, CLAUDE.md (docs/research, docs/superpowers, docs/design excluded as dated historical records): `provenance`, `--phrase`, `items[]`, `red[_ -]?flag`, `triggers?:`, `engram query`, `engram learn runbook`, `--red-flag`; then a broader re-run (situation.similarity, ranks purely, lexical, verbatim, per-phrase, red-flag/redflag, runbook, `--text`, shim/follow-frame/first-action) which surfaced 9 extra files, all dispositioned below.

| File | Disposition | Reason |
|---|---|---|
| docs/architecture/adr.md | update (done) | ADR-0026 amendment: measured please failure + single lexical exception |
| docs/GLOSSARY.md | update (done) | `provenances` gains `trigger`; `surfaced notes` mentions hits; new `trigger` and `trigger hit` entries; other provenance/`--phrase` hits (explore/recent/escalation/source) unaffected |
| docs/architecture/c3-components.md | update (done) | K6 row, payload shape, verified-order line, skills->query edge label, sequence-diagram note for `--text`/`applyTriggerHits` |
| README.md | update (done) | `engram query` line gains `--text`, trigger-hit and `--limit` exemption; added missing `engram learn runbook` line with `--red-flag`/`--trigger`. BREAKING `--limit` note: see its own row below |
| agent-instructions/guidance/shim.md | update (done, 5.1) | first-action block gains `--text` + verbatim sentence; re-entry paragraph keeps its own re-keyed phrases (not the user's message): keep |
| agent-instructions/skills/write-memory/SKILL.md | update (done, 5.2 edit; GREEN unrun) | `triggers` handoff field, `--trigger` template line, authoring rule |
| docs/architecture/c1-system-context.md | keep | recall-skill sequence diagrams (`--phrase p1..p10`); recall's own query passes no `--text` |
| docs/architecture/c2-containers.md | keep | recall-skill query pipeline diagram (per-phrase stages) unchanged; the trigger stage is documented at L3 (c3) |
| docs/architecture/memory-invariants.md, memory-system-rigor.md | N-A | retired tier/`--tier` invariants; "items[]" mention is historical |
| docs/ROADMAP.md | N-A | issue-chain planning table; no query-surface claims |
| agent-instructions/skills/recall/SKILL.md | keep (flag) | L131 "ranks purely by situation-similarity" remains true for recall's own queries (no `--text`), but the L131-132 sentence 'no dedicated Step-1 phrase or query flag of its own' is no longer accurate of the mechanism: triggers are runbook-only and `--text` is their flag. Editing a SKILL.md needs a writing-skills cycle, so the rewording is deferred to the #760 recall-to-runbook change |
| agent-instructions/skills/learn/SKILL.md | keep | runbook handoff (L189-194) lists no red_flags/triggers either; triggers are authored via explicit handoff from a parent that has them, none exist yet |
| agent-instructions/skills/curate/SKILL.md | keep | `engram query --phrase` is a semantic lookup of an offer's situation, no user message |
| agent-instructions/skills/please/SKILL.md | keep | out of scope (parked change owns it); "Escalation provenance" is unrelated |
| agent-instructions/guidance/delegate.md, skills/recall/tests/* | N-A | no query-surface claims / recall baselines |
| README.md `engram amend` line | update (done) | gained `--done-when/--body`, `[--red-flag]`, `[--trigger]` per AmendArgs |
| README.md BREAKING `--limit` note | update (done) | dated notice, but 'every mode' now carries the trigger-hit exemption |
| docs/GLOSSARY.md `trigger hit`; README `engram query` line; c3-components K6 | update (done) | caveat: a runbook is a hit only with a compatible sidecar |
| openspec/specs/recall-runbook-surfacing Purpose | update at archive (task 4.5) | deltas cannot edit Purpose; it states 'no dedicated query flag/ranking mechanism' |
| openspec/specs/recall-runbook-surfacing | keep (delta exists) | this change's delta narrows the purity requirement; main spec syncs on archive |
| openspec/specs/learn-runbook-capture, guidance-runbook-follow-frame, write-memory-worker | keep (delta exists) | deltas in this change; sync on archive |
| openspec/specs/recall-payload-cuts | keep (flag) | says `--limit` caps `items[]` Channel 1; trigger hits are exempt. Covered by this change's `runbook-lexical-triggers` spec ("--limit does not drop a hit"); no delta on recall-payload-cuts, add one at archive if Joe wants the exemption stated in-place |
| openspec/specs/vault-merged-recall | keep (flag) | `--limit`/merge semantics with `ENGRAM_PARENT` not addressed by this change's tasks; verify separately |
| openspec/specs/recall-two-channel-payload, recall-query-timings, recall-centroid-sampling, recall-glance-deep-dial, recall-matched-note-floor, vault-query-model-provenance (model_id, not item provenance), retrieval-probe-signal-fidelity, ingest-prune-detach, vault-offer-curation, vault-vocab-lifecycle, vault-note-identity, learn-qa-capture, route-*, memory-loop-audit, production-guidance-activation, update-deploy-sync | N-A | mention `engram query`/`items[]`/`provenance`/`verbatim` in unrelated senses; no claim contradicted by `--text`/`triggers` |

