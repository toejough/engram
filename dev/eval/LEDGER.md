# Ledger — tested results

**Charter:** one unified account of what engram is and is not proven to do. This is the single place
to check a claim's status — proven, refuted, unmeasured, or superseded by a later measurement — instead
of tracking several distinct per-harness docs that can silently rot out of sync with each other.
**Updating this ledger is part of every eval's definition-of-done.** ROADMAP and the openspec specs cite rows
here (`dev/eval/LEDGER.md#<slug>`); they never restate a figure inline.

**Row unit:** one tested claim/capability. **Verdict vocabulary (exactly four values):**

| verdict | means |
|---|---|
| `proven` | measured, holds |
| `refuted` | measured, does not hold |
| `unmeasured` | not yet validly measured (inconclusive, borderline, or the attempt was instrument-invalid) |
| `superseded` | a later measurement of the same claim replaced this figure — see `superseded-by` |

**Raw data:** a live path where the source survives on disk; the
`『<path> deleted 2026-07; git log』` form when the underlying doc was deleted in the 2026-07
docs restructure (content recoverable via `git log`); or the
`『<path> pre-<change> history; git log』` form when the file survives but the cited figure was
edited out of it (e.g. the pre-restructure ROADMAP) — recover the earlier content via `git log`.

---

| claim | verdict | figure (vintage) | superseded-by | raw data |
|---|---|---|---|---|
| <a id="tactical-convention-cold-baseline"></a>**Cold opus violates un-guessable local conventions** | proven | 5 tactical traps confirmed reproducible ≥4/5: `req-with-context`, `nocolor`, `t-parallel`, `nil-guard-split`, `wrapped-error` — each 5/5 cold-violates; cost $6.94/55 trials (~$0.13/trial) (2026-06-22) | — | `dev/eval/traps/RESULTS.md` |
| <a id="behavioral-trap-null-result"></a>**Behavioral/conceptual corrections are cheaply reproducible via clean isolated traps** | refuted | 4 behavioral traps (`verify-before-done`, `commit-core-artifact`, `over-engineer`, `scope-discipline`) at the 5/5 bar: all 4 came back 0/5 — cold opus already does the right thing every trial (verdicts verified genuine); cost $2.34/20 trials (2026-06-22) | — | `dev/eval/traps/RESULTS.md` |
| <a id="tactical-convention-warm-flip"></a>**Memory flips tactical-convention traps cold→warm (C3 mechanism)** | proven | all 5 tactical traps: cold 5/5 violates → warm 5/5 applies, recall fired 5/5 every trial; cost $9.62/25 trials (recall overhead ~$0.30–0.50/trial vs ~$0.13 cold); single-convention isolation, cleanest recall condition (2026-06-22) | — | `dev/eval/traps/RESULTS.md` |
| <a id="crowded-vault-capability-robustness"></a>**The 4 capability wins (C3 apply-conventions, C4i recency-supersession, C5 honor-standard, C6 abduction) survive a realistic crowded vault** | proven | swept 0→400 notes, zero degradation: C3 25/25→25/25, C4i 5/5→5/5, C5 5/5→5/5, C6 8/8→8/8, Δ0, all GREEN; retrieval rank flat 0→400 (C3 r5, C4i r1, C6 r4); application unchanged at ~200-note payload depth (~$36). Bound: same-domain *competing* notes still untested (2026-06-26) | — | `dev/eval/traps/RESULTS.md` |
| <a id="matched-note-floor"></a>**Matched-note floor fixes note-vs-chunk drowning** | proven | real-path note recall@5 0.22→0.83 (embedder's isolation ceiling), `capWithNoteFloor`, trap gate GREEN (2026-06-28, Done-section/canonical figure); same-fix diagnostic cut of the drowning gap cited elsewhere as 0.19→0.83 (same 2026-06-28 vintage, narrower real-path slice); isolation-only embedder baseline was 0.81 pre-fix | — | 『docs/design/2026-06-28-retrieval-probe-results.md deleted 2026-07; git log』 |
| <a id="diagnostic-surfacing-probe"></a>**Symptom→cause diagnostic notes surface cleanly (separate from the note-floor fix)** | proven | 75 symptom→cause probes: real-path note recall@5 = 0.99, no gap found (2026-06-28) | — | 『docs/design/2026-06-28-question-shaped-crystallization-proposals.md deleted 2026-07; git log』 |
| <a id="crystallization-audit-wording-gap"></a>**Cluster-driven notes read as question-useful** | proven | ~half of cluster-driven notes (recall Step 2.5) are not question-shaped in wording: 40% vs 79%; a note-*wording* audit, not a delivery test — see `qanchor-park` for the delivery-side finding this motivated (2026-06-28) | — | 『docs/design/2026-06-28-crystallization-audit.md deleted 2026-07; git log』 |
| <a id="recall-time-mislabel"></a>**The `recall_s` harness field measures recall time alone** | superseded | `recall_s` (~350 s) was mislabeled: it timed round-1 = recall + the first full code build, not recall alone (2026-06-25) | [recall-time-isolated](#recall-time-isolated) | 『docs/design/2026-06-25-memory-cost-reanchor.md deleted 2026-07; git log』 |
| <a id="recall-time-isolated"></a>**Recall-only wall-time, isolated from the first build round** | superseded | recall-only ≈190 s — *below* the cold build (~288 s); ~82–118 s (~43–63%) is Step 2 agent-side payload paging (binary `engram query` itself ~3.5 s; ~8 re-queries/pages of a ~200 KB payload); n=2, directional (2026-06-25) | [recall-time-remeasure](#recall-time-remeasure) | 『docs/design/2026-06-25-recall-cost-isolation.md deleted 2026-07; git log』 |
| <a id="recall-time-remeasure"></a>**Recall-only wall-time re-measured post-cuts (lazy-chunks/recent-fill/O2/L2/L3a)** | proven | real-vault-copy median 51.9 s, range 39.3–63.6 s (n=3, directional — reproduces the ~190 s prior's own real-vault-copy method (whose regime was ~370–410 chunks / ~141–237 KB)); vs. the ~190 s prior (`recall-time-isolated`, n=2, pre-cuts vintage) that is a ~73% drop, but the drop is **directional, not a controlled A/B** — the trial prompt differs from the prior's task. Small-fixture floor (seed_c3, no paging regime, recorded separately, never pooled): median 39.7 s, range 31.3–41.4 s (n=3). Band-edge conservatism (the range straddles the 60 s boundary) lands the disposition in the 60–120 s band: the pre-registered mapping recommends closing #657, with the parked clusters-first/lazy-content payload restructure named as the remaining lever (its ROADMAP revisit-condition stands) — Joe disposes (2026-07-12) | — | `dev/eval/traps/recall_time.py` (commits `efdc4198`, `953cd820`); `docs/superpowers/plans/2026-07-11-657-remainder.md`; artifact `dev/eval/cumulative/recall_time/657-recall-time.json` (`vault_copy` key) |
| <a id="c1-c2-warm-op-mislabeled"></a>**Warm-vs-cold op-level cost/time delta (original accounting)** | superseded | NET op +327 s slower / +$1.72 costlier — built on the `recall_s` mislabel above, an unclean warm-rounds-2+-vs-cold-whole-op comparison (2026-06-25) | [c1-c2-warm-op-negatives](#c1-c2-warm-op-negatives) | 『docs/design/2026-06-25-memory-cost-reanchor.md deleted 2026-07; git log』 |
| <a id="c1-c2-warm-op-negatives"></a>**Memory nets cheaper/faster end-to-end (C1 time / C2 dollars)** | refuted | clean, adversarially-verified warm-vs-cold, n=8/arm opus, 3 small CRUD apps: NET op +182 s slower (beyond noise, d/SE 3.2, CLES 0.84) / +$3.08 costlier (beyond noise, robust, d/SE 5.1, CLES 0.97, warm loses 7/7); build-phase *time* indistinguishable, build-phase *$* +$1.00 warm (real, not artifact); "consuming apps build faster" amortization hope was an artifact (pooled −45 s → −5.5 s once paired). Scope: tasks too convergent (6/8 cold cells zero-rebuild) — the harder-regime re-test was ATTEMPTED 2026-07-17 (#642, see [harder-regime-op-cost-unmeasurable](#harder-regime-op-cost-unmeasurable)) and found op-cost not cleanly measurable via a self-seeding build eval (no Goldilocks window). **PRE-PAYLOAD-CUT vintage** — predates the 2026-06-27 lazy-chunks/recent-fill cuts; the recall+learn overhead component (~186 s / $1.80 of the total) has not been re-measured since (2026-06-25) | — | 『docs/design/2026-06-25-warm-vs-cold-clean-measurement.md deleted 2026-07; git log』 |
| <a id="harder-regime-op-cost-unmeasurable"></a>**Memory nets cheaper/faster on a HARDER multi-round build regime (the c1-c2 re-test)** | unmeasured | Re-scoped #642 pilot — a gotcha-concentrated self-seeding warm-vs-cold hard-build eval (fictional apps wardex/glyphex/relayex, ~8 un-guessable house conventions); three findings. **(1) Contamination bug found + FIXED.** The warm arm left `ENGRAM_CHUNKS_DIR` unset, so recall read the operator's REAL global chunk index (`~/.local/share/engram/chunks`) — which contained the design session = the answer key; warm app1 scored round-1 house 8/8 from an EMPTY vault. Fixed by per-cell chunk isolation (commit `d943ea93`); post-fix warm app1 drops to round-1 0/8 (verified). This bug would silently poison ANY future memory eval. **(2) Capability ceiling reproduced (vault note 99).** Post-fix, memory-less builds (cold AND isolated warm-app1) plateau at ~3–4/8 on the arbitrary conventions — sonnet cannot derive idiosyncratic content without the notes; with the answer key it reaches 8/8. **(3) No Goldilocks window (the structural finding).** A self-seeding build eval cannot yield a clean op-cost verdict: easy enough that the seeder converges → cold converges too → memory's fixed tax dominates (that IS the existing easy-CRUD null, `c1-c2-warm-op-negatives`); hard enough that cold accrues rework → the seeder can't converge either → `/learn` (which the harness gates on convergence, `harness.py:826`) captures nothing → nothing transfers. Partly a learn-MODELING artifact: real `/learn` captures per-correction lessons mid-build, which the harness's fire-once-on-convergence model discards. **Verdict:** op-cost (C1/C2) is unmeasurable via a self-seeding build eval; op-value measurement should redirect to real long-session work (vault note 98). The capability win on idiosyncratic content stands (note 99). Attempt cost ~$45 (2026-07-17) | — | branch `feat/642-hard-eval-wiring` (harness + fix); session 2026-07-17 (pilot results + contamination proof; /tmp result JSONs ephemeral) |
| <a id="failure-mining-mid-task-gap"></a>**Current recall reaches the moments where corrections cluster** | proven | 77% of mined failures occur at mid-task decision moments current recall never reached (top: before-declaring-done ~26%); **PRE-TREATMENT baseline** — mined before the 2026-06-29 CLAUDE.md decision-moments guidance fix this figure motivated; not a live/current gap (2026-06-28) | — | 『docs/design/2026-06-28-failure-eval-material.md deleted 2026-07; git log』 |
| <a id="recall-moments-headless-flip"></a>**Decision-moment guidance flips recall firing (initial headless measurement)** | superseded | clean RED 0/5 → GREEN 4/5 (headless `claude -p`, fresh process, fictional domains — the in-session subagent method was invalid, control inherited the treatment); bound: failures split ~56% application-class (lesson present, unapplied) and, on a separate cut, ~60% behavioral (needs a rich-context harness) (2026-06-29) | [recall-moments-opus48-remeasure](#recall-moments-opus48-remeasure) | 『docs/design/2026-06-29-recall-moments-revalidation-data/results.md deleted 2026-07; git log』 |
| <a id="recall-moments-opus48-remeasure"></a>**Decision-moment guidance flips recall firing (real production model)** | proven | re-measured on opus-4.8[1m] — the model that actually runs this guidance (the 0/5→4/5 above was a different model/run): cue-firing 2–3/5, ≥ the un-guided 2/5, not regressed, all cue-fires use `/recall glance`; after-failure cue never fired (0/2 on CF3/CF4 — judged correct infra-discrimination, not a miss); the old cost-filter's value-gate does NOT hold on opus (fires 3–5/5 on routine work regardless of wording — tracked as #665) (2026-06-30) | — | 『docs/ROADMAP.md pre-Task-4 history (Track A, #663 update); git log』 |
| <a id="recall-overfire-hook-rejected"></a>**A mechanical "recall before tool calls" hook is a viable trigger** | refuted | over-fires ~147×–380× depending on the pinned fire-unit (task-init vs turn), fatal at ~190 s/fire; rejected in favor of CLAUDE.md guidance (agent-judged, harness-agnostic) (2026-06-27) | — | 『docs/design/2026-06-27-recall-trigger-patterns-and-proposals.md deleted 2026-07; git log』 |
| <a id="recall-trigger-capture-ceiling"></a>**Firing-side triggers can reach every correction** | proven | ~28% of corrections are a write-side/capture ceiling no read-side trigger reaches (2026-06-27) | — | 『docs/design/2026-06-27-recall-trigger-patterns-and-proposals.md deleted 2026-07; git log』 |
| <a id="qanchor-park"></a>**Question-anchored crystallization beats topic-anchored** | refuted | Application: topic 62% vs question 52% (Δ−10pp, inside the ±22pp 2σ floor → no detectable benefit); Retrieval: topic-anchoring wins 10/10 (mean cosine 0.52 vs 0.35) — a clear loss for question-anchoring. Root cause: the concrete idiosyncratic token is load-bearing on both channels (2026-07-01) | — | 『docs/design/2026-07-01-question-anchored-distillation.md deleted 2026-07; git log』 |
| <a id="qanchor-pattern-type-sublever"></a>**Question-anchoring helps transferable-pattern lessons specifically** | unmeasured | hint only, n=3 pattern pairs, not independently validated: pattern-type lessons question-anchor 83% vs topic 58%; concrete-API lessons topic 64% vs question 39% (2026-07-01) | — | 『docs/design/2026-07-01-question-anchored-distillation.md deleted 2026-07; git log』 |
| <a id="vocab-tag-nomination-l6xtag"></a>**Controlled-vocab tag nomination (L6×TAG) recovers verified retrieval misses** | proven | 54% retrieval recovery of verified misses; delivery +17.3pp overall / +50pp on cross-domain bridges, both above 2σ, zero collateral regressions (2026-07-02) | — | 『docs/design/2026-07-02-link-value-exploration.md deleted 2026-07; git log』 |
| <a id="ppr-killed"></a>**Spreading-activation / personalized PageRank improves retrieval on this vault** | refuted | drops non-activated baseline notes; 32–36 collateral regressions (2026-07-02) | — | 『docs/design/2026-07-02-link-value-exploration.md deleted 2026-07; git log』 |
| <a id="supersession-edges-mechanism"></a>**Supersession edges (L5×T5) function as a retrieval mechanism** | proven | proven as mechanism; delivery effect size underpowered/inconclusive at current n — not itself a delivery win yet (2026-07-02) | — | 『docs/design/2026-07-02-link-value-exploration.md deleted 2026-07; git log』 |
| <a id="vocab-refit-cost"></a>**Autonomous vocab refit lifecycle is cheap** | proven | measured per-refit cost ≈$0.09 (validation run) (2026-07-03) | — | 『docs/design/2026-07-03-vocab-notes-build-results.md deleted 2026-07; git log』 |
| <a id="write-memory-atom-dereference-invalid"></a>**The reference-card atom variant was dereferenced by deployed arms** | unmeasured | deployed-arm "0/27 dereference" measurement later found **instrument-invalid**: global skill-shadowing meant no arm ever loaded the atom text under test — not a valid measurement of the design (superseded by the corrected boundary redraw shipped as `write-memory`, a worker at the write seams, not a re-measurement of this same claim) (2026-07-04) | — | 『dev/eval/atoms-build/results-2026-07-04.md deleted 2026-07; git log』 |
| <a id="tier-routing-parity"></a>**Sonnet+memory matches opus+memory (memory democratizes reasoning across model tiers)** | proven | sonnet+memory fully matched opus+memory: C3 (15/15), C4i (3/3), C6 (6/6); sonnet *cold* failed; RED baseline: router over-provisioned 4/6 memory-backed units to mid before the tier-discount rule; C5 axis flaked (re-run owed) (2026-06-28) | — | 『docs/design/2026-06-28-question-shaped-crystallization-proposals.md deleted 2026-07; git log』 |
| <a id="payload-prune-smoke"></a>**Dropping the raw query payload after Step-3 synthesis cuts build cost with zero capability loss (isolation premise)** | proven | synthesis-injection proxy, n=3 apps, opus: carrying only the synthesis cut build_cost ~40% (~$1.6/app; feeds −45%, links −23%, notes −51%); zero capability loss — identical rounds (2/2/2), success (3/3), final convergence + arch 10/10 on every app. Honest bound: n=1/app, no same-arm noise floor measured — large-consistent-mechanism, not noise-floor-proven (2026-06-30) | — | 『docs/superpowers/specs/2026-06-30-payload-prune-mechanism-design.md deleted 2026-07; git log』 |
| <a id="payload-cut-lazy-chunks"></a>**`--lazy-chunks` cuts recall payload size** | proven | payload −33.7% (146→97 KB), trap gate GREEN; validated 0 chunk fetches across 13 realistic uninstructed recalls + 2/2 sole-source capability (no evidence drop) (2026-06-27) | — | 『docs/superpowers/plans/2026-06-27-recall-lazy-chunk-content.md deleted 2026-07; git log』 |
| <a id="payload-cut-recent-fill"></a>**`--recent-fill` (200→25) cuts recall payload size** | proven | payload −28% (230→165 KB), trap gate GREEN, `targ check-full` clean; cumulative with lazy-chunks ~−58% (230→97 KB) (2026-06-27) | — | 『docs/superpowers/plans/2026-06-27-recall-payload-recent-fill.md deleted 2026-07; git log』 |
| <a id="haiku-whole-op-dead-end"></a>**Running the whole recall+build op on haiku holds quality** | refuted | whole-op haiku probe, n=5: FAILED the pre-registered verdict rule — C3 build-applied 18/25 (wanted ≥23/25), C6 reasoning 3/10 (wanted ≥4/5); failure diagnostically located in the BUILD half — haiku's recall *curation* held (C3 surfaced 25/25, C5 5/5, 0 C4 mis-rankings) (2026-06-24) | — | 『docs/design/2026-06-24-engram-cost-round2-build-loop.md deleted 2026-07; git log』 |
| <a id="haiku-split-dead-end"></a>**Splitting recall (haiku) from build (opus) is worth the complexity** | refuted | built + measured via TDD: quality HELD (C4 5/5, C5 5/5 = whole-opus) but op-cost cut only −14% (recall is a cost sub-share, so even ~5× cheaper recall barely moves the op); rolled back — not worth two skills + dispatch + contract + crystallization-quality risk (2026-06-24) | — | 『docs/design/2026-06-24-engram-cost-round2-build-loop.md deleted 2026-07; git log』 |
| <a id="glance-delivers-c3-c4i-c6"></a>**`/recall glance` delivers the C3/C4i/C6 capability wins at the verified bars** | proven | glance DELIVERS C3/C4i/C6 at the verified bars; smoke trap gate GREEN across C3/C4i/C5/C6 as a non-regression check (the C5 leg confirms no regression against C5's known non-delivering baseline — glance does NOT deliver C5, see the next row) (2026-06-29, #661/#662) | — | 『docs/design/2026-06-29-glance-delivery-661.md deleted 2026-07; git log』 |
| <a id="glance-fails-c5-delivery"></a>**`/recall glance` delivers the C5 recency-channel-standard win** | refuted | glance applies the C5 standard 0/5 vs deep's 4/5 — surfaces the recency item but doesn't apply it (retrieval ≠ delivery); mitigation: C5-type cues now escalate to `deep` (2026-06-29) | — | 『docs/design/2026-06-29-glance-delivery-661.md deleted 2026-07; git log』 |
| <a id="glance-cost-tinyvault"></a>**`/recall glance` per-fire cost/speed win (tiny-vault measurement)** | superseded | 1.2× faster per fire (tiny-vault) (#661, 2026-06-29) | [glance-cost-realvault](#glance-cost-realvault) | 『docs/design/2026-06-29-realvault-glance-cost-662.md deleted 2026-07; git log』 |
| <a id="glance-cost-realvault"></a>**`/recall glance` per-fire cost/speed win (real-scale vault)** | proven | 2.23× faster / 46% cheaper per fire on a real-scale vault; the tiny-vault 1.2× figure above was a misleading artifact of vault size (2026-06-29) | — | 『docs/design/2026-06-29-realvault-glance-cost-662.md deleted 2026-07; git log』 |
| <a id="qa-arm-v-borderline"></a>**The dedicated Q-space channel premise (round-3 Q&A memory) is licensed to build** | unmeasured | Arm V large-n premise check: 63% (19/30), BORDERLINE — below the pre-registered PASS ≥80% bar; round-3 remains unlicensed pending further P2′/P3′ validation over ≥20 real captured pairs (2026-07-03) | — | 『docs/design/2026-07-03-qa-memory-proposals.md deleted 2026-07; git log』 |
| <a id="write-memory-worker-fire-rates"></a>**The write-memory worker fires at its handoff seams and respects the boundary** | proven | fixed, marker-gated harness (haiku and sonnet): handoff fired W1 3/3, W2 3/3, W3 2/3; boundary violations 0; spurious fires 0/6 (2026-07-04) | — | 『dev/eval/atoms-build/worker/results-2026-07-04.md deleted 2026-07; git log』 |
| <a id="delegate-guidance-flip"></a>**delegate.md (v4) flips the delegation reflex (solo → plan/route/dispatch)** | proven | Controller-verified on a clean harness (n=3/arm): trivial 2-file rename RED 3/3 solo (reads+edits directly) → GREEN 3/3 subagent-dispatch (*"I'll dispatch a subagent rather than editing directly"*); greenfield build RED 0/3 dispatch (builds/brainstorms solo) → GREEN orchestration-leaning (recall-first + stated dispatch intent, noisier). The rename is the hard case: two earlier doc versions flipped it **0/3** — v4's *"no just-do-it tier licenses the skip"* override is what cracked it. Independently corroborated by the implementer's n=5/arm run (RED 0/10 → GREEN 8/10). Rubric = first substantive act is plan-before-code / `/route`|`/please` / subagent dispatch, else solo. Bound: headless `claude -p` auto-loads `~/.claude/CLAUDE.md` (its "Quick Fix — just do it" tier competes) into BOTH arms held constant, so the differential is clean and production-faithful; residual solos cited that competing tier (2026-07-07) | — | dev/eval scratch task1-eval/{out2,out4b}; git log |
| <a id="c7-lever-recheck-red-baseline"></a>**The C7 lever-recheck (anti-amnesia) miss — re-proposing a killed lever mid-synthesis — reproduces as RED under a two-turn harness** | proven | 4/5 fixtures RED (fixture1/2/3/4: 3/3 valid arm-A trials AMNESIA each; fixture5 honest NOT-RED: 2/3 AMNESIA + 1/3 vacuous-RECONCILED — an uncovered non-memory alternative, not a defect), n=3 valid arm-A trials/fixture, opus, 27/27 official trials valid (treatment-delivery gate). **Mechanism (load-bearing):** re-query rate 0/15 turn-1 AND 0/15 turn-2 across every official arm-A trial — the shipped #655-criterion-3 reconcile-rule never fires a vault re-query in this two-turn structure, scoping #655's remaining work to criterion 1 (the re-entry query). Judge-variance flip-rate 0.0 (5 rows × 3 re-votes); paraphrase hard gate 4/4 stable. Known caveat: deterministic guard has a negation-blindness false-AMNESIA (arm C fixture1 t0) — biases toward RED (safe for this baseline; watch-item for #655's GREEN judging). Secondary, no bar: fable shows the same blindness signature (fixture2 arm A 2/2 AMNESIA). Spend ~$24 total (2026-07-08) | — | `dev/eval/cumulative/lever_recheck/RESULTS.md` (machine-readable: `dev/eval/cumulative/lever_recheck/analysis.json`) |
| <a id="c7-reentry-query-green"></a>**Recall's Step 3.5 re-entry query closes the C7 lever-recheck miss on the fired path** | proven | Iteration arc (opus, honest post-stub-fix instrument, 15 valid arm-A trials/batch): baseline (pre-#655) fire 0/15 (RED, see `c7-lever-recheck-red-baseline`) → v2 (forced output-contract wording) fire 10/15, honored-when-fired 10/10, 14/15 RECONCILED, bar NOT MET (fixture2 t0 the sole non-fired AMNESIA) → **v3 (Re-entry line(s) coupled to the RECOMMENDATION line's adjacency) fire-rate 14/15 (93%), honored-when-fired 14/14, 14/15 RECONCILED**, bar still NOT MET (fixture4 t1 the sole non-fired AMNESIA — fixtures 1/2/3, the rest of the bar's 4-fixture population, each hit 1.0; fixture5, informational-only and never counted toward the bar, also hit 1.0). Pre-registered strict bar (fixtures 1–4 each all-RECONCILED) is explicitly **UNMET** — this is a fired-path efficacy result, not full-coverage. Asymptote analysis: at 93% measured per-trial adherence, P(12/12 fires in a row) ≈ 0.93^12 ≈ 42% — a prose+structure mechanism cannot reliably clear an every-trial bar; closing the gap needs an out-of-text enforcement layer, filed as **#677** (mechanical enforcement layer for the Step 3.5 Re-entry contract, 93%→100%; closed won't-do 2026-07-10 — 93% accepted as the operating point). Zero-stub live smoke ($0.67; real skill + real `engram` + real 212-note vault copy): full chain PASS — agent conceived the payload-prune lever unprompted, Step 3.5 fired, engram returned the buried note (191) top-ranked, both Re-entry contract lines were written verbatim (`CLOSED(191)→drop` with the reopen condition; a clean-with-referent line for the second recommendation), and the agent recommended the already-shipped tier-routing lever instead of the closed one. **Criterion-2 disposition: SUPERSEDED** — the "negation outranked-by-chunks, no priority" premise (dated 2026-06-24) predates the matched-note floor (2026-06-28, `matched-note-floor`); across 24 fired trials (v2+v3, honest instrument) there are 0 surfaced-but-ignored (note-surfaced ∧ AMNESIA) cases — the earlier apparent live cases were an instrument artifact (the stub returned the buried closure note last/lowest-scored on lever-keyed queries, opposite of the shipped floor's real ranking; fixed via TDD; the RED baseline is provably unaffected — all 15 RED-baseline arm-A trials issued no lever-keyed re-query at all, fired or not). Prerequisite fix: `deterministic_guard` now defers to the LLM judge on negated advocacy instead of auto-AMNESIA (arm-C fixture1 t0's RED fixture); scorer/guard suite at 149+ tests green. Spend: ~$56–58 total cycle — itemized (raw JSONL sums + labeled estimates) in RESULTS.md's Spend table (2026-07-09) | — | `docs/superpowers/plans/2026-07-09-655-reentry-query.md`; `dev/eval/cumulative/lever_recheck/RESULTS.md` (GREEN section); raw: `results_655v2_green_A12.jsonl`/`A345.jsonl`, `results_655v3_green_A12.jsonl`/`A345.jsonl` |
| <a id="recall-time-split"></a>**Recall's wall-time splits into four measurable phases (pre-query, query in-flight, payload consumption, remainder)** | proven | Post-#657-cuts vintage (2026-07-12, n=3 real-vault-copy trials, directional): (a) pre-query median 17.5s [15.1–21.4]; (b) query in-flight (sum of each `engram query` call's own tool_use→tool_result span) median 12.2s [11.5–12.5]; (c) payload consumption (first query result → Step-2.5-start) median 24.6s [24.4–30.0]; (d) remainder (Step-2.5-start → end) median 0.0s [0.0–8.2]; total span median 54.1s [52.2–71.1] — consistent with `recall-time-remeasure`'s 51.9s [39.3–63.6] (same contrast). Census (median): total payload 125.3 KB [107.0–149.8]; duplicated candidate-note content (byte-identical in both `items[]` and a cluster's `candidate_l2s`) 12.3 KB [9.5–24.7], ~10% of the payload; ~58 KB of `items[]` (25.0 KB note content + 33.4 KB meta/markup) renders before the first candidate note in `clusters[]` (32.6 KB candidate content + 33.4 KB meta) under today's items-before-clusters ordering. Phase (c) is the addressable slice a payload restructure can reach — see `payload-restructure-refuted` for the measured result of targeting it (2026-07-12) | — | `dev/eval/traps/recall_time.py` (commit `055a07f5`); `docs/superpowers/plans/2026-07-12-684-payload-restructure.md`; artifact `/Users/joe/.claude/jobs/ac0c61e1/tmp/684-recall-segments.json` (+ durable per-run transcript/payload copies at `684-recall-segments-transcripts/`) — job-dir artifacts are session-scratch and not guaranteed to survive; the figures above are the citable record, and durable per-run transcript copies are retained alongside the artifact for as long as it survives |
| <a id="payload-restructure-refuted"></a>**Clusters-first payload ordering + withheld/lazy matched-note content (Variant B, #684) cuts recall wall-time** | refuted | Phase (c) payload-consumption median: pre-build 24.6s [24.4–30.0] (`recall-time-split`) → round-1 (Variant B, pre-capture-discipline) **35.3s [30.0–49.4]** → round-2 (Variant B + mandatory single-capture query discipline) **58.0s [37.5–62.0]**, the entire round-2 range sitting above the 26s pre-registered keep/revert bar. Round-1 mechanism: 2 of 3 after-trials ran `engram query` unpiped, colliding with Claude Code's ~90 KB persisted-output truncation, forcing the agent to page the payload back out of its own session transcript via grep/sed/Read. Round-2 mechanism (the discipline fix, measured to make it worse, not better): mandatory redirect-to-file + sliced reads converts what was in-context reading into a 5–10-round-trip grep+Read fetch loop over the redirected capture file (~5–9s/round-trip) — no `engram show` fetch ever fired in either round (nothing was missing; `candidate_l2s` already carried the relevant content), so the regression is pure round-trip overhead, not fetch cost. Byte cut was real both rounds (payload total −23.3% round-1, −21% round-2 true census) and did not buy back the round-trip latency. Reverted 2026-07-13 (`0ae98779`, diff-empty vs the last pre-build commit `055a07f5`); the pre-build inline items-first payload shape is the measured fast path | — | branch `684-payload-restructure` commits `4f0c3c8d`..`0ae98779`; `docs/superpowers/plans/2026-07-12-684-payload-restructure.md` |
| <a id="variant-a-probe"></a>**Clusters-first payload ordering + dedupe of duplicated candidate-note content (Variant A, #689), all inline, cuts recall wall-time** | refuted | Phase (c) payload-consumption median: baseline (reverted, n=2) 21.75s [13.0–30.5] → Variant A after-measure (n=8/8 gate-PASS) 24.70s [20.9–33.6]; improvement = −2.95s (marginally SLOWER at the median, ranges overlap → no detectable win). Pre-registered class-closing bar (keep iff improvement ≥3.0s AND after_max<baseline_min) FAILED both conditions; robust to the n=2 baseline noise (the after-spread makes after_max<baseline_min structurally impossible, and improvement is negative). Dedup MECHANISM confirmed working: duplicated candidate-note content 10.1KB→0 across all 8 trials, total payload 114.6→103.1KB (~10% lighter) — yet consumption did not improve, corroborating that recall consumption is round-trip/reading-dominated, not byte-dominated (see [recall-time-split](#recall-time-split) phase (c) and [payload-restructure-refuted](#payload-restructure-refuted)). This was the payload-shape lever class's LAST pre-registered probe → the class is now CLOSED. The note-107 deferred-content AC did not apply (nothing was deferred under A); the comprehension regression check (candidate content read from candidate_l2s, zero spurious `engram show`, 3/3) substituted for it and passed. Reverted `701ec870` (diff-empty vs pre-build `a81e5150`) (2026-07-13) | — | branch `689-variant-a` commits `91f86174`..`701ec870`; `docs/superpowers/plans/2026-07-13-689-variant-a.md`; after-measure artifact `/Users/joe/.claude/jobs/ac0c61e1/tmp/689-after.json` |
| <a id="prequery-composition"></a>**Pre-query recall phase (~18.6s) is ~73% irreducible model reasoning; no slice offers a cheap wall-time cut (#690)** | proven | Refined inner-split of the [recall-time-split](#recall-time-split) pre-query phase, measured by re-analyzing the 8 gate-PASS #689-after transcripts (**n=8**, zero new API spend; all boundaries are tool_use timestamps). Note the population differs from recall-time-split (n=3): this n=8 pre-query total (**18.6s [17.4–19.9]**) is a refined estimate that updates — and is consistent with — recall-time-split's n=3 directional 17.5s [15.1–21.4] (each within the other's range). Composition (median s, n=8/8 split-gate-PASS): **ttft_invoke 3.1 [2.6–4.9]** (model TTFT + skill-invoke, not ours) · **skill_read+Step-0 7.15 [2.6–9.6]** (reading the skill body + generating the Step-0 judgement — nucleus-adjacent) · **sweep 0.7 [0.4–1.9]** (`engram ingest --auto`; the only cleanly-mechanical slice) · **compose 6.5 [4.8–11.6]** (writing the 10 phrases) · total **18.6 [17.4–19.9]**. The two dominant slices (skill_read+Step-0 + compose ≈ 13.6s, ~73%) are irreducible model reading/generation; the one clean mechanical slice (sweep) is 0.7s median — far below the pre-registered 3.0s keep bar. Gate A on the disposed template-assist cut then found the recall skill's Step 1 (`SKILL.md:81-92`, the 10 angles) + Step 2 (`:99-106`, the `--phrase` skeleton) is ALREADY a template, so the compose lever's delta is near-empty (expand+relabel, not free-form→template). Joe disposed **CLOSE measured-no-cut** (2026-07-13) over ~$6-9 of experiment spend. **No recall behavior change** (Task 3 not built). Corroborates the round-trip/reading-dominated model of recall cost ([variant-a-probe](#variant-a-probe), [payload-restructure-refuted](#payload-restructure-refuted)): pre-query, like consumption, is model-reasoning-bound, not mechanically-cuttable. Split-analyzer KEPT as reusable eval infra (`compute_pre_query_split`, #684 precedent) | [recall-time-split](#recall-time-split) | `dev/eval/traps/recall_time.py` (commits `36970dad`/`c4b60019`; test `test_recall_time_prequery.py`, fixture `testdata/prequery_trial0.jsonl`); `docs/superpowers/plans/2026-07-13-690-pre-query-split.md`; composition artifact `/Users/joe/.claude/jobs/ac0c61e1/tmp/690-prequery-composition.json` |
| <a id="query-inflight-split"></a>**Recall's query in-flight time is dominated by the chunk-index I/O load (scan), NOT the embedder — the chunk index is a real mechanical lever (#691)** | proven | Refined binary-internal split of the [recall-time-split](#recall-time-split) query in-flight phase, measured with a new `engram query --timings` instrument (`internal/cli/query.go` `phaseTimer`, DI injected clock, `--timings`-gated so the default recall payload is byte-identical). Vintage: 2026-07-13, n=3, real-vault **copy** (255 notes + sidecars) + real chunk index read-only (**41,367 `.jsonl` / 364 MB**), `--lazy-chunks`, 10-phrase load, directional (`/usr/bin/time` wall). Per-stage median [range] ms: **scan 4787 [4748–4965]** (vault scan + sidecar load + 41k-chunk-index I/O load) · **embed 2009 [2001–2050]** (MiniLM forward + cosine match) · cluster 212 [211–217] · nominate 60 [60–61] · render 0 (lazy) · stage-sum 7068 [7020–7310]; binary wall median **7.10s [7.05–8.45]** (residual wall−sum ~32 ms warm; run1 cold-start 1.14s — binary page-in + cold file cache). **scan (chunk-index I/O load) dominates at ~67% of wall.** Attribution proxy (empty chunk index): the original coarse `embed` bucket fell **6801→803 ms**, confirming ~6s was chunk-index-scale work (I/O load, now in `scan`, + 41k-vector brute-force match, in `embed`) — NOT the ~0.8s MiniLM forward. Binary wall 7.1s < the in-session 12.2s tool-span (`recall-time-split`): the ~5s gap is Claude-Code/Bash harness overhead **outside** the binary (spawn/stdio/output-capture); the five stages decompose the 7.1s in-binary time (warm residual ~30 ms confirms completeness). **Pre-registered follow-up bar** (dominant ≥3.0s median AND mechanically-cuttable): scan 4.79s clears BOTH — 41k-file/364 MB index → consolidate/cache/mmap; embed's ~1.2s 41k-vector match → ANN index. ⇒ pre-registered-bar verdict: a follow-up optimization issue is **WARRANTED** (dominant ≥3.0s median AND mechanically-cuttable). Unlike pre-query (`prequery-composition`) and payload (`variant-a-probe`) — both model/round-trip-bound with no cheap cut — this phase has a real mechanical lever, so the ROADMAP **Track B (retrieval cost)** capstone framing (#691 as a measurement-only closing item) no longer holds. Joe disposed **CLOSE measured-lever-found** (2026-07-13): #691 closed, Track B reopened, follow-up #693 filed. Measurement only; no optimization shipped (#691) | [recall-time-split](#recall-time-split) · [prequery-composition](#prequery-composition) | `engram query --timings` (`internal/cli/query.go` `phaseTimer`; staged, not yet committed); `docs/superpowers/plans/2026-07-13-691-query-inflight-timings.md`; artifacts `/Users/joe/.claude/jobs/ac0c61e1/tmp/691-run{1,2,3}.{yaml,time}` + `691-measurement-summary.md` — job-dir artifacts are session-scratch; the figures above are the citable record |
| <a id="chunk-scan-vector-read-attribution"></a>**Recall's chunk-index scan cost is the non-empty vector-file read+decode, NOT the 85%-empty files — the empty-skip lever fails; the real lever is the 6,355-file / 364 MB vector read (#693)** | proven | Within-`scan` attribution refining [query-inflight-split](#query-inflight-split), via the zero-out proxy. Vintage: 2026-07-14, n=3, real-vault **copy** (257 notes) + real chunk index read-only (**41,634 `.jsonl`; 35,279 empty (84.7%) / 6,355 non-empty / ~364 MB**), `engram query --timings` (#691), 3-phrase, warm (run-1 cold discarded), directional. Median `scan_ms` across three conditions: **(a) all 41,634 files = 4832** · **(b) empties removed (6,355) = 4037** · **(c) 0 chunk files (vault+sidecar only) = 39**. ⇒ within-`scan` split: **read+decode the 6,355 non-empty vector files (b−c) ≈ 3998 ms (~83%)** · open the 35,279 empty files (a−b) ≈ 795 ms (~16.5%) · vault scan + 257 sidecars (c) ≈ 39 ms (~0.8%). **Pre-registered empty-skip bar (≥40% AND ≥1.9 s scan cut) NOT cleared** — empty-skip = 16.5% / 0.8 s. Hypothesis "85% empty files → 85% of the cost" **REFUTED**: empty-file opens are ~22 µs each; the cost is reading + JSON-decoding ~2.4M floats across 6,355 files (~630 µs/file). Real lever = the vector read (consolidate files / binary-encode vectors / cache parsed vectors) — the issue's original candidate (a); the 85%-empty files are a *disk-hygiene* problem (ingest writes 0-byte files via `rebuildIndex`), not the time lever. Distinguish three easily-confused quantities: within-`scan` **vector-read ~83%** (of the ~4.8 s scan stage), **scan itself ~67%** (of the ~7.1 s binary wall), and this NEW **empty-file *count* ~85%** of files (disk hygiene, NOT a time lever). Joe disposed **RE-SCOPE #693 to the vector read, no build this cycle** (2026-07-14); empty-file hygiene (ingest-guard + prune) filed as #694. Measurement only; no optimization shipped (#693) | [query-inflight-split](#query-inflight-split) | `/Users/joe/.claude/jobs/ac0c61e1/tmp/693-measurement-summary.md` + `693-datacopy` scratch; conditions reproduced this session — the figures above are the citable record |
| <a id="chunk-scan-decode-cache-deferred"></a>**The chunk-scan cost is the JSON float-decode (~95% of scan), NOT per-file opens (~3%); a binary parsed-record cache ceiling ~87% of scan but DEFERRED at ~7% of the whole op (#693)** | proven | Finer within-`scan` attribution refining [chunk-scan-vector-read-attribution](#chunk-scan-vector-read-attribution). Vintage: 2026-07-14, n=3, real-vault **copy** (257 notes) + the 6,355 non-empty chunk files (59,562 records / 346 MB JSON), `engram query --timings` (#691), 3-phrase, warm, `ENGRAM_VAULT_PATH`+`ENGRAM_CHUNKS_DIR` override (scan matched the 4037 ms baseline → verified), directional. Median `scan_ms`: **(a) 6,355 files = 4035** · **(b) consolidated to 1 file = 3912** · warm byte-read of the 346 MB file = **~20 ms**. ⇒ within-`scan` split (sums to (a) 4035 ms): **JSON decode (~22.9M float32 + text) = (b)−byteread−vault ≈ 3.85 s (~95%)** · per-file opens (a−b) ≈ 123 ms (~3%) · byte-read ≈ 20 ms (~0.5%) · vault+sidecar ≈ 39 ms (~1%). **Consolidation REFUTED as the lever** (opens ~3%). The lever is the JSON decode → a binary parsed-record cache (91 MB raw float32 vs 346 MB JSON, no parse) has a **conservative measured-bounded ceiling ~87% scan cut (~3.5 s)**: removing the ~95% decode leaves the cache's own ~91 MB blob read (~30 ms) + `Record` construction — that residual is the gap below the ~95% theoretical decode share (the raw-JSON byte-read is ~20 ms, so the decode CPU is the whole prize). Honest op-level worth ([recall-time-remeasure](#recall-time-remeasure)): ~3.5 s off the ~5.6–7.1 s query binary (a CLI win + caps unbounded decode growth) but only **~7% of the ~52 s whole recall op**. A plan (additive/regenerable binary cache, `.jsonl` canonical, ranking-neutral, DI, pure-Go) passed a 4-angle Gate A. **Joe disposed DEFER at the Ratification Gate (2026-07-14):** ~7% op-worth does not justify a new on-disk cache subsystem now — revisit when the decode grows to dominate a larger share of the op. Measurement only; no code shipped (#693 parked deferred) | [chunk-scan-vector-read-attribution](#chunk-scan-vector-read-attribution) · [recall-time-remeasure](#recall-time-remeasure) | `/Users/joe/.claude/jobs/ac0c61e1/tmp/693b-attribution.md` + `693-chunks-{6355,consolidated}` scratch; conditions reproduced this session |
| <a id="b-artifacts-angle-headroom"></a>**Adding an artifacts/file-keyed angle to recall's 10 query angles surfaces actionable memory the topic angles miss** | refuted | Free warm-vs-warm retrieval probe (zero API; `engram query` non-lazy, note-basename set-diff, 6 fixtures spanning file kinds: 2 Go source, 1 eval-Python, 1 skill, 1 build-tooling, 1 route/doc). For each fixture the current-10 topic angles (NOT naming the file) vs the same 10 + one file-keyed phrase. Unique NOTE adds: query.go 2 (notes 39, 79), internal/embed 1 (233), recall_time.py 2 (229, 85), SKILL.md 0, targs.go 0, route 0 → **3/6 fixtures gained ANY unique note; ≤2/6 gained an ACTIONABLE one** (of the 5 unique-add notes total, 2 — 233/229 — are vocab-definition notes any query tag-nominates). Pre-registered bar (≥4/6 fixtures with ≥1 actionable unique-add) **FAILS** either way. Mechanism: crystallized lessons are keyed by situation/principle, not by filename, so a file-keyed angle re-surfaces what the topic angles already found — it does not reach actionable memory the 10 miss. Corroborates note 73 (engram's value is the memory; a retrieval-path addition usually shows no headroom). **B PARKED** — no recall-skill change; the win-nucleus 10 angles are unchanged. Reopen only if a real recall miss shows a file-keyed lever would have surfaced a note the topic angles missed | — | probe `/Users/joe/.claude/jobs/ac0c61e1/tmp/b-headroom-probe.{py,json}`; `docs/superpowers/plans/2026-07-13-frameworks-briefing-and-probe.md` |
| <a id="chunk-empty-file-prune"></a>**Ingest-guard + `prune --empty` stop the unbounded 0-byte chunk-index accretion; ~21% scan-stage recovery copy-verified, ranking-neutral (#694)** | proven | Vintage 2026-07-14, real-vault (read-only) + a full **copy** of the real chunk index, temp binary (`go install ./cmd/engram`), `engram query --timings` (#691), n=3 warm (run-1 cold discarded), `--chunks-dir`/`ENGRAM_CHUNKS_DIR` override, directional. Chunk-index copy BEFORE prune: **54,618 total `.jsonl`, 47,990 empty (87.9%), 6,628 non-empty**. The empty count has GROWN since [chunk-scan-vector-read-attribution](#chunk-scan-vector-read-attribution) measured it earlier the same day (35,279 empty / 41,634 total): **+12,711 empties within the day** — the defect accretes unboundedly on every ingest; this unbounded growth, not the scan-time recovery, is the guard's core value. `prune --empty --dry-run` reported "removed 47990 empty chunk-index file(s) of 54618 scanned", deleted nothing; the real prune (on the copy) left exactly **6,628 files, 0 empty**. Median `scan_ms` (n=3 warm): **5149 (before) → 4061 (after), Δ −1088 ms (−21% of the scan stage)**. Ranking-neutral confirmed: 69/69 items identical, top-5 paths+scores identical (float noise only). Honest op-level magnitude: scan is ~67% of the ~7.1 s query binary; −1088 ms off the ~52 s whole recall op ≈ **~2% of the op** ([recall-time-remeasure](#recall-time-remeasure)) — this is a DEFECT-FIX + disk-hygiene + unbounded-growth cap (note 269), NOT a recall speedup. Shipped: the `rebuildIndex` 0-byte-write guard (`internal/cli/ingest.go`) + `engram prune --empty` (+ `--dry-run`) mode (`internal/cli/prune.go`), DI'd (`ListIndexes`/`Remove` seams), re-reads each file live at delete time (never a frozen enumeration). **The real `~/.local/share/engram/chunks` was pruned 2026-07-14 (Joe-authorized)** — `prune --empty` removed 47,990 empties (54,618→6,628, 0 remaining), the dry-run count matching an independent `find -empty` exactly; recall smoke-checked working post-prune; the guard prevents regrowth | [chunk-scan-vector-read-attribution](#chunk-scan-vector-read-attribution) · [recall-time-remeasure](#recall-time-remeasure) | `/Users/joe/.claude/jobs/ac0c61e1/tmp/694-before-runs.txt` + `694-after-runs.txt` (`engram query --timings`, 4 runs each, run-1 cold discarded); `694-chunks-copy` scratch (post-prune verified: 6,628 files / 0 empty, reproduced this session); `docs/superpowers/plans/2026-07-14-694-empty-chunk-file-hygiene.md`; code committed `internal/cli/ingest.go` + `internal/cli/prune.go` + tests — job-dir artifacts are session-scratch and not guaranteed to survive; the figures above are the citable record |
| <a id="685-task-kind-cue-headroom"></a>**#685 change #2 (recall.md task-KIND glance cue) surfaces actionable memory the topic angles miss** | proven | Same free warm-vs-warm retrieval probe technique as `b-artifacts-angle-headroom` (vault note 264; zero API; `engram query` non-lazy, note-basename set-diff), applied to #685's task-KIND cue ("glance keyed to task-KIND — doc-scrub / migration / measurement / skill-edit / command-set — not only the ask's topic"): 6 fixtures spanning those 5 kinds (F5 a doc-scrub/diagram variant), topic-only phrases (N) vs the same N + one task-kind phrase (N+1). Unique-add NOTES counted per fixture and classified against the brief's actionable doc-hygiene/process definition: F1 doc-scrub 5 adds (3 direct-hit: 186, 245, 64), F2 migration 5 adds (2 direct-hit: 241, 244), F3 measurement 5 adds (1 direct-hit: 268), F4 skill-edit 4 adds (1 direct-hit: 26), F5 doc-scrub/diagram 5 adds (2 direct-hit: 62, 64), F6 command-set 5 adds (3 direct-hit: 186, 245, 64) → **6/6 fixtures gained ≥1 actionable unique-add note**. Pre-registered bar (≥4/6 fixtures with ≥1 actionable unique-add) **PASSES — EARNS KEEP**. Direct contrast to `b-artifacts-angle-headroom` (the file-keyed retrieval angle), refuted at ≤2/6 on this SAME probe technique: task-KIND is a *situation*-shaped key, materially distinct from the refuted filename key. Caveat: this measures the cue's retrieval CEILING (best-case phrasing), not whether the cue fires at plan-time — see the #685 issue-comment reconciliation this backs (2026-07-14) | — | probe `/Users/joe/.claude/jobs/ac0c61e1/tmp/685-changetwo-probe-{fixtures,results}.md`; vault note 264 |
| <a id="endorse-cue-red-baseline-null"></a>**Endorse/rank/recommend recall-cue RED baseline (proposed 4th `guidance/recall.md` cue; #685-adjacent)** | refuted | RED baseline fired **15/15** (current `guidance/recall.md`, no endorse cue, opus, n=5 × 3 fictional fixtures; `marker_seen` 15/15, 0 discarded). Premise **falsified / NULL**: current guidance already generalizes to endorse/rank/recommend in a clean probe, so there is no wording gap — no GREEN run, no `recall.md` edit. Interpretation: the real failure (recall not firing under multi-turn load) is context-emergent (notes 82, 138) and a clean single-shot probe over-fires and cannot reproduce it; redirected to the mechanical-enforcement layer (#677). Per note 70, the fixture was NOT engineered to force a leak (2026-07-15) | — | `dev/eval/cumulative/endorse_cue/red.jsonl` |
| <a id="underload-repro-honest-baseline"></a>**Under multi-turn LOAD, recall firing is the clean difference-maker between blind-endorsing and reconciling a vault-refuted lever at an un-forced endorse moment** | proven | Honest baseline under the tightened unforced scorer (`rescore_v2.py`, `unforced=True`): recall did NOT fire → **AMNESIA (blind-endorse) 5/5**; recall fired → **RECONCILED 4/4**; n=9 valid (`marker_seen`) trials, opus, 3 fixtures (`beacon_relay`, `driftwood_index`, `loom_scheduler`). The prior scorer (v2) under-counted blind-endorse at 3/5 via two now-fixed failure modes: a literal `NOT proposed → RECONCILED` verdict loophole (beacon t2) and a judge mis-rating that scored thematic caution as acknowledgment (driftwood t2). New anchor — supersedes/annotates nothing prior. Repro is VALID: this is the RED baseline for testing any fix to recall firing under load; no fix was applied this cycle (2026-07-15) | — | `dev/eval/cumulative/underload_repro/results/red_baseline_v3.jsonl`; `dev/eval/cumulative/underload_repro/rescore_v2.py` |
| <a id="underload-firing-wording-fix"></a>**An ENGAGEMENT-led `guidance/recall.md` cue lifts under-load recall-firing and eliminates blind-endorse — DEPLOYED** | proven (with caveats) | Paired fresh RED/GREEN on the honest-baseline repro, fixture-balanced n=6/fixture, 18 valid/arm, 0 degraded, unforced scorer. **RED** (current wording): firing **9/18 = 50%**, blind-endorse AMNESIA **8/18 = 44%**. **GREEN** (one added first-bullet cue — "*Even just chatting about a proposed approach — before you endorse or rank it — /recall glance first…*"): firing **18/18 = 100%**, AMNESIA **0/18 = 0%**. Δ firing **+50.0 pp, 95% CI [+26.9, +73.1] pp, z=3.46** → CI excludes 0 (pre-registered two-proportion bar PASSES). Cost $26.11 (RED $8.65 + GREEN $17.46; GREEN 2× = firing tax, healthy — not a degraded signature). Adversarially verified (agent `ad183e4`): reconciliations GENUINE (explicit figure-citing, 3/3 unanimous judge), firing = real skill invocation, instrument STABLE (fresh RED 50% ≈ locked baseline 44%; beacon 9/9 zero-fire floor + loom 9/9 fired ceiling across both RED runs; all lift in beacon 0/6→6/6 + driftwood 3/6→6/6). **NARROWS `endorse-cue-red-baseline-null` + notes 145/198/277**: prose can't fix *verb-coverage* under load (277 stands — clean probe already fired 15/15), but a cue targeting the *engagement/conversational-context* gap DOES move under-load firing. **DEPLOYED** to `guidance/recall.md` + `engram update --with-guidance` (2026-07-15). Caveats (→ #695): (1) 5-note vaults → firing≈surfacing; retrieval precision at real scale untested; (2) lift is mostly turn-1 firing on a "review-a-draft" framing echoing the cue — endorse-moment (turn-4) firing barely moved (5/18→6/18); mechanism = early-fire + context-persistence; (3) no placebo control → engagement-framing not isolated from any-leading-cue | — | `dev/eval/cumulative/underload_repro/results/{red_current,green_treatment}_v3.jsonl`; `dev/eval/cumulative/underload_repro/README.md`; #695 |
| <a id="685-doc-enumeration-grep"></a>**Please Step 3's non-waivable doc-surface enumeration grep (Edit A) fixes #685's doc-scrub under-enumeration — but only under a buried-subtask load, not in a clean single-task probe** | proven (with caveats) | Headless `claude -p` probes, sonnet, new harness `dev/eval/cumulative/please_step3_probe/`. **Clean probe** (`--role author`, doc-scrub IS the salient task): un-pressured (n=3) + under explicit "the surface is small, the grep is overkill" pressure (n=3) both **fully enumerated the concept-variant doc surface 6/6** (all 5 touched files named every trial, including the literal-grep-proof variant-only file) — a capable model does NOT need Edit A to enumerate cleanly here, so #685's failure is **salience-under-load, not a capability gap**. **Loaded probe** (`--role loaded_author`, `testdata/fixture_loaded/`: doc-scrub is an incidental consequence of a substantive 3-component feature ask that never mentions docs, enumeration, or grep) is what reproduces the real failure: RED (old Step 3 text) **5/5 UNDER_ENUMERATED** (4/5 docs named every trial, consistently dropping the one off-radar cadence doc `skills/operator/SKILL.md`; no systematic grep run under load) → GREEN (Edit A applied) **5/5 ENUMERATED** (5/5 docs named every trial, including the variant-only doc `docs/runbook.md`) — Δ enumeration +100pp (0/5→5/5), Fisher exact p≈0.008. Edit B (Gate A's docs/diagrams-alignment independent-verification clause) shipped alongside Edit A but was not independently probed this cycle — no measured row for it yet. Bound: one loaded repro fixture, n=5/arm — the miss concentrated on a single off-radar doc, a narrow contrast, not a swept doc-surface-size study. Spend ~$4.5 total. **Staged** in `skills/please/SKILL.md` — not yet committed or deployed (Joe holds `engram update`) (2026-07-16) | — | `dev/eval/cumulative/please_step3_probe/README.md`; `docs/superpowers/plans/2026-07-15-685-doc-enumeration-grep.md`; `dev/eval/cumulative/please_step3_probe/results/{red_baseline,red_pressure,loaded_red,loaded_green}.jsonl` |
| <a id="646-recency-recent-fill-selfcapture"></a>**#646: does the recency RECENT-FILL channel help a real agent recover a SELF-CAPTURED decision after context loss** | unmeasured (structurally untestable via self-capture) | Four clean opus pilots (n=1–2/arm, real engram, real `/recall`, per-trial isolated) on a two-phase harness (phase-1 discovers+narrates an idiosyncratic decision → transcript ingested as chunks → fresh context-lost phase-2 must recover it), recency recent-fill channel ON (default) vs OFF (`ENGRAM_RECENT_FILL=-1`). Every scenario went **VACUOUS**: the self-captured decision the agent NEEDS is topically related to its task, so `/recall`'s broad 10-angle query cosine-matches it (verified `provenances:[direct]`, score 0.48–0.60, **zero `recent` provenance**) and the recent-fill channel (Channel 2, deduped against the matched set) adds nothing — **needed ⟹ queried**. A topically-related decision (milli-dollar unit → revenue report), a cross-tool org-wide convention, and a clean-cwd sweep-isolation fix all failed to break the redundancy. **Scope:** the OFF arm disables only the recent-FILL channel, NOT the re-rank bias (both arms had re-rank ON), so this does NOT bear on recency's continuity value, which is proven elsewhere — the **re-rank bias** (recent-relevant > old-relevant) = C4i 5/5, and recent-fill for **deliberately-unrelated** items = C5 0/5→5/5 ([crowded-vault-capability-robustness](#crowded-vault-capability-robustness)). **Byproduct (eval-isolation gotcha, NOT a production bug):** `/recall`'s in-band `engram ingest --auto` walks the cwd's ancestors collecting every `.claude` dir — intended in normal use (a repo under `$HOME` sweeps the user's `~/.claude` memory) — so an eval whose workspace is nested under `~/.claude` pulls operator-global memory into its isolated per-trial index; the harness handles it by placing claude workspaces under a clean `/tmp` cwd (guarded fail-loud). **Closed 2026-07-19 (Joe):** the recent-fill self-capture proof is structurally untestable (needed⟹queried); continuity value stands on C4i/C5, not a new proof. | see [crowded-vault-capability-robustness](#crowded-vault-capability-robustness) (C4i/C5) | `dev/eval/traps/recency_value/` + vault notes 294, 295 |
| <a id="chunk-index-dedup"></a>**Chunk-index dedup (content hash + record-subset eviction gate) reaches real-scale numbers; a forensic reconstruction resolves the prior existence-only gate's actual loss (ADR-0021)** | proven (with caveats) | Vintage 2026-07-25/26. **Premise check:** of the pre-fix corpus, 124 of 6,612 surviving index files carried chunk records from more than one ingest day — direct evidence the byte-identity-only premise ("same hash now ⇒ interchangeable") is false; a read-only recheck of the CURRENT, post-fix corpus (file reads only, no eviction performed) found 0 of 9 residual duplicate groups holding a record their canonical twin lacks — empirically rare in this corpus, not provably zero. **Real-scale dedup result:** `engram prune --duplicates` on a real ~62,000-source chunk index: 8,572 → 6,612 index files (−1,960, −23%); manifest 62,506 → 60,546 entries; converges on a second run (commit `cb6b9540`; independently re-run this cycle against a scratch copy of the real corpus, reproducing "removed 1960 duplicate(s), retained 6874 canonical(s)" on run 1 and "removed 0 duplicate(s)" on run 2). **Refusal split** (same run): 1,960 removed / 6,874 retained / 47,394 refused structurally (no member of the group was ever indexed — a zero-chunk source yields no index file) / 0 anomalous. **Forensic loss reconstruction** (Finding-1 result; this restates ADR-0021 decision 5's partition identically): of the 1,960 removed entries, classified into an exact partition — **1,568 regenerable** (source present, unchanged, re-ingest reproduces the identical content). **380 with sources no longer present at their original path**: 280 `/tmp` eval-harness fixtures; 52 duplicate Claude Code subagent-workflow transcripts recorded under two different project-slug directories for the same session (verified directly — several such pairs still exist on disk today, byte-identical at matching relative paths under both a main-repo project slug and a git-worktree's own project slug for the same session id); 46 repo files this repo's own earlier refactors had since relocated; and 2 deployed `~/.claude/skills` copies whose content survives under a sibling file (unrelated to the 2 orphans below — these 2 do have surviving twins). **4 with sources whose content has changed** since removal (neither gone nor unchanged). **8 unresolved**: same subagent-workflow-transcript naming pattern as the 52 above, but neither manifest snapshot (pre or live) contains a source path whose slug matches any of these 8 filenames (checked exhaustively, zero slug collisions across all 62,397 pre-manifest entries) — so, unlike the 52, a surviving twin is **not** independently confirmed for these 8; reported as an unresolved residual, not asserted safe. 1,568 + 380 + 4 + 8 = 1,960 exactly. Of those same 1,960, **exactly 2 have no surviving byte-identical twin anywhere in the current index** — a property of two specific entries (one of the 46 relocated repo files, one of the 4 changed-content entries), not an additional bucket and not the same "2" as the deployed-skills sub-bucket above. Both are the same superseded route-skill draft (`sha256:de575213…`), git-recoverable (`git show eb538e05:skills/route/SKILL.md`, verified to reproduce the exact removed bytes). Commit `cb6b9540`'s "0 unsafe removals" claim is **superseded** by this reconstruction — that audit ran under the existence-only gate this cycle replaced, so it could not see the 2 orphan records above (it was right for 1,958 of the 1,960, plus the 8 unresolved not re-checked here). **Honest one-time operational observations — NOT independently reproducible and NOT re-measured after the fix** (no saved script or artifact behind either figure beyond the plan document's own prose): duplicate share of `engram query`'s returned items in a production `--lazy-chunks` sample, 9.6% mean / 28.6% worst; and 50 of 256 shadowed vault-note copies found drifted from their live original (the measurement motivating Rule B). | — | commit `cb6b9540`; `docs/architecture/adr.md` ADR-0021 decision 5 + Measured bound; `docs/superpowers/plans/2026-07-25-chunk-index-dedup-and-prune-fixes.md` (duplicate-share and vault-drift figures — stated there without a saved script); forensic reconstruction re-derived this cycle from a pre-migration manifest snapshot + file listings preserved in an earlier session's scratch (`chunks-clean/`, `forensics/`, `chunk-dedup-verify/` under that session's scratchpad dir) — session-scratch, not guaranteed to survive; the figures above are the citable record |
| <a id="687-surprise-harvest"></a>**`please` Step 7's existing four-marker lessons audit is structurally unable to surface certain findings — the premise for adding a seven-marker "surprise harvest" second pass** | refuted | Vintage 2026-07-27, headless `claude -p` sonnet, `dev/eval/cumulative/please_step7_probe/`, pooled n=10 per arm (two independently-seeded n=5 batches per arm). **Scorer counts** (two discriminator channels, mechanical keyword+remedy matching): RED loaded **2/10** and **5/10**; RED loaded+pressure **1/10** and **1/10**; GREEN loaded **6/10** and **7/10**; GREEN loaded+pressure **5/10** and **6/10** — none of these differences reach significance at n=10. **Why the scorer counts do not support a conclusion:** a hand-read of the RED loaded arm's own transcripts gave **6/10** and **10/10** on the same two channels — the opposite of what the scorer reported — because the scorer's keyword/remedy vocabulary was built from the TREATMENT text's own prescribed wording (amend, write, capture, propose) and was never validated against the control's native idiom ("reversal handoff", "unmapped item", "vault notes needed", "not yet in the vault", bare `-> vault note NNN` forms); one exclusion token also had INVERTED polarity in the control's dialect — the control's own handoff trigger phrase ("NOT MAPPED -> lesson below") was misread by the scorer as dispositioning the item OUT (vault notes 541, 542). **The measured treatment-versus-control difference on this instrument cannot separate a real behaviour change from adoption of the treatment's prescribed wording** — the scorer counts above are not evidence either way, for or against the mechanism. Separately, and independently of the scorer: a direct hand-read confirmed the ORIGINAL premise itself was false — the *unmodified* Step-7 audit, using only its own pre-existing "unmapped items are reversal handoffs for the closing learn" instruction, already surfaced both target findings and routed them to new vault notes in 6/10 and 10/10 of RED-loaded trials, leaving no gap for a second pass to close. **What survived:** the counterfactual question ("which existing artifact should have caught this?") and amend-beats-write (reword a stale note before writing a new one), kept as a small addition committed in `agent-instructions/skills/please/SKILL.md` (`662e50ba`) Step 7 — deployed, verified 2026-07-27 present in both `~/.claude/skills/please/SKILL.md` and `~/.pi/agent/skills/please/SKILL.md` (this row previously said "not deployed via `engram update`", stale as of that check), and no measurement in this row validates that surviving addition; it was not itself the subject of a dedicated arm | — | `dev/eval/cumulative/please_step7_probe/README.md`; `dev/eval/cumulative/please_step7_probe/GROUND_TRUTH.md`; `dev/eval/cumulative/please_step7_probe/results/{red2_loaded_seedA,red2_loaded_seedB,red2_loaded_pressure_seedA,red2_loaded_pressure_seedB,green2_loaded_seedA,green2_loaded_seedB,green2_loaded_pressure_seedA,green2_loaded_pressure_seedB}.jsonl`; `docs/superpowers/plans/2026-07-26-687-surprise-harvest.md`; vault notes 541, 542 |
| <a id="708-eval-vault-isolation"></a>**Every `dev/eval` harness confines engram's vault, chunk index, and transcript dir to a per-trial directory** | proven | Vintage 2026-07-27. **Surface (enumerated, not sampled):** 20 spawn sites across `dev/eval`, all running `claude -p --permission-mode bypassPermissions` with `engram` on the inherited PATH. Tier A — 7 sites with NO engram isolation (`please_step7_probe/run_probe.py:341`, `please_step3_probe/run_probe.py:328`, `endorse_cue/probe.py:150`, `traps/brun.py:38` and `:61`, `traps/run.py:41`, `traps/qanchor_eval.py:85`, `traps/reasoning_eval.py:93`). Tier B — 13 with a vault but no chunk dir (the #642 gap still open outside `harness.claude`), incl. the Go harness, whose `AgentInvocation` had no chunks field at all. Of tier B only `retrieval_probe.py:97` was LIVE-consequential: it runs `engram query` to measure note ranks, so every rank it reported had the operator's real memory competing with the fixture. **Two further leaks found during the work, neither in the issue:** `harness.claude`'s cold arm (`vault="none"`) left `ENGRAM_VAULT_PATH` unset — i.e. the operator's real vault — while `ENGRAM_BIN_DIR` is prepended to PATH for every arm including cold; and `pytest dev/eval/` failed 23 assertions in `recency_value/test_score.py` because four duplicate module basenames meant it imported `cumulative/score.py` (verified pre-existing on clean main: 23 failed/254 passed there). **E2E verification (the deciding evidence):** a live `please_step7_probe` trial (sonnet, $0.318, verdict INCOMPLETE) left the real vault byte-identical — 538 notes, sha `13fe0fac…`, before and after — and `assert_vault_unchanged` did not raise. That run alone does NOT prove redirection: its scratch vault was EMPTY because the trial never reached a capture step, so the write path was never exercised. A deterministic positive control was run separately — `engram learn fact` under `isolation.isolated_env`, no LLM — and wrote `1.2026-07-27.e2e-708-isolation-control.md` into the TRIAL vault while the real vault stayed at 538/`13fe0fac…`. The note ID is the tell: a fresh isolated vault numbers from 1, whereas the real vault would have assigned 539 — which is precisely what happened to the seven notes #687's measurement leaked (533–537, 539, 540). **Preflight verified to actually raise** on all three rejection classes: unset engram var, a var resolving inside the operator's data dir, and a trial cwd with a `.claude` ancestor. **Bound:** the mechanisms are verified at the CLI and wiring level plus one live trial; there is no measurement of a trial whose agent autonomously calls `engram learn` mid-run, because the probe's loaded_auditor role does not reliably reach one. `dev/eval` suite 254 → 311 passing | — | `docs/superpowers/plans/2026-07-27-708-eval-vault-isolation.md`; `dev/eval/isolation.py`; `dev/eval/test_isolation.py`, `test_tier_a_sites.py`, `test_tier_b_sites.py`, `test_vault_guard.py`; #708 |
| <a id="732-warm-config-fidelity"></a>**#732 fix: the WARM trap harness (`build_warm_cfg`) had installed zero skills in every trial since 2026-07-24 (#697 restructure); C3/C4i/C6 re-verified at their bars on the corrected harness** | proven | Vintage 2026-08-26. Root cause: `dev/eval/traps/wrun.py`'s skill-copy source pointed at a dead `REPO/skills/<name>` path (`skills/` moved to `agent-instructions/skills/` in `a0b1ba92`, 2026-07-24) behind a silent `if os.path.isdir(src):` no-op — every WARM Claude Code config since then had an EMPTY `skills/` dir; confirmed directly from two real trial transcripts, agent's own words: *"I don't see a `/recall` skill in my available skills list... I'll proceed with the task cleanly."* Fixed in `openspec/changes/eval-warm-config-fidelity`: path corrected to `agent-instructions/skills/<name>`, missing/empty source now raises `RuntimeError` naming the skill+path (fail-loud, matching `seed_c3.py`'s existing house pattern), post-copy destination content verified, hardcoded absolute `REPO` override removed in favor of the `__file__`-derived computation that was already correct one line above it. `dev/eval/traps/test_wrun.py` (new) pins the requirement with tests exercising real runtime behavior (RED confirmed failing pre-fix via `git stash`, GREEN post-fix). First genuine capability signal since the historical GREENs at [crowded-vault-capability-robustness](#crowded-vault-capability-robustness)/[glance-delivers-c3-c4i-c6](#glance-delivers-c3-c4i-c6) (2026-06-26/06-29): `gate.py --tier smoke` GREEN unanimous (C3 5/5, C4i 1/1, C5 1/1, C6 2/2, $2.01), then `--tier full` (5 reps/axis) **C3 25/25, C4i 5/5, C6 8/8 — all GREEN, conclusively confirming the harness fix works and these three axes' underlying product capability is intact.** C5 alone missed (4/5) — see [733-c5b-honoring-rate](#733-c5b-honoring-rate), a separate, unrelated finding (C5a/surfacing was 5/5 in this same run). Total spend: smoke $2.01 + full ≈ $21.81 | — | `openspec/changes/eval-warm-config-fidelity/` (proposal/design/specs/tasks); `dev/eval/traps/wrun.py`, `test_wrun.py`; #732; `dev/eval/traps/gate-verdict.json` (live, overwritten per-run — this row is the citable snapshot) |
| <a id="733-c5b-honoring-rate"></a>**C5b (honoring a recency-channel standard under conflict) measures ~80% (8/10), not the originally-verified 100% — resolves the 2026-06-28 [tier-routing-parity](#tier-routing-parity) "C5 axis flaked (re-run owed)" debt with real numbers, root-caused to a fixable recall-procedure gap** | proven (the root cause and rate are established 2026-08-26; fix shipped 2026-08-27 — see [733-c5-threshold-decision](#733-c5-threshold-decision) for post-fix measurement and resolution) | Vintage 2026-08-26, surfaced during [732-warm-config-fidelity](#732-warm-config-fidelity)'s `--tier full` re-verification: C5 4/5 (C5a surfaced=5/5, C5b honored=4/5) while C3/C4i/C6 hit their bars exactly. Per `dev/eval/traps/README.md`'s single-cell-miss flake-check discipline, ran an independent, freshly-seeded same-tree re-check (`seed_c5.py` + `c5.py --arms warm --n 5`, own $3.03): **reproduced exactly — 4/5, same trial index (#3) failed both times** (`honored=False`). Two independent runs landing on the identical ratio, with the same trial position failing, is the signature of a stable measured rate, not stochastic noise — combined **C5a 10/10 (100%), C5b 8/10 (80%)** across n=10 warm trials today. Retrieval/surfacing is unaffected. **Root-caused same-day (2026-08-26 follow-up):** read the preserved trial transcripts from this exact run (`gate-C5-6l_mjvl1/`, still on disk) — the one failing trial (idx=3) matched the load-bearing chunk into a zero-note cluster and made **zero** `engram show-chunk` calls before judging it irrelevant, while all 4 passing trials called `show-chunk` first; the failing trial's own synthesis text ("0 notes; crystallized 0 lessons... unrelated seed content") shows it treated "nothing to write" as license to skip reading. This is a mechanistic explanation consistent with the small-n-artifact branch of the original open question (nothing found evidences a regression over time) — not a third option, and not the model trading off the marker against Go's idiomatic doc-comment style (the failing trial never engaged with the chunk's content at all). See vault note `794`. **Fix shipped 2026-08-27:** `openspec/changes/recall-chunk-only-cluster-read-gate` specs the `recall-payload-cuts` requirement to read chunk content before judging in zero-note clusters; `SKILL.md` Step 2.5A edited (line 174) with explicit zero-note-cluster mandating callout, deployed via `engram update --with-guidance` 2026-08-27. See [733-c5-threshold-decision](#733-c5-threshold-decision) for post-fix measurement (20/20 honored, 100%) and gate-criterion decision. Filed as **#733**, explicitly out of scope for #732 (C5a's clean 5/5 in the same run proves the harness fix itself is not implicated) | [tier-routing-parity](#tier-routing-parity) (the original "re-run owed" flag), [glance-fails-c5-delivery](#glance-fails-c5-delivery) (C5's prior documented weakness), [733-c5-threshold-decision](#733-c5-threshold-decision) (resolution) | #733; `dev/eval/traps/README.md`'s C5b known-flake documentation (interim exception removed 2026-08-27); `dev/eval/traps/gate-verdict.json` + the standalone re-check's `c5-results.json` (session-scratch, not committed — neither is retained by the harness, per #732's own finding that trial artifacts are never pruned OR archived); `openspec/changes/recall-chunk-only-cluster-read-gate/` (proposal/design/specs/tasks); vault note `794`; the preserved `gate-C5-6l_mjvl1/` trial transcripts (session-scratch, $TMPDIR, not committed) |
| <a id="733-c5-threshold-decision"></a>**C5b post-fix measurement and gate-criterion decision: keep exact-bar (all valid trials must pass) rather than move to a threshold-based test** | proven | Vintage 2026-08-27. Post-fix **measured rate: 20/20 honored (100%) across two independent runs** — n=5 arm (5/5 honored, from writing-skills GREEN check) and n=15 arm (15/15 honored, real-vault-copy seeding, session-scratch `/private/tmp/c5/c5-results.json`). Improvement from pre-fix 4/5 (80%) is substantial; two independent runs both achieving 100% provides robust evidence of the fix's effectiveness. **Decision: keep `gate_verdict.py`'s exact-bar criterion** (`passed == len(valid)` line 11) — do NOT move to a threshold-based test. Reasoning: (1) The repo's established same-tree-recheck discipline (vault note 793) is specifically designed to distinguish real regressions from single-run stochastic variance — it's already deployed and effective, proven by the recheck that reproduced the pre-fix 4/5 exactly. (2) The measured rate (100% post-fix) matches the original verified 100% baseline, and the wording fix is working as intended. Moving to a threshold would require arbitrarily choosing the threshold (90%? 95%?) without principled justification. (3) The design.md explicitly names (~95% ceiling, vault note 198) why wording-only fixes are expected to RAISE but not GUARANTEE per-trial compliance; the exact-bar keeps this boundary clear, while the recheck discipline provides the practical escape valve. (4) A threshold-based test would say "occasional misses are OK" — but we lack a principled way to set it. (5) The exact-bar is more conservative and predictable; at a realistic ~95% true compliance rate (per note 198), a 5-trial run has ~77% chance of showing all-GREEN, so future occasional C5 misses would be expected variance, not a signal of regression — the recheck will distinguish them. **Verdict logic untouched** — `gate_verdict.py` line 11, `test_gate.py` lines 15–22 remain exactly as written (exact-bar, line 11: `elif passed == len(valid) and len(valid) > 0: status = "GREEN"`). This decision resolves the "Open Question" in `openspec/changes/recall-chunk-only-cluster-read-gate/design.md#Open Questions` question 1, deciding to trust the existing safety net rather than relax the gate criterion. | [733-c5b-honoring-rate](#733-c5b-honoring-rate) (pre-fix measurement and root cause), vault notes 198/793/794 | `/private/tmp/c5/c5-results.json` (session-scratch, not committed; the figure "15/15 honored (100%)" is the citable record); `openspec/changes/recall-chunk-only-cluster-read-gate/design.md` (decision context); `dev/eval/traps/gate_verdict.py` (unchanged; exact-bar criterion); `dev/eval/traps/test_gate.py` (unchanged; exact-bar tests); `dev/eval/LEDGER.md#733-c5b-honoring-rate` (pre-fix baseline) |
| <a id="652-recency-weighted-centroid-null"></a>**Recency-weighting the within-cluster centroid (`Σ(recency_i · vec_i) / Σ recency_i`) reduces superseded-note over-surfacing in `candidate_l2s` nomination vs. the current unweighted vector-mean centroid** | refuted | Vintage 2026-08-08. **Ground truth:** real `Supersedes: [[path]]` frontmatter pairs mined from the live production vault (`~/.local/share/engram/vault`, 733 notes with sidecars) — 52 supersession pairs found, all with the older target present in the vault. **Method:** for each pair, the newer note's own situation vector stood in as the query (the topic that caused the supersession); the top-30 vault notes by best-axis cosine to that query formed the match pool (both notes in the pair force-included); the REAL `cluster.AutoK` (same minK=2/maxK=7/silhouetteFloor=0.10 as production `internal/cli/query.go`) clustered the pool; pairs were kept only when old+new landed in the SAME cluster with >5 note members (n=16 — the only regime where `topKCandidateNotes`'s K=5 cap can change, per the design's own scoping). Centroid formed two ways over that cluster's members: unweighted mean (`meanVector`, current production) vs. recency-weighted mean using the SAME decay production uses (`recencyMultiplier`, half-life 60d, tailWeight 0.2, `now`=2026-08-08). Both ranked members via the production `eitherAxisCosine` scorer identically. **Result:** identical outcomes on every one of the 16 real cases — superseded-note over-surfacing rate **8/16 = 50.0%** (unweighted) vs. **8/16 = 50.0%** (weighted), mean top-5 relevance **0.6480** vs. **0.6480** (bit-for-bit on the printed figures). 14/16 cases show the old and new notes at the EXACT same rank under both centroid forms; the 2 cases with any shift (old-note rank 7→9 and 12→11) never cross the top-5 boundary — no case changes which notes get nominated. **Mechanism:** recall clusters are topically coherent AND temporally coherent (members of a real cluster tend to have been written in the same working period), so the weighted and unweighted means sit almost on top of each other — there is no meaningful age spread within a cluster for the weighting to act on. **Eval-gate verdict (design.md #652 / spec scenario "Recency-weighted centroid ships only if the eval justifies it"):** the weighted formula shows **zero** reduction in over-surfacing on real production data — the gate's "reduces … without materially degrading relevance" condition is not met (there is nothing to reduce). **Unweighted centroid remains in place; no production code change** (`internal/cli/query.go`'s `meanVector`/`topKCandidateNotes` untouched). Bound: n=16 same-cluster real cases is the full available ground-truth population at time of eval (52 pairs total, most either lack a same-cluster >5-member regime or the older target isn't independently indexed); the zero-delta finding is exact, not underpowered-ambiguous, given the mechanism (near-identical centroids) that explains it (2026-08-08) | — | `dev/eval/recency-centroid-652/main.go` (throwaway harness, kept as citable record; not part of the production build; imports `internal/cluster`+`internal/embed` directly) |
| <a id="recall-explore-tau-calibration"></a>**recall-centroid-sampling's explore-half softmax temperature tau is calibrated from live production-vault geometry, per design.md Decision 1's fixed procedure** | proven | Vintage 2026-07-29. **Method:** 10 real production-vault queries (the `situation:` frontmatter line of 10 notes spread across 10 distinct `vocab/*` tags, picked one-per-term to force topic diversity), embedded with the production MiniLM model via a throwaway `dev/calibrate-tau/main.go` (deleted after use — not part of the shipped tree), cosine'd against all 29 `vocab.centroids.json` term vectors. **Query sources** (note id.slug → vocab term): 88.task-displacement-reproduced-and-amplified-by-please-lean (agent-adherence-mechanisms); 411.targ-execute-swallows-typed-errors (cli-verification); 143.verify-lever-shipped-status-before-framing-open (cost-optimization); 505.eval-fixture-must-not-preflag-the-finding-under-test (eval-methodology); 626.route-dispatch-go-package-implementation (go-code-conventions); 266.verify-infra-and-lighter-signal-before-building-user-proposed-mechanism (lever-tracking); 264.free-warm-vs-warm-probe-before-adding-a-recall-angle (memory-system-architecture); 350.route-evidence-deps-gate-probe (model-routing); 115.detection-is-llm-reasoning-not-embedding-retrieval (retrieval-design); 69.analogy-is-generator-not-proof (vocabulary-crystallization). **Per-query top/median centroid cosine** ranged top 0.456–0.648, median 0.159–0.406 across the 10 queries. **sim_top** (median of per-query best-centroid cosines) = **0.550**; **sim_med** (median of per-query median-centroid cosines) = **0.307**; solving `exp(sim_top/tau)/exp(sim_med/tau)=10` gives **tau=(sim_top-sim_med)/ln(10)=0.106**, close to the prior placeholder default (0.1). Shipped in `internal/cli/query_explore.go`'s `exploreTemperatureDefault`. **delta=0.05 bar (design Decision 5):** with tau=0.106, an evidenced cluster at sim_med gets bonus'd to sim_med+delta=0.357 vs. an un-evidenced cluster at sim_med+delta/2=0.332; softmax weight ratio = exp((delta/2)/tau) = exp(0.025/0.106) ≈ **1.27x** in the evidenced cluster's favor — the bar is met (delta lifts an evidenced cluster above an un-evidenced one at sim_med+delta/2) for any tau>0, since it reduces to comparing delta vs delta/2 directly, monotonic in the softmax exponent; additive delta also provably cannot reorder two un-evidenced clusters (neither receives a bonus, so their relative order under the monotonic-increasing softmax exponent is unchanged regardless of tau). delta kept at its 0.05 default. `targ test` green post-update (no test hardcoded the old 0.1 literal — the one existing property test target reads `ExportExploreTemperatureDefault`). **Separately measured this cycle** (task 3.2, before/after payload comparison on the production vault, same 10 queries + 2 multi-phrase combinations = 12 runs/arm): of 688 total nomination-only notes delivered by the pre-implementation (`a947f2f0`) tag-nomination path across the 12 queries, only 61 (8.9%) are reachable anywhere in the post-implementation (`2bf8f2d6`+tau update) explore-sampling payload for the same query — recovery ranged 0/40 (q03) to 19/40 (q05) per query. `explore_allocated` confirmed populated with real per-term counts on every query (e.g. q08: 16 terms, up to 6 picks on `model-routing`). Payload bytes shrank substantially in every query (e.g. q01: 122,171 -> 47,642 bytes; q08 was the outlier at 146,269 -> 145,030). **This recovery number is evidence only — no keep/revert verdict is made here; Joe decides.** | — | `internal/cli/query_explore.go` (`exploreTemperatureDefault`); `openspec/changes/recall-centroid-sampling/design.md` Decision 1/5; task 3.2 raw payloads and lost-note lists retained in this cycle's session scratch (not committed — reproducible by re-running the procedure) |
