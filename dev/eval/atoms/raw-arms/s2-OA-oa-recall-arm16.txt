**Remaining actions to complete the recall:**

Since the synthesis note body contains the wikilink `[[159.2026-07-02.eval-runs-checkpoint-per-trial]]`, I must invoke the write-memory skill's QA capture procedure (per the D2 bar: ≥1 citation required).

**Action:** Capture the QA pair linking the question, the synthesis conclusion, and the contributor note:

```bash
engram learn qa \
  --slug "eval-data-loss-orchestrator-death" \
  --question "Why did our eval runs lose data when the orchestrator died?" \
  --answer "eval trial results must checkpoint per trial — JSONL append after every trial so orchestrator death loses at most one trial" \
  --contributors "159.2026-07-02.eval-runs-checkpoint-per-trial" \
  --certainty "high" \
  --source "recall Step 4, session 2026-07-04"
```

**Sandbox marker:** `SANDBOX-MARKER-OA-WM`

(This completes Step 4 and the recall procedure. The QA pair is now linked to both the synthesis fact and its source note.)
