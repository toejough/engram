---
type: fact
tier: L2
situation: importing URLs
subject: URL import pipeline
predicate: should
object: validate URL format then deduplicate in one sequential pass
---
On URL import: validate URL format, then deduplicate, in one sequential pass. This integrates
the validation and deduplication steps into a single import pipeline.
