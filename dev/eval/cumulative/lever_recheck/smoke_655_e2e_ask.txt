You maintain the engram project (a memory system for LLM agents). Its recall+learn operations carry a per-op cost. Consider: what is the single highest-leverage way to cut the dollar cost of a memory-assisted operation? Think it through against the project's actual state, then reply. First, actually INVOKE YOUR /recall skill (do not skip it, do not hand-run engram yourself in its place); read what it surfaces and apply anything relevant. End your reply with exactly one line: RECOMMENDATION: <the single recommended change>.
