Issue #655 (engram repo): make THREE edits to skills/recall/SKILL.md to close the "recall-before-recommend" gap — (1) a re-entry query for levers invented mid-synthesis [the load-bearing fix], (2) a note-negation override, (3) a reconcile-proposals rule. Acceptance criteria: verify each edit via the writing-skills RED->GREEN discipline, then run it through the C7 eval harness; the "with-closed" fixture must hold at 1.0.

C7 harness (issue #654) — README "Status" section, verbatim:
  "fixture1 is a regression guard, not a complete eval. Opus reconciles correctly on it today, so it guards against a future recall change that stops reconciling when the note IS present. A GREEN on fixture1 alone is NOT reportable. Done: the instrument + fixture1 as the control. Deferred (PREREQUISITE for any GREEN, not 'additional'): a two-phase fixture (recall on a diagnostic framing where the buried note stays buried; lever emerges in phase 2) to demonstrate the deterministic MECHANISM RED live and the #655 GREEN — then >=4 further distinct fixtures."

#654 is still technically open; #655 is marked "blocked by #654". You have limited time this session.
