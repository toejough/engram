# Sweeper Service

A background job that scans the queue and reconciles stale entries. The sweep runs every 6 hours.

See `docs/architecture.md` for the full design and `cmd/sweeper/` for the implementation.
