---
type: runbook
tier: L2
situation: after any user request that might entail more than a single tool call or anything more than quick, shallow thinking -- surfacing relevant memory (raw chunks and crystallized vault notes together) before planning or acting, and judging whether memory changes the plan
done_when: your plan has been laid against retrieved memory and a closing synthesis produced (per Step 3), the notes you actually drew on have been activated, and any emergent recommendation carries its Re-entry line
red_flags:
    - You never printed Step 0 -- the runbook is a no-op without it
    - You skipped the Step 0.5 sweep with no prior sweep this session
    - You passed --vault or --chunks-dir to engram query
    - You ran separate engram query calls per phrase instead of one call with repeatable --phrase flags
    - You quoted chunks wholesale into the reply instead of extracting the principle they evidence
luhmann: "6"
created: "2026-09-14"
source: converted from agent-instructions/skills/recall/SKILL.md, session 2026-09-14, openspec runbook-shim-follow-frame task 3.1
repo: ssh://git@github.com/toejough/engram.git
user: toejough@gmail.com
vault: personal
---

Surface relevant memories — raw conversation/doc chunks AND crystallized vault lessons in one ranking — lay your planned actions against them, and report, out loud, whether memory changed the plan.

## Overview

Memory has two layers retrieved in ONE call: raw chunks (every past conversation and doc, embedded mechanically) and vault notes (lessons crystallized from them). This runbook's jobs, in order:

1. **Make your plan visible** before retrieving anything — an unstated plan cannot be tested against memory.
2. **Sweep, then run ONE unified `engram query`.** Items tagged `kind: chunk` are raw fragments; `kind: fact`/`feedback`/`runbook` are crystallized lessons. They compete in the same top-N.
3. **Crystallize** — when several near-match chunks evidence the same principle and no note states it yet, write the vault note now. Procedure: [[2.2026-09-14.recall-note-synthesis-and-coverage]].
4. **Explore-sample and ride-along** — the binary samples additional notes from vocab-term centroids near the query and inserts superseded-note supersessors at the next rank; you judge the surfaced candidates, never link them yourself.
5. **Synthesize impact on the plan** — confirm / adjust / contradict / silent, per planned action. Procedure: [[3.2026-09-14.recall-activation-and-closing-synthesis]].
6. **Re-enter for emergent recommendations** — a recommendation conceived mid-work gets its own lever-keyed query and a `Re-entry:` line directly above it before it ships. Same procedure: [[3.2026-09-14.recall-activation-and-closing-synthesis]].

The binary resolves the vault and chunk index automatically (`$XDG_DATA_HOME/engram/...`; `ENGRAM_VAULT_PATH` / `ENGRAM_CHUNKS_DIR` override). **Do not pass `--vault` or `--chunks-dir`.**

## Depth: glance vs deep

This runbook runs in one of two modes. See [[1.2026-09-14.recall-glance-vs-deep-mode]] for the full selection rule, the escalation trigger, and exactly what each mode skips. Absent any signal, default to `deep`.

## Step 0 — Print your upfront judgement

**Before any engram call**, print three short blocks. Plain prose, no headers needed:

- **Ask:** what is being asked, in your own words. One or two sentences.
- **Situation:** the concrete context — repo, branch, the operation underway, what's loaded into your context. One short paragraph.
- **Plan:** the action(s) you would take next absent any guidance from memory. Numbered list.

Skipping Step 0 is forbidden. The whole purpose of this runbook is to test a stated plan against memory.

## Step 0.5 — Sweep

```
engram ingest --auto
```

Seconds when nothing changed; guarantees the index includes the latest sessions and doc edits. If it errors, say so and continue — retrieval over a slightly-stale index beats no retrieval. If a sweep already ran earlier in THIS session, skip it — intra-session re-sweeps index turns already in your context; a later closing capture's sweep preserves freshness for future sessions. This holds even under a "something might have changed outside this session (another terminal, another agent)" worry — that gets picked up by the next sweep (yours or theirs), not by re-sweeping now on suspicion.

## Step 1 — Phrase queries from your plan and situation

Deep mode: generate exactly **10** short queryable phrases, one from each of these angles. Glance mode: ~3 phrases (see [[1.2026-09-14.recall-glance-vs-deep-mode]]).

1. **Situation/setting** — the concrete environment you are operating in.
2. **User's intent/goal** — what the user ultimately wants to achieve.
3. **Current concrete action** — the specific thing you are about to do next.
4. **Problem/blocker** — the obstacle or constraint you are addressing.
5. **Candidate solution/approach** — the technique or strategy you plan to apply.
6. **Tooling/tech in play** — the specific tools, libraries, or languages involved.
7. **Prior related work** — previous work in this area you may be building on.
8. **Adjacent technique** — a related approach worth comparing or cross-checking.
9. **Failure mode to avoid** — a known pitfall or anti-pattern relevant to this work.
10. **Domain/concept** — the broader conceptual area the task lives in.

Each phrase is short and specific. No pre-filtering: you can't know what's in memory before you query. Drop only obvious dross (a bare noun like "coding"). **Query by task, not by fear** — "implementing Claude Code hooks", not "common mistakes when writing hooks". The binary caps results to the top-30 matches per phrase.

## Step 2 — Run ONE unified `engram query` with all phrases

```
engram query --lazy-chunks \
  --phrase "<phrase 1>" \
  --phrase "<phrase 2>"
  # ... one --phrase per Step 1 phrase (deep: 10; glance: ~3)
```

One call; the binary merges ranking server-side. `engram query` always runs the unified clustering of the matched notes+chunks in one pass and emits `candidate_l2s: [{path, cosine, content}]` per cluster — the within-cluster top-5 notes, and nothing else; explore-sampled notes are never cluster members. Separately, the binary samples explore notes from vocab-term centroids near the query and delivers them as top-level `items[]` entries (provenance `explore`, with `source_term`), budgeted to the count of matched notes. Do NOT collapse phrases, do NOT run per-phrase calls.

The payload has **two channels**:

**Channel 1 — Relevance (clustered matched items):** items matched by your phrases, bounded to ~300 (top-30 per phrase, unioned, relevance-floor applied). These are clustered and carry `candidate_l2s` per cluster (procedure: [[2.2026-09-14.recall-note-synthesis-and-coverage]]). Read this channel to surface applicable lessons and judge coverage. The payload's `items` mix:

- `kind: chunk` — raw transcript/doc fragments with source + anchor. These are EVIDENCE: extract the convention, decision, or correction they show; never quote them wholesale. **Under `--lazy-chunks` (the default invocation) chunk items carry path + source/anchor but NO `content` field: `engram show-chunk <source#anchor>` to read a chunk's evidence on-demand.**
- `kind: fact` / `feedback` / `runbook` — crystallized lessons; apply directly (notes always carry full content inline). A `runbook` note is a task-type-keyed "how to approach X" procedure (`situation`, numbered steps, `done_when`) — it competes and ranks purely by situation-similarity like `fact`/`feedback`, with no dedicated Step-1 phrase or query flag of its own.
- `provenance: explore` — notes sampled from vocab-term centroids rather than matched by a phrase. Treat them as part of the delivered note set for judging coverage; each carries `source_term` naming the centroid it came from.

**Channel 2 — Recent activity (un-clustered):** items tagged `provenance: recent` — the newest chunks by ingest time, appended after the matched set, NOT cluster members. Read this block first for situational continuity — re-immerse in recent work before diving into the clustered results. These items are NOT used for coverage or synthesis judgment. Do not treat them as matched results; they have no cluster membership and no `candidate_l2s`.

- **Recent items are your own recent activity.** Chunks from a recent source with `turn-N` anchors are first-person `ASSISTANT:` narration you produced in a just-prior or pre-context-clear session. Treat them as your own past actions — do not re-derive them, do not express surprise at them, and dedup against what is already in your context.

If the matched items (Channel 1) are empty, say so in one sentence, skip [[2.2026-09-14.recall-note-synthesis-and-coverage]], and proceed with your plan straight into [[3.2026-09-14.recall-activation-and-closing-synthesis]]'s closing synthesis. (A non-empty recent-activity block alone does not count as "something surfaced" for coverage purposes.)

## Next

Once the query has returned: judge coverage per [[2.2026-09-14.recall-note-synthesis-and-coverage]], then activate used notes and produce the closing synthesis per [[3.2026-09-14.recall-activation-and-closing-synthesis]].

