---
type: fact
tier: L2
situation: routing a subagent dispatch and deciding which agent type, model tier, and effort level to use, based on work characteristics and prior evidence
subject: the route skill's dispatch and tier-selection procedure
predicate: requires, in order
object: '(1) orchestrator vs object-level work boundary: you do routing/decomposition/dispatching/sequencing/meta-skills (/recall, /learn, planning), delegate object-level work (code/prose writing, testing, artifact review); (2) tier selection: recall first for tier-performance evidence ("cheap sufficed for X", "cheap failed on Y — needs mid"), default to cheapest tier for EVERY unit (no exceptions, including work that feels hard — predicted difficulty is not evidence), escalate on failure spec-first (first fail → rewrite spec + retry same tier, second fail → escalate one tier), memory-backed units drop one tier; (3) handoff completeness: exact files/paths, acceptance checks (concrete verifiable conditions: command+output, passing test, held property), explicit do-NOT-touch bounds, recall-first instruction, LESSONS: completion-report contract (subagent reports final report must end with "LESSONS: none" or 1–3 one-line lessons — confirmed corrections, surprising findings, validated approaches; one follow-up if missing, then record "LESSONS: none" and move on); (4) evidence recording per dispatch: classwork-kind (unit shape, kept consistent for prior-work reuse), tier used (cheap/mid/deep), model at dispatch (e.g. "cheap (haiku)"), why (recalled evidence / memory-discount / cold-start default), outcome (review verdict PASS/FAIL, never subagent self-report), escalation (if failed and escalated, tier it passed at), duration (harness wall-clock or best-effort observed), cost (harness usage → dollar via price table with explicit units, or raw token count, never fabricated); (5) structured vault write after dispatch resolves: evidence note handed to write-memory with kind/slug/tags/situation/subject/predicate/object/source fields and aggregate update via engram query/amend/learn-fact commands; (6) loop that improves rubric: route picks tier → record outcome → evidence note/aggregate recallable immediately → /learn crystallizes strong general findings → /recall surfaces evidence next dispatch → starting tier reflects evidence; (7) cold-start priors (overwritable by evidence): everything by default cheap, memory-backed units one tier down floored at cheap, work-kind/go-package-implementation mid; (8) two rules every dispatch obeys: subagent recalls first (mandatory /recall before work, with unit-drawn phrases), decompose before dispatch (break large multi-file multi-concern units into smaller pieces and route each, decomposition is orchestration not delegation); (9) red-flag stop-points: do not pick tier based on "looks hard" hunch (only recalled evidence raises start), never on first failure escalate (rewrite spec first), never skip recording outcome (no record → no evidence → rubric never improves), never omit structured write (do write-memory handoff + route-evidence-<work-kind> amend-or-create), never take subagent "done" claim as outcome (outcome is review verdict ground truth, subagent reports confabulate), never invent cost/duration (only if harness exposes), never treat cold-start table as prescription (evidence wins), never hardcode model name (route by tier not name), never skip subagent recall (mandatory), never dispatch complex work as one big agent (decompose first).'
luhmann: "1"
created: "2026-09-12"
source: 'session 2026-09-12, context: route skill (agent-instructions/skills/route/SKILL.md) converted to fact for phase-2 eval'
repo: ssh://git@github.com/toejough/engram.git
user: toejough@gmail.com
vault: personal
---

Information learned: when routing a subagent dispatch and deciding which agent type, model tier, and effort level to use based on work characteristics and prior evidence, the route skill's dispatch and tier-selection procedure requires, in order: (1) orchestrator vs object-level work boundary: you do routing/decomposition/dispatching/sequencing/meta-skills (/recall, /learn, planning), delegate object-level work (code/prose writing, testing, artifact review); (2) tier selection: recall first for tier-performance evidence ("cheap sufficed for X", "cheap failed on Y — needs mid"), default to cheapest tier for EVERY unit (no exceptions, including work that feels hard — predicted difficulty is not evidence), escalate on failure spec-first (first fail → rewrite spec + retry same tier, second fail → escalate one tier), memory-backed units drop one tier; (3) handoff completeness: exact files/paths, acceptance checks (concrete verifiable conditions: command+output, passing test, held property), explicit do-NOT-touch bounds, recall-first instruction, LESSONS: completion-report contract (subagent reports final report must end with "LESSONS: none" or 1–3 one-line lessons — confirmed corrections, surprising findings, validated approaches; one follow-up if missing, then record "LESSONS: none" and move on); (4) evidence recording per dispatch: classify work-kind (unit shape, kept consistent for prior-work reuse), tier used (cheap/mid/deep), model at dispatch (e.g. "cheap (haiku)"), why (recalled evidence / memory-discount / cold-start default), outcome (review verdict PASS/FAIL, never subagent self-report), escalation (if failed and escalated, tier it passed at), duration (harness wall-clock or best-effort observed), cost (harness usage → dollar via price table with explicit units, or raw token count, never fabricated); (5) structured vault write after dispatch resolves: evidence note handed to write-memory with kind/slug/tags/situation/subject/predicate/object/source fields and aggregate update via engram query/amend/learn-fact commands; (6) loop that improves rubric: route picks tier → record outcome → evidence note/aggregate recallable immediately → /learn crystallizes strong general findings → /recall surfaces evidence next dispatch → starting tier reflects evidence; (7) cold-start priors (overwritable by evidence): everything by default cheap, memory-backed units one tier down floored at cheap, work-kind/go-package-implementation mid; (8) two rules every dispatch obeys: subagent recalls first (mandatory /recall before work, with unit-drawn phrases), decompose before dispatch (break large multi-file multi-concern units into smaller pieces and route each, decomposition is orchestration not delegation); (9) red-flag stop-points: do not pick tier based on "looks hard" hunch (only recalled evidence raises start), never on first failure escalate (rewrite spec first), never skip recording outcome (no record → no evidence → rubric never improves), never omit structured write (do write-memory handoff + route-evidence-<work-kind> amend-or-create), never take subagent "done" claim as outcome (outcome is review verdict ground truth, subagent reports confabulate), never invent cost/duration (only if harness exposes), never treat cold-start table as prescription (evidence wins), never hardcode model name (route by tier not name), never skip subagent recall (mandatory), never dispatch complex work as one big agent (decompose first).
