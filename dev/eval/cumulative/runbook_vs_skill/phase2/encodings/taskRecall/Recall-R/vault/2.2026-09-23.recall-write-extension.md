---
type: runbook
tier: L2
situation: carrying out the write action for a cluster's already-judged Covered, Near, or Absent verdict, and persisting a sound closing-synthesis conclusion -- runs only under recall-deep, never under recall-glance
done_when: every cluster's verdict has the right single write issued (or the Covered action's provenance-only amend, never a content rewrite), and any sound non-trivial synthesis conclusion has been handed to write-memory, with its QA capture if the body wikilinks a citation
red_flags:
    - You called engram learn --target to update a note in place instead of engram amend
    - You wrote two notes (a fact AND a feedback) for one cluster instead of one representative note
    - You rewrote a Covered candidate's content instead of provenance-enriching only
    - You composed an engram learn or amend command yourself instead of handing off to write-memory
    - You hand-authored a vocab tag or a Supersedes backlink instead of letting the binary assign them
    - You tried to invoke write-memory as a Skill tool instead of running it in Bash
luhmann: "2"
created: "2026-09-23"
source: converted from agent-instructions/skills/recall/SKILL.md, session 2026-09-23, openspec recall-glance-skill-to-runbook task 2.5
repo: ssh://git@github.com/toejough/engram.git
user: toejough@gmail.com
vault: personal
---

Runs only under `recall-deep`, after [[1.2026-09-23.recall-core]]'s Step 2.5 has already judged each cluster's Covered/Near/Absent verdict. This note carries out the write ACTION for that verdict, and Step 4's closing-synthesis persist. `recall-glance` never reaches this note — it stops after [[1.2026-09-23.recall-core]]'s Step 3.5.

## The write action per coverage verdict (recall-core's Step 2.5C)

| Outcome | Action |
| --- | --- |
| **Covered** | `engram amend --target <candidate-path> --activate --chunk-source <new-chunk-ids>` — provenance-enrich only; **do not rewrite content**. If this note CORRECTS/narrows/refutes a surfaced note, also pass `--supersedes "<basename>\|<type>\|<claim>"` (types: `updates\|narrows\|refutes`). |
| **Near** | `engram amend --target <candidate-path> --chunk-source <chunk-ids> --subject ... --predicate ... --object ...` (or `--behavior/--impact/--action`) — re-synthesize content from all members, recency-weighted. Add `--supersedes "..."` if this note corrects a surfaced note. |
| **Absent** | Follow [[1053.2026-09-22.write-memory-compose-execute-verify]] with this handoff — kind=fact or feedback (pick per the cluster's principle), situation + content fields, `--source "<descriptive>"`, the cluster's chunk-source IDs, plus supersedes details if the new note corrects a surfaced note. It composes, executes, and reports the note path. |

**One write per cluster; one representative note per cluster.** The representative is always a note (never a chunk). For `absent`, write exactly one note (fact *or* feedback) covering the cluster's principle — never one fact AND one feedback for the same cluster.

Pass one `--chunk-source <source#anchor>` (repeatable) for every **chunk** source in the cluster (provenance tracking). For a new note, pass the same flag plus `--source` (human-readable provenance). Vocab tags are assigned **automatically** by the binary on every write — do not hand-author them. Never hand-author a `Supersedes:` backlink either — the binary maintains the inverse automatically from `--supersedes`.

**WAIT for each write before moving to [[1.2026-09-23.recall-core]]'s next cluster.** Writes are blocking and inline — the note created or updated by one cluster may be a candidate for another.

## Step 4 — Persist the reasoned conclusion from Step 3's closing synthesis

When the Step 3 closing synthesis (in [[1.2026-09-23.recall-core]]) reaches a **sound, non-trivial conclusion that no existing note states** — something a future session (or a *less capable model* that can't re-derive it) would want, and that you or a human may later **inspect or correct** — crystallize it. Reasoning that is never written down evaporates; this records the *outcome* and grows the web.

Follow [[1053.2026-09-22.write-memory-compose-execute-verify]], handing it ONE synthesis note per conclusion (kind=fact or feedback, per the conclusion's shape):

- **The note IS the conclusion** (the reasoned lesson), phrased as such — not a restatement of an input.
- **Certainty by inference mode:** deduction → state it as following necessarily; **abduction / induction → mark it _probable / best-explanation / defeasible_**, never as certain.
- **Mark it as derived** in `--source`, e.g. `--source "synthesis (abduction) from recalled memory"`, so a human or a weaker model can tell it is a reasoned conclusion to review — not a primitive fact.
- **If the synthesis conclusion CORRECTS, narrows, or refutes an existing surfaced note**, include the superseded note's basename, type (`updates|narrows|refutes`), and claim in the write-memory handoff — the binary maintains the inverse automatically. Otherwise no link ritual is needed. This does NOT restrict inline `[[full-basename]]` prose citations in the synthesis body: hand-type one directly in the body whenever it cites an input note's claim — ordinary authoring, no CLI mechanism does it for you (the Gate below, plus the QA-capture step, depend on you having written them).

**Gate — do not rot the vault:** persist ONLY conclusions you judge sound. If it is a hunch, you'd hedge below "probable", or it merely re-aggregates one note, do NOT persist. One synthesis note per conclusion; link all of its inputs.

**After the synthesis note: if the synthesis body contains ≥1 `[[full-basename]]` wikilink, ALSO follow [[1053.2026-09-22.write-memory-compose-execute-verify]]** with kind=qa — verbatim question, the synthesis conclusion as the answer, certainty matching the synthesis note's label, contributors = the wikilink basenames, source "recall Step 4, session <date>". Contributors are auto-extracted from the wikilinks you already wrote in the synthesis body — do NOT free-list. If the synthesis body contains no wikilinks, skip the QA capture.

