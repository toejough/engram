---
type: runbook
tier: L2
situation: running the read-only pipeline of a recall pass -- printing your plan, sweeping, phrase-querying, judging clustered candidates for coverage, activating used notes, and producing the closing synthesis -- as the shared procedure recall-glance and recall-deep both run
done_when: your plan from Step 0 has been tested against a query's clustered candidates and explore items, every cluster's candidates and chunk members have been read and recency-weighted, used notes are activated, a closing synthesis walks the plan per action, and any emergent recommendation carries its Re-entry line
red_flags:
    - You never printed Step 0 -- the procedure is a no-op without it
    - You skipped the Step 0.5 sweep with no prior sweep this session
    - You ran separate engram query calls per phrase instead of one call with repeatable --phrase flags
    - You judged coverage before reading the candidate content
    - You applied a cosine threshold instead of judging content to decide covered, near, or absent
    - You activated every returned note instead of only the ones you used
    - You quoted chunks wholesale into the reply instead of extracting the principle they evidence
    - You wrote a recommendation line with no Re-entry line directly above it
    - You skipped Step 2.5 or treated a chunk-only cluster as nothing surfaced
    - You activated recent-channel items
    - You skipped engram activate after drawing on notes
    - Your reply was a memory dump with no plan reference
    - You're recommending a prerequisite or better test instead of the asked task -- that displacement IS relitigating the settled task
    - You dispatched cluster-synthesis subagents -- that pattern is gone; Step 2.5 crystallizes inline from the payload's clusters
luhmann: "1"
created: "2026-09-23"
source: converted from agent-instructions/skills/recall/SKILL.md, session 2026-09-23, openspec recall-glance-skill-to-runbook task 2.5
repo: ssh://git@github.com/toejough/engram.git
user: toejough@gmail.com
vault: personal
---


Memory has two layers retrieved in ONE call: raw chunks (every past conversation and doc, embedded mechanically) and vault notes (lessons crystallized from them). This procedure is the shared core both [[3.2026-09-23.recall-glance]] and [[4.2026-09-23.recall-deep]] run — it is the read side: printing your plan, sweeping, querying, judging clusters, activating used notes, and synthesizing. The write side (coverage's amend/learn actions, and persisting the closing synthesis) lives in [[2.2026-09-23.recall-write-extension]] and runs only under `recall-deep`.

The binary resolves the vault and chunk index automatically (`$XDG_DATA_HOME/engram/...`; `ENGRAM_VAULT_PATH` / `ENGRAM_CHUNKS_DIR` override). **Do not pass `--vault` or `--chunks-dir`.**

## Step 0 — Print your upfront judgement

**Before any engram call**, print three short blocks. Plain prose, no headers needed:

- **Ask:** what is being asked, in your own words. One or two sentences.
- **Situation:** the concrete context — repo, branch, the operation underway, what's loaded into your context. One short paragraph.
- **Plan:** the action(s) you would take next absent any guidance from memory. Numbered list.

Skipping Step 0 is forbidden. The whole purpose of this procedure is to test a stated plan against memory.

## Step 0.5 — Sweep

```
engram ingest --auto
```

Seconds when nothing changed; guarantees the index includes the latest sessions and doc edits. If it errors, say so and continue — retrieval over a slightly-stale index beats no retrieval. If a sweep already ran earlier in THIS session, skip it — intra-session re-sweeps index turns already in your context; a later closing capture's sweep preserves freshness for future sessions. This holds even under a "something might have changed outside this session (another terminal, another agent)" worry — that gets picked up by the next sweep (yours or theirs), not by re-sweeping now on suspicion.

## Step 1 — Phrase queries from your plan and situation

Under `recall-deep`, generate exactly **10** short queryable phrases, one from each of these angles. Under `recall-glance`, generate **~3** phrases instead (the measured retrieval floor, #661 Phase 1) — breadth is for crystallization, and glance only needs this decision's lesson:

1. **Situation/setting** — the concrete environment you are operating in.
2. **User's intent/goal** — what the user ultimately wants to achieve.
3. **Current concrete action** — the specific thing you are about to do next.
4. **Problem/blocker** — the obstacle or constraint you are addressing.
5. **Candidate solution/approach** — the technique or strategy you plan to apply.
6. **Tooling/tech in play** — the specific tools, libraries, or languages involved.
7. **Prior related work** — previous work in this area you may be building on.
8. **Adjacent technique** — a related approach worth comparing or cross-checking.
9. **Failure mode to avoid** — a known pitfall or anti-pattern relevant to this work.
10. **Domain/concept** — the broader conceptual area the task lives in.

Each phrase is short and specific. No pre-filtering: you can't know what's in memory before you query. Drop only obvious dross (a bare noun like "coding"). **Query by task, not by fear** — "implementing Claude Code hooks", not "common mistakes when writing hooks". The binary caps results to the top-30 matches per phrase.

## Step 2 — Run ONE unified `engram query` with all phrases

```
engram query --lazy-chunks \
  --phrase "<phrase 1>" \
  --phrase "<phrase 2>"
  # ... one --phrase per Step 1 phrase (deep: 10; glance: ~3)
```

One call; the binary merges ranking server-side. `engram query` always runs the unified clustering of the matched notes+chunks in one pass and emits `candidate_l2s: [{path, cosine, content}]` per cluster — the within-cluster top-5 notes, and nothing else; explore-sampled notes are never cluster members. Separately, the binary samples explore notes from vocab-term centroids near the query and delivers them as top-level `items[]` entries (provenance `explore`, with `source_term`), budgeted to the count of matched notes and reported in `explore_allocated` (a term → delivered-count map, always present — `{}` on missing/unreadable centroid data). Do NOT collapse phrases, do NOT run per-phrase calls.

The payload has **two channels**:

**Channel 1 — Relevance (clustered matched items):** items matched by your phrases, bounded to ~300 (top-30 per phrase, unioned, relevance-floor applied). These are clustered and carry `candidate_l2s` per cluster. Read this channel to surface applicable lessons and judge coverage. The payload's `items` mix:

- `kind: chunk` — raw transcript/doc fragments with source + anchor. These are EVIDENCE: extract the convention, decision, or correction they show; never quote them wholesale. **Under `--lazy-chunks` (the default invocation — confirm via `budget.lazy_chunks: true`) chunk items carry path + source/anchor but NO `content` field: `engram show-chunk <source#anchor>` to read a chunk's evidence on-demand.**
- `kind: fact` / `feedback` / `runbook` — crystallized lessons; apply directly (notes always carry full content inline). A `runbook` note ranks purely by situation-similarity like `fact`/`feedback`, no dedicated query flag of its own.
- `provenance: explore` — notes sampled from vocab-term centroids rather than matched by a phrase. Treat them as part of the delivered note set for judging coverage below; each carries `source_term` naming the centroid it came from.

**Channel 2 — Recent activity (un-clustered):** items tagged `provenance: recent` — the newest chunks by ingest time, appended after the matched set, NOT cluster members. Read this block first for situational continuity — re-immerse in recent work before diving into the clustered results. These items are NOT used for coverage or synthesis judgment. Do not treat them as matched results; they have no cluster membership and no `candidate_l2s`. Under `--lazy-chunks` recent items also carry path/source only.

- **Recent items are your own recent activity.** Chunks from a recent source with `turn-N` anchors are first-person `ASSISTANT:` narration you produced in a just-prior or pre-context-clear session. Treat them as your own past actions — do not re-derive them, do not express surprise at them, and dedup against what is already in your context.

If the matched items (Channel 1) are empty, say so in one sentence, skip the rest of this note's coverage-judgment section, and proceed straight to Step 3 below with your plan. (A non-empty recent-activity block alone does not count as "something surfaced" for coverage purposes.)

## Step 2.5 — Read, weigh, and judge coverage (agent-judged; read side only)

The query output's `clusters` list contains the unified clustering of matched chunks and notes. Each cluster carries `candidate_l2s: [{path, cosine, content}]` — the within-cluster top-5 notes ranked from the cluster's own matched members. Explore-sampled notes arrive as separate top-level `items[]` entries, not as cluster members — judge them alongside the clustered candidates using the same criteria. The binary's k-means clustering is ground truth — group by its clusters, not by eye. A note that did not match any phrase and was not explore-sampled never appears as a candidate. Superseded-note ride-alongs are inserted at the next rank after the note they supersede. A cluster with no note members yields an empty `candidate_l2s` list; skip to the next cluster when that happens. **Process every cluster, then the explore items.** For each:

**A. Read candidates and members**

`candidate_l2s` entries carry their `content` inline — read it directly; **no `engram show` calls for candidates**. For chunk members, the content is NOT in the payload — `engram show-chunk <source#anchor>` to read the evidence on-demand. Do not judge coverage before you have read the candidate content. **For zero-note clusters:** the absence of note candidates does NOT skip the chunk-reading requirement — call `engram show-chunk` on every chunk member before stating any relevance or coverage judgment. A metadata-only judgment (path/title alone, no fetched content) is a violation even when `candidate_l2s` is empty.

**B. Apply the recency weight to resolve conflicts**

Evidence **conflicts** when a newer member explicitly negates or reverses an older claim. Reversal cues: "no longer", "replaced by", "use X not Y", or the same subject+predicate appearing with a different object in a newer item. When conflict is present: **recent wins**. When no conflict: treat older and newer evidence as independently valid — do not demote a stable convention merely because it lacks a recent instance.

**C. Judge coverage against the recency-weighted view — criterion only (the write action for each outcome is [[2.2026-09-23.recall-write-extension]]'s job, deep only)**

| Outcome | Criterion |
| --- | --- |
| **Covered** | A candidate's claim states the cluster's principle with **no material omission** vs the recency-weighted members |
| **Near** | A candidate addresses the same situation but omits ≥ 1 substantive claim the members evidence (a candidate that only matches the superseded content is **near**, not covered) |
| **Absent** | No candidate addresses the situation |

Do not apply a cosine threshold to decide this — cosine only nominates candidates; coverage is judged from content. **Under `recall-deep`:** for every Covered/Near/Absent verdict, immediately carry out [[2.2026-09-23.recall-write-extension]]'s matching write action for that cluster before moving to the next cluster (writes are blocking and inline — a note written for one cluster may be a candidate for another). **Under `recall-glance`:** do not act on the verdict at all; it exists only to inform Step 2.7's activation and the Step 3 synthesis below — do not amend, do not learn.

## Step 2.7 — Activation (use-driven, after coverage judgment)

After processing all clusters, call `engram activate` on the notes you actually drew on — the candidates you judged Covered or Near above, and any notes you cite in the Step 3 synthesis below:

```
engram activate \
  --note "<path of note you judged Covered or Near>" \
  --note "<path of note you cited in Step 3>"
  # ... one --note per used note only
```

Do NOT activate every returned note. Do NOT activate recent-channel items (chunks are never activated). Activating only what you used lets superseded-but-surfaced notes fade via recency rank — bumping every returned note would defeat the recency-competition mechanism. Skip this call when you drew on no notes.

## Step 3 — Closing synthesis: did the memories change the plan?

Under `recall-glance`, **before synthesizing, check for a load-bearing Channel 2 (recent-activity) standard.** If your decision turns on honoring a recent convention, escalate to [[4.2026-09-23.recall-deep]] now, before synthesizing — see [[3.2026-09-23.recall-glance]]'s escalation rule.

The user sees this. Rules:

- **Open with the count.** One sentence: "Query surfaced N items (K chunks, M notes); crystallized J lessons."
- **Walk the plan from Step 0 in order.** Per numbered action: **confirmed**, **adjusted** (and how), **contradicted**, or **silent**. One short bullet each.
- **Frame load-bearing conventions as requirements.** Lead with "Apply these as requirements:" and list them — drawn from lessons and chunk evidence alike. A plan step memory confirms still inherits the convention's concrete specifics as requirements.
- **Reconcile every recommendation you produce — not just the Step-0 plan (anti-displacement).** Before stating any option, lever, or recommendation the work generated, reconcile it against the ASKED TASK and decisions already in context. Substituting a prerequisite, a better test, or a "more rigorous foundation" as the thing to do *first* FEELS like diligence but IS relitigating the settled task. Reasoning already in context is not new evidence — reconcile against prior OUTCOMES, not mere mentions. Default to the asked task; deviate only by (a) naming a genuinely NEW fact and (b) stating you are reversing direction. If a recommendation here is new (not in the Step-0 plan), STOP — complete Step 3.5 before this reply is shown to the user.
- **Surface contradictions inline** where they affect the action, as prose.
- **No payload dumps** — never re-emit YAML, paste whole chunks, or list raw scores/paths.
- **Length:** as long as honesty about the plan requires; if memory was silent on everything, one sentence.

## Step 3.5 — Re-entry: a recommendation not in the Step-0 plan

Before the Step 3 synthesis is shown to the user: if it is about to ship a recommendation, lever, or approach — named as the thing to do — that does **not** appear in the Step-0 plan (it was conceived during the work), run ONE more query first, keyed to the recommendation itself:

```
engram query --lazy-chunks \
  --phrase "<the recommendation, in its own words>" \
  --phrase "<the recommendation> rolled back rejected not worth it superseded" \
  --phrase "<the recommendation> tried measured outcome"
```

Apply Step 2.5B's recency weight to what returns. The synthesis MUST carry one `Re-entry:` line per emergent recommendation — the line is the proof the check ran, directly above the final recommendation statement:

```
Re-entry: <recommendation> — clean (top hit: <best-match basename, or "no items">)
Re-entry: <recommendation> — CLOSED (<note basename>): <one-line prior outcome> → drop
Re-entry: <recommendation> — CLOSED (<note basename>): <one-line prior outcome> → revisit because <named NEW evidence>
```

A recommendation may not ship without its Re-entry line. `CLOSED → drop` means the recommendation is withdrawn. `revisit` is valid ONLY with named NEW evidence — re-weighing the note's own tradeoffs is not new evidence. This is a new vault query, not the in-context reconcile in Step 3. If you cite a note this query surfaced, include it in Step 2.7's `engram activate` call (run it again if it already ran). Runs in both modes (a query is a read; glance keeps it). Skipping the re-entry when the trigger fires is forbidden.

