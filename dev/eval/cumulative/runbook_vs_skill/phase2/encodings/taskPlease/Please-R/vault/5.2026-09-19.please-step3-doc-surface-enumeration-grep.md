---
type: runbook
tier: L2
situation: enumerating every doc surface by grep before writing the doc-scrub task when a planned change alters a repeated invariant (payload shape, sweep cadence, command set, count, naming convention) in a please plan
done_when: 'the grep was RUN over the repo (the term itself, its synonyms, its hyphenated or compound forms, and the OLD text echoes in diagram labels and comments), and the resulting per-file disposition list (file to keep / update / rewrite / N/A, each with a one-line reason) is pasted INTO the plan itself'
red_flags:
    - Skipping the grep because the surface looks small or obvious -- its cost scales with the surface, so a small surface is a cheap grep, never an exemption
    - Searching only the literal keyword -- also search synonyms, hyphenated or compound forms, and the OLD text echoes in diagram labels and comments
    - Listing dispositions from memory or leaving the list out of the plan -- the per-file list must be pasted into the plan text, because gate A's docs/diagrams-alignment reviewer verifies it and runs its own independent discovery pass
luhmann: "5"
created: "2026-09-19"
source: 'session 2026-09-19, context: please skill (agent-instructions/skills/please/SKILL.md) converted to runbook set for the phase-2 eval, sub-runbook 3 of 3'
repo: ssh://git@github.com/toejough/engram.git
user: toejough@gmail.com
vault: personal
---

# Please sub-runbook — Step 3 doc-surface enumeration grep (non-waivable)

When a planned change alters a *repeated invariant* — a payload shape, a sweep cadence, a command set, a count, or a naming convention echoed across docs, diagrams, or skills — RUN this grep over the repo before writing the doc-scrub task: search the term itself, its synonyms, its hyphenated or compound forms, AND the OLD text's echoes in diagram labels and comments. Paste the resulting per-file disposition list (file → keep / update / rewrite / N/A, with the one-line reason) INTO the plan itself. The grep runs regardless of how large the surface looks — its cost scales with the surface, so a small surface is a cheap grep, never an exemption. Gate A's docs/diagrams-alignment reviewer independently verifies this list (see the adversarial-review-gates sub-runbook).
