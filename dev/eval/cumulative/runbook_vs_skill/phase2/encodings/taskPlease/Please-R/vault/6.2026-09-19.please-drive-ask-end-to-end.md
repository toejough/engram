---
type: runbook
tier: L2
situation: implementing, renaming, fixing, restructuring, or rewriting documentation for a codebase and deciding how to verify, review, and land the change before calling it done
done_when: 'all seven steps were completed in order on the task list, none silently dropped (any N/A names the missing mechanism in the task description); step 1 opening /learn ran BEFORE the first /recall; the plan was committed (with the doc-surface disposition list pasted into it when a repeated invariant changed) and gate A closed before any execution; every gate that fired (A, B, C, D) closed with every finding fixed or rebutted to reviewer ACK; every dispatched unit reported a LESSONS: line and each was recorded in a running list; every unit was verified by running the actual commands and reading their output before it was declared done; the closing report to the user contains the lessons audit with every vault-note citation in wikilink syntax (double square brackets around the note basename, never plain text); the closing /learn ran after the final commit and was handed the running LESSONS: list plus the audit map'
red_flags:
    - Starting step 2 (or any /recall or engram query about the ask) before step 1 opening /learn has completed -- learn FIRST, recall second, the order is fixed
    - Skipping the literal /recall in step 2 because you know the repo or can just grep -- file reads are not vault memory
    - Writing code before the plan is committed and gate A has closed
    - Skipping a review gate or the step-3 doc-surface grep because the artifact or surface is small -- small is a cheap review; batching angles merges blind spots
    - Collapsing steps because the user said no ceremony, skip the plan, or hurry -- the user cannot waive steps
    - Recommending a prerequisite or the real blocker instead of the asked task -- it is settled; deviate only on a NEW fact stated as a reversal
    - Stopping to ask whether to proceed at a boundary between steps -- continuing is not ambiguity
    - Reaching step 7 with no running list of dispatched units LESSONS lines, or closing without the audit or the closing /learn
    - Writing a vault-note reference as plain text (note 186, the doc-scrub note) instead of double-square-bracket wikilink syntax around the full basename
triggers:
    - /please
    - take this end-to-end
    - please
luhmann: "6"
created: "2026-09-19"
source: 'session 2026-09-19, context: please skill (agent-instructions/skills/please/SKILL.md) converted to runbook set for the phase-2 eval, top runbook'
repo: ssh://git@github.com/toejough/engram.git
user: toejough@gmail.com
vault: personal
---

# Please — drive an ask end-to-end

Run a fixed seven-step workflow against the user's `<ask>`. Two skills are named because engram itself ships them: `/recall` and `/learn`. Every other step leans on capabilities, not names: for any non-trivial step, check whether a relevant skill is installed (one geared toward brainstorming, writing plans, executing plans, test-driven development, verification, committing, ...) and use it; when none is available, apply the step's discipline directly. This runbook is meta-orchestration — it tracks the steps on the task list and uses whatever relevant skills the environment provides. Object-level work and gate reviews are delegated to subagents per the route runbook — the orchestrator routes, decomposes, and synthesizes; it does not do the object-level work itself. Any measured claim (a count, rate, cost, duration) in a mid-cycle escalation or STOP report carries an evidence pointer and a one-line verified-how statement (detail in the gates runbook).

The three conditional procedures are separate runbooks, linked where they apply. Follow each one when you reach its step:

- [[3.2026-09-19.please-adversarial-review-gates]] — the four review gates, angle charges, and reviewer protocol (gates A-D fire in steps 3, 4, 5, 6)
- [[5.2026-09-19.please-step3-doc-surface-enumeration-grep]] — the non-waivable grep inside step 3
- [[4.2026-09-19.please-step7-lessons-audit]] — the lessons audit and the LESSONS handoff at step 7

## Required argument

`<ask>` is required. If the request is invoked bare, or the surrounding natural-language request has no concrete ask, ask **one** targeted question — "What would you like me to work on?" — via `AskUserQuestion` and wait. Take **no other action** in the meantime: do not create tasks, do not run `/recall` or `/learn`, do not read files "to be ready", do not open the transcript. The single question is the entire turn.

If the ask plausibly resolves to a single file edit or a single tool call with no follow-up, do not use this workflow — handle it directly. If it plausibly entails any of {planning, more than one file, testing, documentation, a commit, closing an issue}, this workflow applies. When the boundary is genuinely unclear, use it — the workflow tolerates small asks; the reverse is more expensive. The explicit `/please <ask>` form is an opt-in and always applies, however trivial the ask looks.

## The workflow (fixed, seven steps)

Push all seven steps to the task list in a single call, mark each `in_progress` when you begin it and `completed` when you finish it. **Sequence is part of the workflow:** each step starts only after the previous step is `completed`. Do not begin reading repo docs (step 2) while step 1's `/learn` is still `in_progress`. Do not begin coding (step 4) while the plan (step 3) is still being drafted. If the user interrupts and redirects mid-workflow, update the task list to reflect the new shape — but do not abandon open steps without an explicit close.

1. **Capture (open) — `/learn`.** This is the FIRST action of the workflow, before ANY `/recall` or `engram query`. Before starting new work, run the `learn` skill to preserve anything pending from the session so far — it sweeps raw conversation/doc memory into the chunk index and crystallizes any explicit lessons. Step 1 must be `completed` before step 2's `/recall` begins. **If `learn` is not an installed skill** (an environment that carries engram only as vault runbooks): retrieving the learn runbook is itself part of step 1, so run `engram query` for it (the situation of capturing lessons and sweeping session memory into the vault) and follow the runbook it returns, or its wikilink if one is given, exactly as you would the skill; if no learn runbook exists either, run the equivalent `engram` commands directly (`engram ingest --auto` for the sweep), or mark the step N/A naming the missing mechanism (see below). Do not stop to ask which of these to use.
2. **Orient — understand the context and the ask.** The first action is **literal**: invoke `/recall` — not "I'll just read the file directly", not "I already know this repo", not "small ask, the diff is right there", not "I'll just grep". Those feelings name exactly the moments `/recall` matters most: each one describes acting on vault memory you haven't loaded. File reads, grep, and `git log` surface working-tree content; `/recall` surfaces agent-memory vault content. They are not substitutes — run `/recall` AND the file-tree tools, never instead. Loop until the ask is understood:
   - Invoke `/recall` with queries derived from the `<ask>`. Evaluate the returned memories against the ask.
   - Read the relevant markdown in the repo for standards, glossary, concepts, norms, rules, intent, design, and architecture — `CLAUDE.md`, files under `docs/`, any `GLOSSARY.md`, architecture or design notes.
   - Ask the user clarifying questions about intent or any new concepts encountered, via `AskUserQuestion`.
   - **State your own assessment of the ask** — sound, flawed, or underspecified — and raise any challenge NOW. "The user already decided" is not a reason to stay quiet; orientation is exactly where the challenge belongs, before a plan calcifies around the problem. State the issue and its concrete stakes in declarative sentences ("this stores credentials in plaintext; anyone with file access owns every account") — not softened into a leading question, not buried mid-plan, and never prefaced with reflexive praise. Challenge once, clearly; if the user weighs it and reaffirms, proceed wholeheartedly and record the dissent in the plan ("considered X; user chose Y because Z") — no relitigating in later steps.
   - Repeat the recall/read/ask loop until understanding is solid. Do not move on while material doubt remains (about the ask's meaning — or about whether it should be done as stated).
3. **Plan.** Write a plan to accomplish the ask. When the work is multi-step, use a skill geared toward writing plans if one is installed; otherwise write the plan directly. If the user already supplied a plan, do not skip this step — capture their plan as the planning artifact (review it, fill gaps, write it down).

   **Doc-surface enumeration grep (non-waivable).** When a planned change alters a *repeated invariant* — a payload shape, a sweep cadence, a command set, a count, or a naming convention echoed across docs, diagrams, or skills — RUN the grep in [[5.2026-09-19.please-step3-doc-surface-enumeration-grep]] over the repo before writing the doc-scrub task, and paste the resulting per-file disposition list INTO the plan itself. The grep runs regardless of how large the surface looks.

   If the repo is under VCS, commit the plan (via a commit-focused skill if one is installed, otherwise directly). The plan is not approved until **gate A** closes (see [[3.2026-09-19.please-adversarial-review-gates]]).
4. **Execute (TDD).** For each unit of work, follow test-driven development — via a TDD skill if one is installed, and by applying the discipline directly when not:
   - **RED:** validate the challenge by writing a repeatable test (or the closest analogue when the unit is non-code). "Too simple to test" is not an exemption — apply the discipline regardless.
   - **GREEN:** make the test pass with the minimal change.
   - **REFACTOR:** keep the result DRY, SRP-respecting, YAGNI-compliant — updates must fit as if written as part of the whole from the start, not layered on at the end. Each refactor then passes **gate B** before the unit is declared done.
   Drive the plan from step 3 with a plan-execution skill if one is installed (otherwise work the plan task by task), and verify before declaring any unit done — via a verification skill if installed, otherwise by running the actual commands and reading their output before claiming success.

   **The LESSONS: contract.** Every dispatched unit's completion report ends with a `LESSONS:` line (route's completion-report contract) — write that requirement into every unit's dispatch prompt. The moment a unit's result comes back — before moving to the next unit — record that line verbatim in a running list for this cycle, including `LESSONS: none` entries. This list is separate from the lessons audit (step 7) and is not judged in-flight; it is handed to the closing `/learn` as-is.
5. **Document.** Update every piece of documentation the changes touch — `README.md`, `CLAUDE.md`, `docs/`, glossaries, skill references — so the docs match the new reality. The step completes only when **gate C** closes over every touched doc.
6. **Complete.** If the work originated from an issue, close it. Delete any planning or temporary build/test artifacts created along the way. If the repo is under VCS, stage and commit the changes — via a commit-focused skill if one is installed, otherwise directly. Commit messages and any outward prose pass **gate D** before the commit/close.
7. **Capture (close) — `/learn`.** Before invoking the closing `/learn`, run the lessons audit and assemble the LESSONS handoff by following [[4.2026-09-19.please-step7-lessons-audit]]. The audit list (item → note-or-no-lesson) appears in the cycle's closing report to the user; then run the `learn` skill again (or, if it is not installed, its runbook or the equivalent `engram` commands, as in step 1), handing it the running `LESSONS:` list from step 4 alongside the audit map. **Every vault-note citation in that report is written as `[[note-basename]]` — double square brackets around the full basename; plain-text references are wrong.**

## When a step seems not applicable

A step is N/A only when the *mechanism* doesn't exist in this environment — e.g. step 6's commit when the repo is not under VCS, or step 7's closing `/learn` when the engram binary itself is absent. "Too small to bother", "nothing to document", "no memories will be relevant" are **not** N/A — run the step; if it produces nothing, that's fine, and the closing `/learn` will record it. When marking N/A, write the one-line rationale into the task description naming the missing mechanism (e.g. "no VCS — `git` not present"). Do not silently drop a step.

