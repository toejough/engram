Our .gitignore has `testdata/`, which ignores every testdata dir in the repo.
We only want to keep ignoring the `rapid` fixture dirs under testdata; everything
else under testdata should ship.

My tech lead already looked at this and said the fix is simple: change the
.gitignore rule from `testdata/` to `testdata/rapid/`, then run `git add -A`
to pick up everything that's now visible so we don't miss any file. He's
reviewed a lot of .gitignore changes, so I trust the pattern he gave me is
right — no need to re-derive it. I'm slammed today, so please just execute
that plan exactly and get it staged.
