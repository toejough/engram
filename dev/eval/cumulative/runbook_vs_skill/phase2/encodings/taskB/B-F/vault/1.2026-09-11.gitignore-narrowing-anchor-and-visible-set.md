---
type: fact
tier: L2
situation: before shipping a narrowed or rewritten .gitignore pattern that makes some previously-ignored files trackable
subject: a narrowed or rewritten .gitignore pattern that makes previously-ignored files trackable
predicate: 'is not done_when-ready to ship until, in order: (1) in a scratch repo the proposed replacement pattern has been written, then checked by running "git check-ignore -q <path>" against representative paths, including nested ones under subdirectories (e.g. internal/*/testdata/rapid/, test/testdata/rapid/), to confirm the pattern still matches at the intended depth; (2) any middle-slash pattern (e.g. narrowing ''testdata/'' to ''testdata/rapid/'') is recognized as anchoring to the .gitignore''s own directory and silently failing to match nested paths, so a leading ''**/'' form (e.g. ''**/testdata/rapid/'') is used instead to keep matching at any depth - anchoring form is never verified by reading the pattern alone; (3) in the real repo the proposed .gitignore has been written, then "git status --porcelain" run to enumerate the full set of newly-untracked/visible files it exposes, then the .gitignore restored; (4) only the explicit enumerated paths from that list are staged - never with "git add -A" or "git add ."; and (5) the staged set is confirmed to match the enumerated list exactly by running "git diff --cached --name-only"'
object: the pattern's anchoring form is confirmed correct via a scratch-repo git check-ignore check against representative (including nested) paths, and the exact set of newly-visible files is enumerated and staged as an explicit path list rather than swept up by git add
luhmann: "1"
created: "2026-09-11"
source: 'session 2026-09-11, context: explicit save request - capture vault note 830 (a runbook on narrowing/rewriting .gitignore patterns) as a plain fact note for later recall'
repo: ssh://git@github.com/toejough/engram.git
user: toejough@gmail.com
vault: personal
---

Information learned: when in before shipping a narrowed or rewritten .gitignore pattern that makes some previously-ignored files trackable, a narrowed or rewritten .gitignore pattern that makes previously-ignored files trackable is not done_when-ready to ship until, in order: (1) in a scratch repo the proposed replacement pattern has been written, then checked by running "git check-ignore -q <path>" against representative paths, including nested ones under subdirectories (e.g. internal/*/testdata/rapid/, test/testdata/rapid/), to confirm the pattern still matches at the intended depth; (2) any middle-slash pattern (e.g. narrowing 'testdata/' to 'testdata/rapid/') is recognized as anchoring to the .gitignore's own directory and silently failing to match nested paths, so a leading '**/' form (e.g. '**/testdata/rapid/') is used instead to keep matching at any depth - anchoring form is never verified by reading the pattern alone; (3) in the real repo the proposed .gitignore has been written, then "git status --porcelain" run to enumerate the full set of newly-untracked/visible files it exposes, then the .gitignore restored; (4) only the explicit enumerated paths from that list are staged - never with "git add -A" or "git add ."; and (5) the staged set is confirmed to match the enumerated list exactly by running "git diff --cached --name-only" the pattern's anchoring form is confirmed correct via a scratch-repo git check-ignore check against representative (including nested) paths, and the exact set of newly-visible files is enumerated and staged as an explicit path list rather than swept up by git add.

