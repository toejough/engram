---
type: runbook
tier: L2
situation: producing a Luhmann-reparenting answers file after being handed a derive-phase candidate JSON payload from `engram update --reparent-luhmann` -- a standalone, explicitly-invoked batch mode, not part of a normal learn pass
done_when: 'every candidate''s note in the payload has exactly one entry in the answers file, with position judged from note_excerpt/target_excerpt content rather than the similarity score alone; the file matches the required JSON shape with the fingerprint echoed verbatim; it was saved to a file; and the user was told to run the --dry-run apply command first, then the same command without --dry-run to apply'
red_flags:
    - You skipped a candidate in the reparent batch instead of giving it exactly one answer entry -- there is no "skip this one" option
    - You judged position from the similarity score alone instead of reading note_excerpt/target_excerpt -- a high score can still be an unrelated false positive sharing vocabulary
    - You invoked `engram update --reparent-luhmann` yourself instead of leaving that to the user's (or the acting agent's) next step -- this runbook only produces the answers file
    - You renamed or added fields in the answers file JSON instead of matching the required shape exactly
luhmann: "1056"
created: "2026-09-23"
source: 'session 2026-09-23, context: learn skill (agent-instructions/skills/learn/SKILL.md) converted to runbook set for the phase-2 eval (openspec change learn-skill-to-runbook), sub-runbook 3 of 3 (batch mode --reparent-luhmann)'
repo: ssh://git@github.com/toejough/engram.git
user: toejough@gmail.com
vault: personal
---

# Learn sub-runbook — Batch mode: Luhmann re-eval answers (`--reparent-luhmann`)

**Trigger:** you were handed a derive-phase candidate JSON payload from `engram update --reparent-luhmann` (fields: `candidates` — each with `note`, `note_excerpt`, `target`, `target_excerpt`, `similarity` — plus `instruction` and `fingerprint`), and asked to produce its answers file. This is a deliberate, explicitly-invoked mode — NOT part of the normal Step 1→2.5 sequence a per-capture learn pass walks; it runs standalone, once, over a batch.

For EACH candidate pair, apply the **same disposition test as the top runbook's Step 2 placement decision** — read `note_excerpt` and `target_excerpt` and judge the content relationship, not the `similarity` score alone (a high score can still be an unrelated false positive: two notes can share vocabulary while addressing different problems):

1. `note_excerpt` develops one specific sub-point of `target_excerpt`? → `position=continuation`.
2. Else, same overall thought as `target_excerpt`, at the same level? → `position=sibling`.
3. Else (unrelated despite the similarity score) → `position=top`.

In all three cases, `target` is only meaningful for `continuation`/`sibling`; a `top` verdict still needs a full answer entry.

**Coverage requirement:** every candidate's `note` MUST get exactly one entry in the answers file — there is no "skip this one" option. If a note appears as `note` in more than one candidate (multiple targets above the floor), judge each candidate independently and answer with the ONE `target`/`position` you find most correct; still exactly one output entry per distinct `note`.

Write the answers file as JSON, matching this shape exactly — do not rename fields or add others:

```json
{
  "reparenting": [
    {"note": "<id>", "position": "continuation|sibling|top", "target": "<id>"}
  ],
  "fingerprint": "<echoed VERBATIM from the input payload's fingerprint field>"
}
```

Save it to a file (e.g. `/tmp/reparent-answers.json`) and tell the user to run:

```
engram update --reparent-luhmann --answers <file> --dry-run
```

to review the resulting renames, then re-run the same command without `--dry-run` to apply. This runbook does not invoke either command itself — that is the acting agent's (or user's) next step.
