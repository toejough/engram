---
type: fact
tier: L2
situation: committing changes in a git (non-jj) repo and need the commit skill exact step order and formatting/safety rules
subject: the commit skill process
predicate: requires, in order
object: '(1) check for a .jj directory first — if jj repo, use jj commands not git; (2) check state with git status, git diff --staged, git diff — if nothing to commit, report and stop; note what is staged vs unstaged; (3) review recent commits for style with git log --oneline -5; (4) stage changes — prefer specific file paths over git add -A, do not stage unrelated files; (5) compose the message in the format <type>(scope): <description> followed by a blank line then a body explaining why the commit was made — motivation, problem solved, how it fits the bigger picture, what decisions were made, lessons learned for future developers, not just a summary of what was done — followed by a blank line then the trailer AI-Used: [claude]; common types are feat, fix, refactor, test, docs, chore; TDD phase message formats are: TDD Red = test(scope): add tests for <feature>, TDD Green = feat(scope): implement <feature>, TDD Refactor = refactor(scope): <cleanup>; (6) commit by passing the composed message (including the trailing AI-Used: [claude] line) as the -m argument to git commit, supplied via a quoted here-document so the blank lines and wrapping survive intact; (7) verify with git log -1 and git status. Rules that apply throughout: the AI-Used trailer is exactly AI-Used: [claude], NOT Co-Authored-By; never amend pushed commits — check git status for the ahead-of indicator first; separate concerns — do not mix functional changes with lint/style fixes; first line under 72 chars, body wrapped at 72 chars; stage specific files — do not use git add -A or git add .; never use dangerous commands — no git checkout with -- . as target, no git restore ., no git reset --hard.'
luhmann: "1"
created: "2026-09-11"
source: 'session 2026-09-11, context: Joe asked to save the git-commit skill procedure (/Users/joe/repos/personal/engram/.claude/skills/commit.md) to the vault for later recall'
repo: ssh://git@github.com/toejough/engram.git
user: toejough@gmail.com
vault: personal
---

Information learned: when in committing changes in a git (non-jj) repo and need the commit skill exact step order and formatting/safety rules, the commit skill process requires, in order (1) check for a .jj directory first — if jj repo, use jj commands not git; (2) check state with git status, git diff --staged, git diff — if nothing to commit, report and stop; note what is staged vs unstaged; (3) review recent commits for style with git log --oneline -5; (4) stage changes — prefer specific file paths over git add -A, do not stage unrelated files; (5) compose the message in the format <type>(scope): <description> followed by a blank line then a body explaining why the commit was made — motivation, problem solved, how it fits the bigger picture, what decisions were made, lessons learned for future developers, not just a summary of what was done — followed by a blank line then the trailer AI-Used: [claude]; common types are feat, fix, refactor, test, docs, chore; TDD phase message formats are: TDD Red = test(scope): add tests for <feature>, TDD Green = feat(scope): implement <feature>, TDD Refactor = refactor(scope): <cleanup>; (6) commit by passing the composed message (including the trailing AI-Used: [claude] line) as the -m argument to git commit, supplied via a quoted here-document so the blank lines and wrapping survive intact; (7) verify with git log -1 and git status. Rules that apply throughout: the AI-Used trailer is exactly AI-Used: [claude], NOT Co-Authored-By; never amend pushed commits — check git status for the ahead-of indicator first; separate concerns — do not mix functional changes with lint/style fixes; first line under 72 chars, body wrapped at 72 chars; stage specific files — do not use git add -A or git add .; never use dangerous commands — no git checkout with -- . as target, no git restore ., no git reset --hard..

