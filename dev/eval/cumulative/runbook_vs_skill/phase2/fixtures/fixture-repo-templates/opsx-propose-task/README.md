# Widget Service

A small internal HTTP service that manages widget inventory records for other
internal teams.

## Layout

- `widget_api/` — request handlers
- `openspec/` — behavior specs and change proposals
