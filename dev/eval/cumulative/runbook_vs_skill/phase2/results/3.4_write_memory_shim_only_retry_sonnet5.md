# write-memory task 3.4: shim-only / R-arm RETRY run, model sonnet5, 2026-09-22

**D8 bar NOT MET — and the wording fix was never actually tested.** Root cause found and confirmed
by direct file diff (below): the reworded call-site text (task 3.4's fix) was applied only to the
frozen fixture copy at `phase2/encodings/taskWriteMemory/recall-learn/{recall,learn}/SKILL.md`
(correctly deployed into each trial repo's `.claude/skills/`, confirmed matching, mtime 2026-09-22).
It was **not** applied to the live canonical skill source at `agent-instructions/skills/{recall,learn}/SKILL.md`,
which `probe.py`'s `build_cfg_template()` separately copies into the isolated CLI config pool used
for every trial (`cfg-N/skills/`). All three trials' `cfg-N/skills/learn/SKILL.md` are byte-identical
(md5 `eee3443e...`) to the **unfixed** `agent-instructions/skills/learn/SKILL.md` still on disk today,
and still read `**REQUIRED SUB-SKILL:** invoke the **write-memory** skill` — the exact pre-fix wording
— not the `**REQUIRED NEXT ACTION:** run \`engram show 6a...\`` text task 3.4 was supposed to test.
Transcript behavior confirms the model acted on the stale copy: all three trials still called
`Skill{skill: "write-memory"}` (the pre-fix wording's literal instruction), not `engram show`.

**So: 0/3 fetched the runbook before composing, same as the original 3.2 run — but this run does not
tell us whether the wording fix works, because the fix was never in front of the model.**

## Per-trial results

| Trial | Runbook fetched before compose? | followed_k/10 | end_state | Tag flag (both writes) | cost | turns |
|---|---|---|---|---|---|---|
| R#0 | **No** — 0 `engram show` calls. `Skill{write-memory}` tried **twice** (draft/warp note, then breaker/preheat note), both `Unknown skill: write-memory` errors; self-composed via `--help`/trial-and-error (briefly guessed `--tags`, self-corrected to `--tag`) | 8/10 | **PASS** | `--tag "component/cooling"` / `--tag "component/electrical"` (correct, quoted) | $0.55 | 25 |
| R#1 | **No** — 0 `engram show` calls. `Skill{write-memory}` tried **once**, same error; self-composed the same way (also briefly guessed `--tags`, self-corrected) | 8/10 | **FAIL** — wrote 2 extra, unrequested `qa` notes alongside the 2 required writes (vault ids `[qa qa 1 2 3 4 5 6 6b 7]`, expected `[1..8]`) | `--tag "component/cooling"` / `--tag "component/electrical"` (correct, quoted) | $0.57 | 26 |
| R#2 | **No** (see nuance below) — 0 `engram show` calls. `Skill{write-memory}` tried **once**, same error; composed and **executed two writes with no `--tag` at all**, then manually hand-edited the written notes' frontmatter to add tags (a red-flagged anti-pattern the runbook explicitly warns against) | 10/10 | **FAIL** — duplicate/malformed note id set (`[1 2 3 4 5 6 6 7]`, expected `[1..8]`) from the discard-and-redo | `--tag component/cooling` / `--tag component/electrical` (correct, unquoted) — **only after discarding and redoing**, see below | $1.12 | 45 |

**R#2 nuance**: after the flawed hand-edit-frontmatter writes, an unrelated `engram check` run (the
agent verifying vault health) surfaced `WARN G3 dangling: 6a.2026-09-22.write-memory-compose-execute-verify → [[basename]]`.
The agent then ran `cat` directly on the runbook's `.md` file in the vault directory (filesystem
`cat`, not `engram show`) to inspect the dangling link's target, read the runbook's compose-block
body, said *"The runbook note ... reveals the correct mechanism: `--tag <family>/<value>`"*,
discarded the two flawed notes (`engram amend --discard`), and recomposed both writes correctly with
`--tag`. This is genuine evidence the model can act on runbook content once it stumbles onto it —
but the discovery came from an incidental linter warning, not from the wikilink cue, was **not**
`engram show`, and happened **after** the first (flawed) compose attempt, not before either required
write. It does not satisfy the design D4 gate either literally or in spirit.

Steps 4/8 (the `--tag component/...` regex checks) fail for R#0/R#1 due to the same
already-flagged, out-of-scope scoring-harness quoting artifact from the 3.2 report (the regex has no
tolerance for a quoted `--tag` value) — not a real composition defect. Corrected, R#0/R#1 would be
10/10; this does not change any verdict below.

## Comparison to the skill row and the original 3.2 run

| Row | n | followed_all k/n | end_state k/n | Runbook fetched in all passing trials? | $ for arm |
|---|---|---|---|---|---|
| Skill (S), from 1.4 | 3 | 3/3 | 3/3 | n/a (no runbook to fetch) | 0.78 |
| Runbook (R), 3.2 original | 3 | 1/3 (2/3 corrected) | 2/3 | **No — 0/2 passing trials** | 1.49 |
| Runbook (R), 3.4 retry | 3 | 1/3 (3/3 corrected) | 1/3 | **No — 0/1 passing trial** | 2.24 |

**Did the wording fix change fetch behavior at all — 0/3 before, ?/3 now: still 0/3.** No change,
and for the reason above, this run doesn't actually speak to the fix's effectiveness. If anything,
the raw `Skill{write-memory}` mistake count went **up**, not down (3.2: R0×2, R1×1, R2×0 = 3 total;
3.4: R0×2, R1×1, R2×1 = 4 total) — expected, since R#2 in this run had the same unfixed text as the
other two trials and the original R#2 (which happened not to try it).

## D8 verdict: NOT MET

- Validity gate (design D4): **FAILS.** 0/3 trials show `engram show <basename>` before composing
  either required write. The one trial that reached `end_state: PASS` (R#0) did so via the same
  guess-error-retry escape hatch as 3.2, with zero runbook contact.
- Composition/end_state axis is also **not** within one trial of the skill row this time:
  `end_state` 1/3 vs skill's 3/3 (a 2-trial gap, worse than 3.2's 2/3), `followed_all` 1/3 raw (3/3
  corrected) vs skill's 3/3.
- **Overall: bar not met.** But the decisive issue is procedural, not a finding about the runbook or
  the wording fix: this run tested the pre-fix skill text by accident.

## What's needed next (for Joe, no further spend attempted here)

1. **Fix the actual bug**: apply the same reworded call-site text to
   `agent-instructions/skills/{recall,learn}/SKILL.md` (the live canonical source), not just the
   frozen `encodings/taskWriteMemory/recall-learn/` fixture copy. Confirm with a content diff (not
   just eyeballing) that `cfg-N/skills/learn/SKILL.md` in a fresh `--setup-only` run actually carries
   the new wording before spending on another full n=3 retry.
2. Once confirmed, task 3.4 is still open for a genuine test of the wording fix — this run does not
   count as that test, even though it consumed the n=3 budget.
3. Separately, R#1's extra unrequested `qa` notes and R#2's discard/redo id churn are real
   composition issues worth a look if/when the fetch problem is solved — not blocking, and not
   iterated on here per task scope (run + record only).

## Cost

$2.24 total for n=3 (R#0 $0.55 + R#1 $0.57 + R#2 $1.12).

Kept trial directories (transcripts + `vault-final/`) preserved under
`/private/tmp/claude-501/-Users-joe-repos-personal-engram/23a08637-d9a1-431d-9ac8-7770901edb97/scratchpad/runbook_vs_skill_runs/p2/write-memory-sonnet5-1790107424-5c5748/`
(ephemeral scratch — not part of the repo; raw results are
`3.4_write_memory_shim_only_retry_sonnet5.jsonl`/`.log` in this directory).
