# 3.3c please shim-only R arm after the shim fix, sonnet5, 2026-09-20

Config: `probe_phase2.py --task please --arms R --shim-only --model sonnet5 --keep`, current shim.md (with the phrase-two re-check), fixture situation = P2j (hybrid, see 3.1 "Process-shaped situation"):
"landing a change end-to-end in a repository that touches code, tests, and documentation and deciding how to verify, review, and commit it before calling it done, such as a breaking rename of a CLI flag, a field rename across docs, a bug fix with tests and docs, or a feature that needs tests and docs".
Raw: `3.3c_shimonly_after_shim_fix_sonnet5_please.jsonl` (n=3). Earlier single runs: `3.3c_shimonly_after_shim_fix_n1_sonnet5_please.jsonl` (situation P2h, process-only wording: runbook NOT retrieved, 0/19, $0.24; that run motivated switching to P2j) and `..._n1b_...` (P2j, retrieved, 1/19, stopped at step 1, $0.25).

## Results (n=3, P2j)

| Arm | n | runbook retrieved | followed_all k/n | end_state k/n | steps hit /19 | $ |
|---|---|---|---|---|---|---|
| Skill (baseline 1.3) | 3 | n/a | 0/3 | 2/3 | 1 / 4 / 6 | 4.11 |
| Bare (baseline 1.3) | 3 | n/a | 0/3 | 0/3 | 4 / 4 / 4 | 0.77 |
| Shim-only R before shim fix (3.3) | 3 | 0/3 | 0/3 | 0/3 | 0 / 0 / 0 | 0.89 |
| Shim-only R after shim fix + P2j (this run) | 3 | 2/3 | 1/3 | 1/3 | 0 / 0 / 19 | 2.70 |

| Trial | retrieved | first-query phrase two (verbatim) | followed | end_state | what happened | $ |
|---|---|---|---|---|---|---|
| R-0 | no | renaming a CLI flag in a codebase and deciding how to implement, verify, and land the change | 0/19 | FAIL | phrase two still named the flag; runbook not in results; agent behaved like bare | 0.25 |
| R-1 | yes | Making a breaking rename of an existing CLI flag in a tool and carrying the change through implementation, verification, and landing it in the repository. | 0/19 | FAIL | read the runbook and its sub-runbooks, restated the plan, then STOPPED before any change: step 1 says run /learn, no learn skill exists in this no-skills config, so it asked | 0.19 |
| R-2 | yes | making a breaking rename to a CLI flag name and landing the change properly in a repo (plus a second 3-phrase query) | 19/19 | PASS | followed all steps, end-state verified | 2.26 |

## Validity (3.2 gate)
Marker seen 3/3; no api_error/rate_limit/timeout; no please skill in the cfg pool and no `Skill{please}` call (the only Skill calls were R-1's attempts at `learn`); no other guidance copy shadowing (0/3 transcripts contain the global CLAUDE.md text; the shim including the re-check appears in all 3). Treatment loaded (top runbook surfaced via engram query and read): 2/3. R-0 is a non-delivery (behaves like bare), reported, not pooled as evidence about the runbook's content.

## D8 verdict
Bar (within one trial of the skill row; effective bar end_state >= 1/3, followed_all >= 0/3): end_state 1/3, followed_all 1/3 -> MET on the letter, on 3 trials with treatment delivered in only 2. Fragile: it rests on one passing trial. Two findings:
1. Retrieval is now working but unreliable: 2/3 (was 0/3). Phrase two is still sometimes deliverable-shaped (shim fix is 3/6 on this prompt wording, see 3a3 doc).
2. R-1 exposed a fixture defect independent of retrieval: the top runbook's step 1 says to run /learn, but in the shim-only config learn exists only as a vault runbook, not a skill, so the agent stopped and asked. The body should say how to open /learn when it is a runbook.
