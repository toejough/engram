# 3.3d please shim-only R arm, situation P2h + step-1 runbook fix, sonnet5, 2026-09-20

Config: `probe_phase2.py --task please --arms R --shim-only --model sonnet5 --n 3 --keep`. Fixture situation = P2h (process-only, "implementing, renaming, fixing, restructuring, or rewriting documentation for a codebase and deciding how to verify, review, and land the change before calling it done"). Top runbook body also carries the W9 fix (step 1 and closing /learn say how to proceed when learn is a vault runbook, not an installed skill; see the fidelity report). Raw: `3.3d_shimonly_p2h_sonnet5_please.jsonl` (+ `.log`); found-corrected rescore `3.3d_shimonly_p2h_sonnet5_please.rescored.jsonl`.

## Trials

| Trial | runbook found | phrase two the agent wrote (verbatim) | followed (live) | end_state | where it stopped | $ |
|---|---|---|---|---|---|---|
| R-0 | yes (1st query) | performing a breaking rename of a command-line interface option and deciding how much verification, testing, and landing process it needs before it is called done | 19/19 | PASS | did not stop; ran a second `engram query` for the learn runbook, followed it (`engram ingest --auto`), then all steps | 1.65 |
| R-1 | NO | performing a breaking rename of a CLI flag and landing the change end-to-end in a repo | 0/19 | FAIL (no plan file committed) | never stopped; runbook not in results, agent renamed and committed directly (bare-like) | 0.27 |
| R-2 | yes (see harness note) | making a breaking rename to an existing CLI flag and deciding how to verify and land the change | 19/19 | PASS | did not stop; queried for the learn runbook and followed it, then all steps | 2.07 |

Phrase one in every trial was the ask restated ("rename the tally CLI's --out flag to --output ..."). R-1's phrase two named the flag and the deliverable, so P2h missed it (the known caveat).

| Arm | n | runbook found | followed_all k/n | end_state k/n | steps hit /19 | $ |
|---|---|---|---|---|---|---|
| Skill (baseline 1.3) | 3 | n/a | 0/3 | 2/3 | 4 / 1 / 6 | 4.11 |
| Shim-only R, P2j (3.3c) | 3 | 2/3 | 1/3 | 1/3 | 0 / 0 / 19 | 2.70 |
| Shim-only R, P2h + step-1 fix (this run) | 3 | 2/3 | 2/3 | 2/3 | 19 / 0 / 19 | 3.99 |

## Validity
Marker seen 3/3; no api_error/timeouts; no please skill in the cfg pool and no Skill tool_use in any transcript; global CLAUDE.md text absent from all 3 transcripts (no shadowing); shim present (engram query used first in 3/3). No degenerate builds (R-1 is a full short rename-and-commit, a non-delivery, not a phantom run). Treatment delivered 2/3; R-1 is a non-delivery, reported, not pooled as evidence about the runbook's content.

## Harness notes
1. Bug (fixed minimally): the live `found` scorer only read the direct `engram query` result. When the result is large it is persisted to a tool-results file and the tool_result is a 2KB preview, so R-2, which surfaced the runbook and read it back with grep on that file, scored found=False despite plainly following the runbook. `score_found_phase2` now also counts a Bash read of a `tool-results` file that contains the carrier basename (test added; 354 tests pass). Record corrected via `--rescore` (found 2/3).
2. `--rescore` under-counts steps (R-0 11/19, R-2 10/19; it cannot see the subagent gate transcripts; the 1.3 skill baseline drops the same way, 4/1/6 live vs 2/1/1 rescored). Steps in the table are the live values; rescore is used only for the corrected `found` flag.

## D8 verdict
Bar: end_state >= 1/3 (skill row is 2/3), followed_all >= 0/3. Result end_state 2/3, followed_all 2/3: MET, matching the skill row on end_state. The step-1 defect is fixed: both delivered trials ran the learn runbook path instead of asking. Remaining weakness is retrieval, not the body: 1 of 3 phrase twos named the flag and P2h missed it.

## Spend tally (this task)
Paid n=3 run: $3.99 (0.27 + 1.65 + 2.07), above the ~$1-3 estimate because two trials completed the full 19-step workflow. No other paid runs in this task (steps 1-3 are no-spend). No rerun.
