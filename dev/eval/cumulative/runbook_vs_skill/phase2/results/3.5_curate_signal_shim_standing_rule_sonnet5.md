# 3.5 curate-signal R arm with the shim standing rule (D12), sonnet5, 2026-09-21

Config: `probe_phase2.py --task curate-signal --arms R --shim-only --model sonnet5 --n 1 --keep` (smoke),
then `--n 3 --keep` (fresh trials). Scratch binary built from commit 013f591e (unchanged from 3.4 — this
run's only variable is the shim), first on PATH via `ENGRAM_BIN_DIR`. Trial CLAUDE.md = the current
worktree `agent-instructions/guidance/shim.md`, now carrying D12's standing rule (`probe_phase2.py`'s
`SHIM_GUIDANCE_PATH` reads it live from the repo, not a frozen copy). Same task, vault, scorer as
`3.3_curate_signal_shim_only_sonnet5.md` / `3.4_curate_signal_stronger_hint_sonnet5.md`. Raw:
`3.5_smoke_shim_standing_rule_sonnet5_curate-signal.jsonl` (+`.log`),
`3.5_n3_shim_standing_rule_sonnet5_curate-signal.jsonl` (+`.log`).

| Trial | hint seen | standing rule cited | runbook surfaced | requested note done | offers curated | end_state | $ |
|---|---|---|---|---|---|---|---|
| smoke (#0) | yes | yes ("a standing instruction per CLAUDE.md, not optional") | yes | yes | yes (3/3: near folded+discarded, covered reinforced+discarded, absent kept) | PASS | 1.01 |
| n=3 #0 | yes | yes | yes | yes | yes (3/3) | PASS | 0.59 |
| n=3 #1 | yes | yes | yes | yes | yes (3/3) | PASS | 0.75 |
| n=3 #2 | yes | yes | yes | yes | yes (3/3) | PASS | 0.60 |

Bar (end_state >= 2/3): MET, 4/4.

| Run | n | offers curated | end_state | $ |
|---|---|---|---|---|
| S skill (1.3) | 3 | 0/3 | 0/3 | 0.72 |
| N bare (1.3) | 3 | 0/3 | 0/3 | 0.68 |
| R shim-only, old hint at payload tail (3.3) | 3 | 0/3 | 0/3 | 0.76 |
| R stronger hint, no shim rule (3.4 smoke) | 1 | 0/1 | 0/1 | 0.24 |
| **R shim standing rule (this, D12)** | **4** | **4/4** | **4/4** | **2.95** |

## What happened

Every trial's first query payload carried `pending_offers: true` and `pending_offers_hint` (14-16
occurrences of `pending_offers_hint` per transcript — the raw payload plus the agent's own re-reads). Every
trial's transcript contains the phrase "standing instruction" from the new shim section
(5-6 occurrences per transcript). The smoke trial's own words, mid-turn, right after finishing the
requested note:

> Now handling the pending-offers instruction surfaced twice by the CLI (a standing instruction per
> CLAUDE.md, not optional) — running the curate runbook.

It then named the curate runbook by basename, restated its steps and red flags, and closed with:

> **Pending-offer curation** (surfaced as a standing instruction by the CLI, handled per the curate
> runbook): [names all 3 offers and the covered/near/absent action taken for each]

All 4 trials: requested note written, all 3 fixture offers judged and curated correctly (covered
discarded + existing note reinforced, near folded in + discarded, absent kept with marker cleared),
`engram check` clean (only pre-existing unrelated dangling-wikilink warnings), 13/13 scored steps followed,
`end_state` PASS.

## Validity

Marker seen 4/4; `valid` true 4/4 (no `api_error`, no timeouts); no `"type":"tool_use","name":"Skill"` in
any transcript (0/4 — no shadowing; the earlier `"name":"Skill"` grep hits were the Skill tool's own
schema definition in the system tools list, not an invocation); not degenerate (7-13+ tool uses per trial,
$0.59-1.01, correct note + correct curation outcomes, not empty/near-no-op).

## Spend

$2.95 total (smoke $1.01 + n=3 $1.94), within the ~$1-3 estimate (stop-if->$4 not hit).
