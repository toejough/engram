# 3.5b route dispatch-tier regression with the shim standing rule (D12), sonnet5, 2026-09-21

Config: `probe_phase2.py --task route --arms R --shim-only --model sonnet5 --n 3 --keep`; trial CLAUDE.md
= the current worktree `agent-instructions/guidance/shim.md` (D12's standing rule added). Same scratch
binary and `ENGRAM_BIN_DIR` as 3.5. Raw: `3.5b_route_regression_shim_standing_rule.jsonl` (+`.log`).

| Trial | route runbook found | followed_all | end_state | $ |
|---|---|---|---|---|
| R-0 | yes | 10/10 | FAIL — "no route-evidence-* note wikilinks one of the dispatch evidence notes" | 0.79 |
| R-1 | yes | 10/10 | PASS | 0.77 |
| R-2 | yes | 10/10 | PASS | 0.75 |

| Run | n | found | end_state | $ |
|---|---|---|---|---|
| 6.3 prior route regression (shim with `--text`, before D12) | 3 | 3/3 | 3/3 | 1.55 |
| **This run (shim with D12 standing rule)** | 3 | 3/3 | 2/3 | 2.31 |

Bar (found 3/3, per task 3a.3): **MET**. `end_state` was not the specified regression bar (6.3's own
"Bar" line names only `found`), and it moved from 3/3 to 2/3.

## Is the end_state miss related to the new shim rule?

No evidence it is. The route fixture vault has no pending offers: trial R-0's own first-turn text reads
"No `pending_offers` field — nothing outstanding to act on beyond this task. The top match is clearly the
governing runbook here," and all three trials proceed straight to announcing and following the route
runbook with no detour. The `pending_offers` / `standing instruction` string hits found by an initial grep
(5 and 4 per transcript) are the shim.md guidance text itself, quoted back into the system prompt / tool
context (shim.md's own new section literally contains both phrases) — not a real triggered payload field
or an actual curation detour. R-0's miss is the route task's own `done_when` check for a
`route-evidence-*` note cross-link, a defect class already known to be trial-variable in this task (not
previously seen failing here, but a single-condition wikilink omission, not a whole-procedure derailment
— found, restated, and followed 10/10 steps).

## Validity

Marker seen 3/3, no `api_error`, no timeouts; `found=True` 3/3 matches the prior 6.3 result exactly.

## Spend

$2.31. Combined with 3.5's $2.95, total this phase: $5.26 (approved ceiling ~$6).
