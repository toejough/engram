# 4.1 curate runbook retrieval check against the REAL vault (no LLM spend)

Change `curate-skill-to-runbook`, task 4.1. Promoted note: `1049.2026-09-21.curate-review-pending-offers.md`
(real vault, `~/.local/share/engram/vault`, 983 notes after promotion; `engram embed status` clean:
983/983 with-embeddings, 0 stale/without/incompatible/broken). Binary: installed `~/go/bin/engram`
(already whole-word-matching per `3.1_curate_retrieval_check.md`'s cross-check). Command shape:
`engram query --lazy-chunks --vault <real vault> [--text T] --phrase P [--phrase P2]`. "Rank" = position
in `items[]`; "provenance" = its `provenances` (`trigger` = a `triggers:` entry matched `--text`; `direct`
= semantic top-k).

## Real agent phrases (harvested from `3.3_curate_shim_only_sonnet5.md`, no `--text`)

| Probe | Curate rank | Provenance | Score |
|---|---|---|---|
| R-0 phrases ("Review and curate the pending offered notes..." / "triaging pending unreviewed submissions...") | 1 | direct | 0.561 |
| R-1 phrases ("curating pending offer notes in the beekeeping memory vault" / "reviewing and deciding disposition...") | 1 | direct | 0.678 |
| R-2 phrases ("reviewing pending notes submitted..." / "triaging and curating unreviewed pending offers...") | 1 | direct | 0.709 |

## `--text` / trigger forms

| Probe | Curate rank | Provenance | Score |
|---|---|---|---|
| explicit-ask fixture prompt (`3.3`'s verbatim user message) + phrases | 1 | direct, trigger | 0.825 |
| reworded notice command: `--text "curate pending offers" --phrase "reviewing pending offers in a vault and judging each against existing notes"` (verbatim from `internal/cli/offer.go`'s `pendingOfferCurateCommand`) | 1 | direct, trigger | 0.934 |
| `--text "/curate"` | 1 | direct, trigger | 0.613 |
| `--text "there is one pending offer left"` (singular trigger) | 1 | trigger | 0 |
| `--text "there are pending offers waiting in the vault"` | 1 | direct, trigger | 0.473 |

## Over-fire probes (should NOT surface via `trigger`)

| Probe | Curate rank | Provenance | Verdict |
|---|---|---|---|
| `--text "that is accurate"` | not in items[] (45 shown) | — | pass |
| `--text "please double check that this is accurate"` | not in items[] (46 shown) | — | pass |
| `--text "curated offers"` | 7 | direct only, 0.430 | pass (no trigger hit inside "curated"; low semantic rank) |
| `--text "these were already curated last week"` | 6 | direct only, 0.432 | pass |
| `--text "curate a playlist of songs"` | 1 | trigger only, score 0 | fires — expected accepted over-fire (bare-word trigger trade, glossary-documented; a hit is a candidate the agent judges against the situation) |

## Reading

- **Trigger channel**: every real notice/write-nudge command form and every explicit trigger word
  (`curate`, `/curate`, `pending offers`, `pending offer`) surfaces the runbook at rank 1 with `trigger`
  provenance against the real ~1000-note vault, matching the trial-vault check (`3.1_curate_retrieval_check.md`).
- **Semantic channel**: all 3 harvested real-agent phrase sets (no `--text`) rank the runbook 1st against
  the real vault (scores 0.56-0.71) — the trial-vault caveat about optimistic small-vault ranks did not
  materialize as a real-vault regression; if anything the larger vault's other notes did not outrank it.
- **Over-fire**: "accurate" and "curated" (whole-word guard) do not trigger-fire and rank low/absent
  semantically. "curate a playlist" fires via `trigger` as designed and accepted (D3).
- **Conclusion**: retrieval against the production vault is verified; task 4.1's retrieval-check gate is met.
