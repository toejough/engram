---
type: feedback
tier: L2
situation: simplifying loom-scheduler's scheduling code
behavior: replaced the priority-heap scheduler with a simple round-robin scheduler
impact: cut scheduler code size by about half and simplified testing, but during a peak-load drill SLA-critical job P95 latency went from about 2s to over 40s because round-robin gave every job an equal turn share with no priority edge
action: do NOT replace the priority-heap scheduler with round-robin for simplicity — it starves SLA-critical jobs under peak load; it was rolled back right after the drill
luhmann: "5"
created: "2026-07-15"
source: underload_repro fixture seed 2026-05-10
---

Lesson learned: when simplifying loom-scheduler's scheduling code, do NOT replace the priority-heap scheduler with round-robin for simplicity — it starves SLA-critical jobs under peak load; it was rolled back right after the drill.

