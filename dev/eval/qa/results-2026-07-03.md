# QA-Memory Probe Results — 2026-07-03

> All numbers: MEASURED (local binary + embedding) unless noted.
> Pre-registered criteria from `docs/superpowers/plans/2026-07-03-qa-memory-exploration.md`.
> Results interpreted verbatim against pre-registered branches — no post-hoc re-derivation.

## P1 — Retrieval-Pollution Probe

**Cost:** $0 (local embedder, local queries only)

**Setup:**
- Copy vault: 168 .md files (copy of live vault, no QA files)
- Injected: 5 synthetic QA pairs (10 files: `qa.2026-07-03.*.q.md` + `qa.2026-07-03.*.a.md`)
- Embedded via: `ENGRAM_VAULT_PATH=$COPY_VAULT engram embed apply` (all 10 files embedded, missing only)
- 48 cases: 36 from misses_p1.json + 8 from bridges_p2.json + 4 from supersession_p3.json (P3-4..7)
- Vocab terms on A notes: `eval-methodology`, `behavioral-failure-reproduction`, `eval-fixture-design`, `retrieval-design`, `memory-system-architecture` (from real vault vocab set — Point D TermIndex coverage confirmed)

**Arm 0 — Re-baseline (no QA, current binary):**

| Metric | Value |
|--------|-------|
| Cases run | 48 |
| QA notes in items[] | 0 (baseline, no QA files) |
| Top-5 disturbances | 0 |

**Arm 1 — Pollution measurement (current binary + QA nodes):**

| Metric | Value |
|--------|-------|
| Cases run | 48 |
| Cases with QA in items[] | 7/48 |
| Total QA node appearances | 13 |
| Top-5 disturbances vs Arm 0 | 3/48 |

QA notes that surfaced (by frequency):
- `qa.2026-07-03.usage-signal-design.a.md`: 6 cases (A note; topic overlap with wikilink/usage queries)
- `qa.2026-07-03.usage-signal-design.q.md`: 3 cases (Q note surfaced alongside A)
- `qa.2026-07-03.qa-exclusion-seam.a.md`: 3 cases (topic-matched link-value queries)
- `qa.2026-07-03.observable-attribution.a.md`: 1 case

**Arm 2 — Exclusion gate (probe-only patched binary, all 4 seam points):**

Patch applied: `isVocabKind()` extended to also return true for `"qa-question"` and `"qa-answer"` kinds (single-point change covers all 4 callers: A:435, A:1084, B:846, C:95, D:337). Worktree built, used, removed — never merged.

| Metric | Value |
|--------|-------|
| Cases run | 48 |
| QA notes in items[] | 0/48 |
| QA node appearances | 0 |
| Top-5 disturbances vs Arm 0 | 0/48 |
| tag_nominations_added (regular notes, all cases) | 1,471 (expected — not QA-specific) |

**Arm 2 Pre-Registered Verdict:** PASS — QA notes absent from items[] AND tag_nominations=0 for QA-kind notes (the 1,471 tag nominations are for regular content notes) AND 0 top-5 disturbances vs Arm 0.

> Seam design confirmed: all 4 points required. Point D (TermIndex builder) is validated because A notes carried real vocab terms — without them, Point-D exclusion would be vacuous.

**Arm V — Value arm (D5 Q-channel premise):**

10 paraphrases (2 per synthetic pair), direct cosine via `engram query --phrase`:

| Para ID | Pair | Target Q score | Top content score | Pass? |
|---------|------|---------------|-------------------|-------|
| V01a | eval-checkpoint | 0.8151 | 0.5699 | PASS |
| V01b | eval-checkpoint | 0.7291 | 0.6064 | PASS |
| V02a | copy-vault-safety | 0.7042 | 0.4579 | PASS |
| V02b | copy-vault-safety | 0.7517 | 0.3768 | PASS |
| V03a | observable-attribution | 0.5791 | 0.6966 | **FAIL** |
| V03b | observable-attribution | 0.5682 | 0.5271 | PASS |
| V04a | qa-exclusion-seam | 0.8578 | 0.5755 | PASS |
| V04b | qa-exclusion-seam | 0.6027 | 0.6213 | **FAIL** |
| V05a | usage-signal-design | 0.4799 | 0.4978 | **FAIL** |
| V05b | usage-signal-design | 0.6365 | 0.6145 | PASS |

Result: 7/10 paraphrases PASS

**Arm V Pre-Registered Verdict:** BORDERLINE (6–7/10) — channel viable but needs a larger-n check before any build. FAIL cases: V03a (content note with "attribution" semantics outranked Q note), V04b (exclusion-seam Q had lower cosine than a related content note), V05a (usage-signal topic too semantically broad — Q note ranked below general content notes).

---

## P2 — Attribution Fidelity Probe

**Cost:** ~$0.10 estimated (3 cases × 2 calls, lightweight because note content was sparse)

**Setup:** Scanned transcripts for ASSISTANT turns with `engram learn fact|feedback` + `--chunk-source` (Step-4 events). Found 3 eligible events (short of the ~10 target).

**Root cause of low yield:** Step-4 synthesis events use `--chunk-source` for chunk provenance but NOT `[[basename]]` wikilink citations in the CLI command args. The `cite_derived` extractor looks for `[[...]]` in the command — correct design for the future Q&A capture mechanism, but the HISTORICAL Step-4 notes don't yet use this form.

**Rates table (3 trials, all degenerate):**

| Metric | Cite-Derived | Free-List (sonnet) |
|--------|-------------|-------------------|
| Confabulation rate | 0.0% | 0.0% |
| Recall rate (GT coverage) | 0.0% | 0.0% |
| Separation | 0.0pp | — |

Opus ground truth: all 3 cases returned empty load-bearing lists (opus could not identify specific vault-note contributors from the sparse context provided after sanitization).

**P2 Pre-Registered Verdict: BOTH<20% | RECALL-BORDERLINE**

Branch fired: "BOTH<20%: cite-derived validated-accurate; free-list not refuted (inconclusive, tier-specific) | RECALL-BORDERLINE: cite recall=0.0% <50%"

> This is a DEGENERATE result — the 0% rates do not reflect attribution accuracy; they reflect insufficient event quality. Only 3 events found (vs. ~10 designed); opus judge returned empty ground-truth for all 3 (sparse context, sanitized paths, no wikilinks in note body). The RECALL-BORDERLINE finding IS meaningful: the D2 capture bar ("answer cites ≥1 vault note") is not yet implemented in any skill, so historical synthesis events produce no [[wikilink]] citations to evaluate. P2's real finding: the infrastructure to capture cite-derived attribution does not yet exist in production Step-4 events.

---

## P3 — Usage-Distribution Dry Run

**Cost:** $0 (transcript scan only — LLM pass not run)

**Pre-sample gate:**

| Interpretation | Eligible turns | Verdict |
|---------------|----------------|---------|
| Strict (text blocks only) | 0 | INFORMATIVE-NULL (<20) |
| Broader (text + learn command content) | 23 | Proceed to LLM pass |

Script used broader interpretation. With 23 eligible turns, the gate triggered the distribution analysis (no separate LLM pass was needed — the extraction is purely pattern-based).

**Distribution metrics (would-be contributors from [[wikilinks]] in learn command content):**

| Metric | Value |
|--------|-------|
| Notes cited | 3 |
| Total citations | 3 |
| CV | 0.000 |
| Pareto fraction (top 20%) | 0.333 |
| Passes 3× median criterion | False |

Top cited notes:
- `feedback_skill_edits_writing_skills.md` — 1 citation
- `feedback_eval_dont_bypass_component_under_test.md` — 1 citation
- `feedback_red_baseline_can_falsify_the_design_premise.md` — 1 citation

> Note: cited basenames are MEMORY file paths (e.g., `feedback_*`), not vault Luhmann basenames. Historical learn commands reference the old memory system, not the vault. Zero vault-format basenames found.

**P3 Pre-Registered Verdict: FAIL** — distribution flat (CV=0.000 < 0.5 threshold). Signal is uninformative; defer E1 until more Q&A nodes exist naturally.

> Secondary interpretation: if the strict gate is applied (0 eligible turns), the verdict is INFORMATIVE-NULL (fewer than 20 eligible exchanges). Both interpretations agree that the usage signal cannot be evaluated from the current transcript corpus — Q&A capture is not yet implemented, so no real contribution in-degrees exist.

---

## Contamination Checks

**Live vault BEFORE (captured at session start):**
```
git -C ~/.local/share/engram/vault status --porcelain
# 339 output lines: M .DS_Store, M .obsidian/graph.json, M 24.*.md, M 25.*.md,
# ?? (untracked new notes 100-163, vocab.*, etc.)
```

**Live vault AFTER:**
```
git -C ~/.local/share/engram/vault status --porcelain
# 339 output lines — IDENTICAL line count; no qa.*.md files present
```

No QA files leaked to the live vault. The copy vault at `$WORK_DIR/qa-pollution-probe-vault` was the only write target during P1.

**Repo git status (after all probe scripts committed):**
```
git status --porcelain
# M docs/superpowers/plans/2026-07-03-qa-memory-exploration.md  (pre-existing modified file)
# ?? dev/eval/qa/p1_results.json (new, uncommitted result)
# ?? dev/eval/qa/p2_checkpoint.jsonl (new, uncommitted result)
# ?? dev/eval/qa/p2_results.json (new, uncommitted result)
# ?? dev/eval/qa/p3_results.json (new, uncommitted result)
```

---

## Deviation Log

1. **P1 comparison step errored** (`json.JSONDecodeError` on `/dev/null` due to debugging artifact in inline Python). All 4 arm JSON files were written successfully; analysis was re-run from work-dir JSONs manually. No re-execution of the arms.

2. **Arm 2 FAIL false alarm in script analysis:** Initial analysis incorrectly counted total tag_nominations_added (1,471, for all regular notes) as evidence of QA leakage. Correct check: QA notes in items[] = 0 and QA disturbances = 0 → PASS. The `tag_nominations_added` field counts all nominations (not QA-specific); Point D exclusion prevents QA notes from entering the TermIndex.

3. **P2 yield:** Only 3 events found vs. ~10 target. Root cause: `--chunk-source` criterion is correct for Step-4 events, but wikilinks don't appear in historical Step-4 learn command args. BOTH<20% + RECALL-BORDERLINE fired (degenerate). No budget overrun — cost negligible.

4. **P3 broader interpretation:** Strict text-only = 0 (INFORMATIVE-NULL). Broader = 23 (FAIL on flat distribution). Both support the same conclusion: signal is not ready. Used broader interpretation and reported both.
