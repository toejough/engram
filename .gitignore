# Binaries
/engram
bin/

# Project state and context
.engram/
/context/
context.test
/state.toml
updates.jsonl

# Test artifacts
coverage.out
*.fail
internal/memory/testdata/
internal/memory/embeddings.db

# Claude local state
.claude/projects/
.claude/settings.local.json
.claude/worktrees/

# Obsidian workspace
.obsidian/

# Session logs (local to developer, not committed)
docs/session-logs/

# Playground (large generated files, regenerated from memories)
playground/scoring-playground.html
playground/corpus/

# Skill results
result.toml
skills/*/result.toml
skills/*/test-coverage.md
.worktrees/
chat.toml

# Node deps (OpenCode plugin's resolved deps; package-lock pins versions)
node_modules/
.claude/scheduled_tasks.lock

# engram vault runtime lock (generated on vault access; never committed)
.luhmann.lock

# Stray eval app-data (notes/links/feeds binaries write these when builds run in-tree)
/notes.json
/links.json
/feeds.json
/subscriptions.json
/.feeds
/.feeds.json
/.links/
/.links.json
# same strays when a build cell runs with cwd = dev/eval/cumulative/
/dev/eval/cumulative/notes.json
/dev/eval/cumulative/links.json
/dev/eval/cumulative/feeds.json
/dev/eval/cumulative/subscriptions.json
/dev/eval/cumulative/.notes.json
/dev/eval/cumulative/.links.json
/dev/eval/cumulative/.feeds.json
/dev/eval/cumulative/.notes/
/dev/eval/cumulative/.links/
/dev/eval/cumulative/notes.db
__pycache__/
