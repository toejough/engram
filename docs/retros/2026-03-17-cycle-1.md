# Retro: Cycle 1 — Correctness

**Date:** 2026-03-17
**Scope:** #311 (evaluator persistence), #312 (stop.sh async isolation), build health

---

## Metrics

| Metric | Value | Target | Status |
|--------|-------|--------|--------|
| Fix/feat ratio | 33% (1 fix, 2 feat, 3 docs) | < 30% | ⚠️ |
| Throughput | 2 blocking issues closed / 1 session | — | ✅ |
| Issues opened vs. closed | 0 opened, 2 closed (#311, #312) | net-negative | ✅ |
| Spec items added | ARCH-81, T-345, T-108, T-109 | — | ✅ |
| `traced verify` pass rate | N/A — CLI incompatible (state.toml removed) | 100% | ❌ |

---

## Pattern

During #312 (rename-before-read in `readSurfacingLog`), 16+ existing test `WithReadFile`
handlers used hard-coded path equality (`name == "/data/surfacing-log.jsonl"`), making
them brittle to internal path changes. The actual feature was a 10-line change in
`evaluator.go`; the test fixup (mass-replace to `strings.Contains(name, "surfacing-log")`)
took roughly equal time. This is the single biggest source of rework this cycle.

---

## Action

Filed as **#321**: Add `makeTestEvaluator` path-redirect helper so rename-aware reads
are transparent to existing tests — motivated by 16 handler updates required for a
10-line implementation change (fix/feat rework ratio 33%, above 30% target).

---

## Notes

- `traced verify` fails with `state.toml not found` — state.toml was intentionally
  removed in `3c3a852`. The traced CLI needs updating to work without state.toml.
  Filed separately against `toejough/traced`.
- Cycle 2 (#313–#320) is now unblocked per roadmap.
