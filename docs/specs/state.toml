[project]
name = "engram"
skill = "specwalk"

# Layer registries

[layers.L1]
items = ["UC-3"]
green_tool = "none"
refactor_tool = "none"

[layers.L2]
items = ["REQ-1", "REQ-2", "REQ-3", "REQ-4", "REQ-5", "REQ-6", "REQ-7", "DES-1", "DES-2", "DES-3"]
green_tool = "none"
refactor_tool = "none"

[layers.L3]
items = [
  { id = "ARCH-1", source = "derived" },
  { id = "ARCH-2", source = "derived" },
  { id = "ARCH-3", source = "derived" },
  { id = "ARCH-4", source = "derived" },
  { id = "ARCH-5", source = "derived" },
  { id = "ARCH-6", source = "derived" },
  { id = "ARCH-7", source = "standard", rationale = "DI everywhere — all I/O through injected interfaces. From CLAUDE.md design principles." },
]
green_tool = "none"
refactor_tool = "none"

[layers.L4]
items = []
green_tool = "none"
refactor_tool = "none"

[layers.L5]
items = []
green_tool = "targ test"
refactor_tool = "targ check"

# Tree nodes — L1

[tree.L1A]
layer = "L1"
name = "Remember & Correct"
items = ["UC-3"]
status = "in_progress"
history = "Rewritten per issue #17: flat TOML files + LLM enrichment. All other UCs deferred to issue #18."
gates = { regroup_proposal = "completed: radical regroup — UC-3 only, all other UCs deferred to issue #18" }

# Tree nodes — L2

[tree.L2A]
layer = "L2"
name = "Remember & Correct Requirements"
parents = ["L1A"]
items = ["REQ-1", "REQ-2", "REQ-3", "REQ-4", "REQ-5", "REQ-6", "REQ-7", "DES-1", "DES-2", "DES-3"]
status = "complete"
history = "7 REQs + 3 DES items. Detection, enrichment, file writing, feedback, degradation, CLI, confidence, hook wiring."

# Tree nodes — L3

[tree.L3A]
layer = "L3"
name = "Remember & Correct Architecture"
parents = ["L2A"]
items = ["ARCH-1", "ARCH-2", "ARCH-3", "ARCH-4", "ARCH-5", "ARCH-6", "ARCH-7"]
status = "in_progress"
history = "7 ARCH decisions: pipeline (ARCH-1), pattern matching (ARCH-2), LLM enrichment (ARCH-3), TOML writer (ARCH-4), renderer (ARCH-5), CLI wiring (ARCH-6), DI standard (ARCH-7)."

# Cursor

[cursor]
node = "L3A"
mode = "green"
next_action = "REFACTOR L3 — bidirectional coverage, consistency. Then REGROUP, COMMIT, descend to L4."
context_files = ["docs/specs/state.toml", "docs/specs/architecture.md", "docs/specs/requirements.md"]
