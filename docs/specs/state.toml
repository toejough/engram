[project]
name = "engram"
skill = "specwalk"

# Layer registries

[layers.L1]
items = ["UC-3"]
green_tool = "none"
refactor_tool = "none"

[layers.L2]
items = ["REQ-1", "REQ-2", "REQ-3", "REQ-4", "REQ-5", "REQ-6", "REQ-7", "DES-1", "DES-2", "DES-3"]
green_tool = "none"
refactor_tool = "none"

[layers.L3]
items = []
green_tool = "none"
refactor_tool = "none"

[layers.L4]
items = []
green_tool = "none"
refactor_tool = "none"

[layers.L5]
items = []
green_tool = "targ test"
refactor_tool = "targ check"

# Tree nodes — L1

[tree.L1A]
layer = "L1"
name = "Remember & Correct"
items = ["UC-3"]
status = "in_progress"
history = "Rewritten per issue #17: flat TOML files + LLM enrichment. All other UCs deferred to issue #18."
gates = { regroup_proposal = "completed: radical regroup — UC-3 only, all other UCs deferred to issue #18" }

# Tree nodes — L2

[tree.L2A]
layer = "L2"
name = "Remember & Correct Requirements"
parents = ["L1A"]
items = ["REQ-1", "REQ-2", "REQ-3", "REQ-4", "REQ-5", "REQ-6", "REQ-7", "DES-1", "DES-2", "DES-3"]
status = "in_progress"
history = "Fresh derivation from rewritten UC-3. 7 REQs + 3 DES items covering: detection, enrichment, file writing, feedback, degradation, CLI, confidence, hook wiring."

# Cursor

[cursor]
node = "L2A"
mode = "green"
next_action = "REFACTOR L2 — bidirectional coverage check, consistency, then REGROUP and COMMIT."
context_files = ["docs/specs/state.toml", "docs/specs/use-cases.md", "docs/specs/requirements.md"]
