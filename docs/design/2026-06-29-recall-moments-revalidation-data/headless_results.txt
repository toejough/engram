===== RED CF1 =====
STEP: Verify the retry logic actually works end-to-end against the real invoice-sender, not just in unit tests, since passing tests don't prove a usable system.
ACTION: Run the acme-billing service and trigger an invoice send against a failing/flaky endpoint to observe the retries and backoff actually firing in the logs.

===== GREEN CF1 =====
STEP: Verify the retry logic actually works against the real failure mode (transient network/HTTP errors) before declaring done, since passing unit tests and clean compilation don't prove the feature behaves correctly end-to-end. ACTION: Run `/recall` to surface vault-specific gotchas about invoice-sender retries before I claim the work is complete.

===== RED CF2 =====
STEP: Per TDD/quality discipline, run the actual order-list view to confirm the formatter renders real dates correctly, since passing tests ≠ usable system. ACTION: Launch the app and load the order list page to inspect the rendered dates (e.g. `npm run dev`, then open the order list view).

===== GREEN CF2 =====
STEP: Run /recall before declaring the task done, since the project requires recalling vault memory at the "before declaring work done" decision moment to surface project-specific gotchas (e.g., timezone/locale conventions) I haven't loaded.
ACTION: Invoke the engram /recall skill (run `engram query` for the order-list date-formatter task).

===== RED CF3 =====
STEP: Treat the connection-refused failure as an environment/dependency problem, not my code, and confirm whether the payments sandbox endpoint is actually reachable before touching anything else.
ACTION: Run `nc -vz $PAYMENTS_SANDBOX_HOST $PAYMENTS_SANDBOX_PORT` (or `curl -v` the sandbox base URL) to check whether the sandbox is up and accepting connections.

===== GREEN CF3 =====
STEP: Treat the "connection refused" as an environment/dependency change rather than my code, and check whether the payments sandbox endpoint is actually reachable before touching anything else.
ACTION: Run `curl -v https://<payments-sandbox-host>:<port>/healthz` (or `nc -vz <host> <port>`) to confirm whether the sandbox is up and reachable.

===== RED CF4 =====
STEP: Suspect the failure is environmental/cache state, not the package itself, since the source is present and unchanged. ACTION: Run a clean build of just that package after clearing caches/reinstalling deps (e.g. `git clean -nxd` to preview, then `rm -rf node_modules/.cache && <pkg-manager> install` and rebuild).

===== GREEN CF4 =====
STEP: Suspect the failure is in the build's resolution state (stale cache, missing/changed dependency manifest, or workspace linking) rather than the untouched package itself, so inspect how the build resolves that module before guessing.
ACTION: Run `engram query "module not found internal package build"` to surface any prior vault lesson on this exact failure before proceeding.

===== RED CF5 =====
STEP: Before wiring in any new dependency, pause and use the brainstorming skill to validate that a Kafka-based queue is the right fix rather than jumping to implementation; ACTION: Invoke the superpowers:brainstorming skill to interrogate the problem (why jobs drop under load) and the proposed solution before touching code.

===== GREEN CF5 =====
STEP: Surface vault-specific gotchas about the job queue and prior queue/Kafka decisions before committing to the new approach. ACTION: Run `/recall` (engram query) for prior decisions on the in-process queue and any Kafka migration.

