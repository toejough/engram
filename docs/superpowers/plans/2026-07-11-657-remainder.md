# #657 Remainder Implementation Plan — re-measure, L3a, O1 disposal

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Re-measure recall-only wall-time (the issue's 350s premise was superseded 2026-06-25 — the true prior is ~190s pre-cuts, unre-measured since), ship L3a (the ingest sweep runs once per session, crystallization and the closing-learn sweep untouched), dispose O1 as subsumed by `--lazy-chunks`, then close or narrow #657 by a pre-registered decision rule.

**Architecture:** One measurement task (new timing script reusing the trap-harness plumbing), one skill-text task (recall + learn Step-0.5/Step-1 sweep dedupe under writing-skills TDD), one docs/disposal task. Trap gate smoke brackets the skill change. No engram binary code changes.

**Tech Stack:** Python (dev/eval/traps plumbing), skill markdown (writing-skills TDD), targ gates.

## Global Constraints

- `targ` only for test/lint/check (NEVER go test/go vet). Full gate: `targ check-full` all 8 PASS. If `check-uncommitted` fails on a dirty tree pre-commit, commit first and run the gate post-commit, amending the unpushed commit on failure (#682 precedent; targ's clean-tree check).
- Every commit: subject ≤72 bytes **measured with `wc -c`**, trailer `AI-Used: [claude]`.
- SKILL.md edits ONLY via superpowers:writing-skills (RED baseline → edit → GREEN + pressure test). Production vault untouched by tests. Diff-scope check (`git diff --stat`) before every commit.
- Work on branch `657-remainder`; review before push; ff-only merge.
- Trap gate (controller-run): fresh BEFORE on the unchanged tree (the #682 targ bump relinked the engram binary — `internal/cli/targets.go` imports targ — so prior GREEN runs don't carry over), AFTER on the final tree. `python3 dev/eval/traps/gate.py --tier smoke`; C5b-honored-only RED at n=1 → one same-tree re-run (both recorded); other RED = real.
- **Measured preconditions** (2026-07-11, live tree + LEDGER; executors re-verify):
  - LEDGER `recall-time-mislabel` row: the 350s figure timed round-1 (recall + first build), superseded. LEDGER `recall-time-isolated` row: recall-only ≈190s, n=2 directional, ~43–63% agent-side payload paging of a ~200KB payload, binary query ~3.5s — **pre-lazy-chunks/recent-fill/O2/L2 vintage**, not re-measured since.
  - O1 subsumption: `internal/cli/query.go:1430-1433` — under `lazyChunks` (recall's default invocation) the chunk-content path (`capChunkContent`/`ContentBudget`) is bypassed entirely; chunks render path/score only. Vault note 79 separately measured the content budget as not-the-bottleneck pre-lazy.
  - Sweep cost today: `engram ingest --auto` warm runs measured this session at ~2–5s each (mostly-reused corpus, 1–22 new chunks embedded per run); recall Step 0.5 + learn Step 1 each run it, 2–4× per please-cycle.
  - The trap harness runs warm trials via `dev/eval/traps/wrun.py`: `claude -p` with a RECALL_PREFIX prompt, `--output-format json` (duration/cost in the envelope); trial session transcripts are Claude Code JSONLs with per-message timestamps. The measurement script reuses this plumbing (vault seeding via `seed_c3.py`).
- **Pre-registered decision rule for #657's disposition** (applies at Task 3 using Task 1's median):
  - median recall-only ≤ 60s → **close** #657 (L3a/O1 dispositions noted; procedure time no longer at the scale the issue targeted);
  - 60–120s → **close** #657 with the parked clusters-first/lazy-content restructure named as the remaining lever (its ROADMAP revisit-condition stands);
  - > 120s → **keep open, re-scope** #657 to the restructure with the fresh number as its premise.
- Scope guards: do NOT defer crystallization (L3a is the sweep only); do NOT touch the Step 2.5 reconciliation or re-entry machinery; async-learn-style relocation stays rejected (settled park).

---

## Controller pre-flight (before Task 1)

Controller sections run in the orchestrating session (where `$CLAUDE_JOB_DIR` is set).

- [ ] Branch: `git checkout -b 657-remainder` from main.
- [ ] Trap gate BEFORE: `python3 dev/eval/traps/gate.py --tier smoke` → GREEN; log to `$CLAUDE_JOB_DIR/tmp/657-gate-before.log`.

---

### Task 1: Re-measure recall-only wall-time (instrument-first, same contrast as the superseded prior)

**Files:**
- Create: `dev/eval/traps/recall_time.py`
- Create (output artifact): `$CLAUDE_JOB_DIR/tmp/657-recall-time.json` (session scratch, not committed; the number lands in the LEDGER in Task 3)

**Interfaces:**
- Consumes: `wrun.py`'s invocation pattern (RECALL_PREFIX, env, `claude -p --output-format json`), `seed_c3.py` vault seeding.
- Produces: per-trial `recall_span_s` + median — Task 3's LEDGER row and the Task-3 decision-rule input.

- [ ] **Step 1: Pilot (n=1) — verify the instrument before the batch.** Run ONE warm trial via the new script: seed a sandbox vault (seed_c3 plumbing), invoke `claude -p` with the RECALL_PREFIX prompt and a small fixture task, capture the JSON envelope AND locate the trial's session transcript (Claude Code writes it under `~/.claude/projects/<cwd-hash>/`). Parse the transcript: recall-span START = timestamp of the first message invoking the recall skill; END = timestamp of the message carrying the Step-3 closing synthesis (match on its required "Query surfaced N items" opening or the `Re-entry:`/plan-walk shape). Print both timestamps + the span. **STOP if either delimiter cannot be located mechanically** — report the transcript shape and escalate the method; do NOT eyeball-estimate.
- [ ] **Step 2: Batch (n=3).** Three trials, fresh sandbox each; record per-trial: `recall_span_s`, total trial duration, cost. Write all rows + median + range to the JSON artifact. n=3 is directional by design (matching the n=2 prior's evidentiary class — this sizes a disposition, not a paper).
- [ ] **Step 3: Report.** The task report states: median, range, the prior (~190s, pre-cuts), and which decision-rule band the median lands in. Every number carries its evidence pointer (the JSON artifact + trial transcript paths).
- [ ] **Step 4: Commit** the script only — subject: `feat(eval): recall-only wall-time re-measure script (#657)` (58 bytes, `wc -c`-measured) + trailer. `targ check-full` green post-commit (python is lint-exempt but the gate must stay green).

---

### Task 2: L3a — the ingest sweep runs once per session (writing-skills TDD, both skills)

**Files:**
- Modify: `skills/recall/SKILL.md` (Step 0.5)
- Modify: `skills/learn/SKILL.md` (Step 1)

**REQUIRED SUB-SKILL:** `superpowers:writing-skills`.

**Design (approved framing):** intra-session re-sweeps mostly index turns already in the agent's context — their freshness value within the session is ~nil; inter-session freshness is preserved because the CLOSING learn always sweeps (it captures the cycle's tail for future sessions). Crystallization is untouched.

- [ ] **Step 1: RED baseline.** Per writing-skills, headless probes (separate `claude -p` processes, haiku, from `$CLAUDE_JOB_DIR/tmp`): fixture context = an in-progress session transcript excerpt showing `engram ingest --auto` already ran minutes earlier, ending at the skill's sweep step; neutral question ("what is your next step?"). OLD text → agent re-runs the sweep (expected RED ≥2/3). Run 3×, record verbatim.
- [ ] **Step 2: GREEN edits.**
  - `skills/recall/SKILL.md` Step 0.5 (anchor: the `engram ingest --auto` block and its "Seconds when nothing changed" paragraph): add the skip rule — "If a sweep (`engram ingest --auto`) already ran earlier in THIS session, skip it — intra-session re-sweeps index turns already in your context; the closing learn's sweep preserves freshness for future sessions."
  - `skills/learn/SKILL.md` Step 1 (anchor: its `engram ingest --auto` block): add — "Skip the sweep if one already ran earlier in THIS session, UNLESS this is the closing learn of a work cycle — the closing learn ALWAYS sweeps (it captures the session's tail for future sessions)."
  - Exact wording may be tuned by the writing-skills discipline; the two rules' content (skip-if-swept; closing-learn-always-sweeps) is fixed.
- [ ] **Step 3: GREEN probes** (3× per skill, same fixtures) → agent skips; plus one closing-learn fixture → agent sweeps (the carve-out holds); plus one pressure probe ("the index might be stale, shouldn't you sweep anyway?") → the text holds.
- [ ] **Step 4: Deploy.** From the repo root: `engram update`; verify with wrap-insensitive greps (`tr -s '[:space:]' ' ' < ~/.claude/skills/recall/SKILL.md | grep -c "already ran earlier in THIS session"` = 1, same for learn's file with its fragment = 1; measured pre-edit baselines: 0 and 0).
- [ ] **Step 5: Commit** — subject: `feat(skills): ingest sweep runs once per session (L3a, #657)` (60 bytes, `wc -c`-measured) + trailer; body carries the freshness rationale + the closing-learn carve-out. Diff-scope: exactly the two SKILL.md files. check-full green post-commit.

---

### Task 3: O1 disposal + docs + disposition

**Files:**
- Modify: `docs/ROADMAP.md` (Track B — the "#657 remaining cuts (L3a/O1)" section)
- Modify: `dev/eval/LEDGER.md` (one new row: recall-only re-measure, post-cuts vintage)

- [ ] **Step 1: LEDGER row.** Add under the recall-time rows: id `recall-time-remeasure`, status per Task 1's result, claim = median + range + n=3 directional + vintage (post lazy-chunks/recent-fill/O2/L2/L3a-pending), evidence pointer = the Task-1 artifact + this plan.
- [ ] **Step 2: ROADMAP rewrite.** The "#657 remaining cuts" section becomes the outcome record: L3a shipped (commit ref), O1 **disposed as subsumed** — `query.go:1430` bypasses the chunk-content path under `--lazy-chunks` (recall's default; note 79's prior negative measurement cited), the fresh recall-time number, and #657's disposition per the pre-registered decision rule. If the rule keeps #657 open, the section states the re-scope instead.
- [ ] **Step 3: Verify.** `grep -n "657" docs/ROADMAP.md` — every remaining mention consistent with the disposition; check-full green; diff-scope (two files).
- [ ] **Step 4: Commit** — subject: `docs(657): re-measure recorded; O1 disposed subsumed; L3a shipped` (65 bytes, `wc -c`-measured) + trailer.

---

## Controller close-out (after Task 3)

- [ ] Trap gate AFTER on the final tree (the skill text changed — this is memory-behavior surface): GREEN required; log to `$CLAUDE_JOB_DIR/tmp/657-gate-after.log`; C5b rule applies.
- [ ] Final whole-branch review (most capable model), review package over merge-base..HEAD.
- [ ] Gate C over ROADMAP.md + LEDGER.md diffs; Gate D over commit messages + the #657 comment (which carries: the superseded-premise correction — including that the cycle's own opening briefing repeated the stale 350s — the fresh number with evidence pointers, L3a ship, O1 disposal, and the decision-rule disposition).
- [ ] Rebase on main if moved → re-run check-full → ff-only merge → push → apply the #657 disposition (close with comment, or re-scope via `gh issue edit`).
- [ ] Retire this plan doc in a follow-up commit (engram convention).
