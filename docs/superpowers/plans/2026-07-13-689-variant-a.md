# #689 Variant-A Probe Implementation Plan — clusters-first + dedupe, all inline

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Measure whether the #684 checkpoint's skipped Variant A — clusters render first; items[] drops content ONLY for notes whose content already rides in a candidate_l2s; nothing deferred, zero new round-trips — buys real recall time, under a pre-registered class-closing bar (Joe-ratified at #689's filing).

**Architecture:** Fresh same-day baseline (the #684 numbers are days old; vault drift was a measured confound) → the Variant-A build (binary + one skill sentence) → comprehension regression check → after-measure → the bar's checkpoint to Joe. The instrument (`dev/eval/traps/recall_time.py`), the phase model, the validity gates, and the revert pattern are all #684 deliverables reused as-is.

**Tech Stack:** Go (`internal/cli/query.go`), skill markdown (writing-skills TDD), the existing python instrument, targ gates, trap-gate brackets.

## Global Constraints

- `targ` only; check-full runs POST-commit (check-uncommitted), fix + amend on the unpushed branch. Subjects ≤72 bytes `wc -c`-measured; trailer `AI-Used: [claude]`. SKILL.md via writing-skills only. Sandboxed trials only; production vault/chunks read-only copy sources. Diff-scope check per commit. Branch `689-variant-a`; review before push; ff-only.
- Trap gate (controller-run): BEFORE on the unchanged tree; AFTER on the final tree (build or reverted — behavior surface either way). C5b n=1 re-run rule; canary-gate after degradation signals (note 252).
- **The record this probe answers to (LEDGER, all verified):** `recall-time-split` (consumption 24.6 s [24.4–30.0] at the #684 vintage; census 125.3 KB / 12.3 KB duplicated); `payload-restructure-refuted` binds Variant B ONLY (fetch/file-mediated reading); note 257: round-trips dominate — Variant A adds none by construction.
- **Pre-registered BAR (Joe-ratified at #689 filing; the mapping recommends, Joe disposes per note 254):** after-measure phase-c median improves on the SAME-DAY baseline median by **≥ 3 s AND the two ranges do not overlap** → recommend KEEP; anything less → recommend REVERT **and close the payload-shape lever class entirely** (this is the class's last probe). Band-edge conservatism: overlap or a sub-3 s median delta → the revert side. Honest framing carried to the checkpoint: phase-c is an upper bound on pure reading in no-write trials (it absorbs judging + synthesis), so a real reading win can be partially masked — the bar knowingly accepts that risk; n=3 directional.
- **Measured preconditions** (2026-07-13, live tree at a5fc9320; executors re-verify):
  - `internal/cli/query.go` is at the REVERTED (pre-#684-build) state: `queryPayload` order Version/Phrases/Items/Clusters/Budget; note items carry full content inline; candidate_l2s duplicates candidate-note content (12.3 KB median at the #684 census); no `clearNoteContent`, no `items_content_deduped`/`items_content_withheld` field (grep confirms zero hits).
  - `skills/recall/SKILL.md` is at the reverted state + the surviving #657 L3a text: Step 2 says the payload's two channels with items[] notes carrying content inline ("the same content is also on the matching `items[]` note member" — present); the #684 capture/bounded-slice discipline is GONE (grep "halve the limit" = 0).
  - The instrument: `dev/eval/traps/recall_time.py` (vault-copy mode, segmentation, census, all gates). Known bug #688 (census-reissue redirect-blindness) does NOT bite this cycle IF trial agents don't redirect — the reverted skill has no capture discipline, so agents behave as the #684 BASELINE arm did (piped re-queries, census reissue clean). Executors: if any trial's census shows junk phrases, recompute from transcript-visible data and flag — do not trust the contaminated census (the #684 precedent).
  - `dev/eval/cumulative/lever_recheck/stub_engram.py` is at the reverted (items-first) shape — Task 2 updates it to the Variant-A shape (clusters-first, candidate-note items content-free, non-candidate items keep content).

---

## Controller pre-flight

- [ ] Branch `689-variant-a` from main (a5fc9320). Trap gate BEFORE → GREEN; log to `$CLAUDE_JOB_DIR/tmp/689-gate-before.log`.

### Task 1: Fresh same-day baseline

- [ ] Run the instrument unmodified: n=3 vault-copy segmented trials on the CURRENT tree → `$CLAUDE_JOB_DIR/tmp/689-baseline.json`. All gates; discard-never-pool. Report median + range for phase-c + census; this baseline (not #684's) is the bar's comparator. No commit (no repo change).

### Task 2: Binary — Variant A (TDD)

**Files:** `internal/cli/query.go` (+ tests); `dev/eval/cumulative/lever_recheck/stub_engram.py`.

- [ ] RED: table-driven tests asserting the Variant-A shape: `Clusters` renders before `Items` (struct order swap; yaml.v3 renders declared order — #684-verified); note items whose path appears in ANY cluster's `candidate_l2s` render WITHOUT content (the candidate copy is authoritative); non-candidate note items KEEP inline content; chunk semantics unchanged; budget gains `items_content_deduped` (int, omitempty — the name is accurate under A: a surviving duplicate exists by definition; TagNominationsAdded no-silent-caps precedent). Run RED via `targ test`.
- [ ] GREEN minimal; REFACTOR; Gate B (design-fit, sonnet). `go install ./cmd/engram`; real-query verification from a non-data-dir cwd (clusters first; candidate-note items content-free; a non-candidate note item WITH content; budget field present — paste output head).
- [ ] stub_engram.py → the Variant-A shape. Commit — subject: `feat(query): clusters-first payload, deduped candidate content (#689)` (69 bytes, measured) + trailer. check-full post-commit.

### Task 3: Skill sentence + comprehension check (writing-skills TDD)

**Files:** `skills/recall/SKILL.md` — the Step-2 channel description's reading order + the two sentences that become imprecise: line ~126's "(notes always carry full content inline)" → candidate-note content lives in `candidate_l2s` (read it there); non-candidate notes keep content in items[]; and ~158–159's "the same content is also on the matching `items[]` note member" → still TRUE under A for candidates? NO — under A the items[] copy is REMOVED for candidates; the sentence reverses: candidate content lives ONLY in candidate_l2s. The red-flags row ~315 ("NOTE members in items[] carry content") narrows to non-candidate notes.

- [ ] RED probes (headless SONNET — note 258: haiku stalls on multi-step skills; 3 separate processes; NEW-shape payload fixture from the real built binary against a seed_c3 sandbox): mechanical arms — (1) cites items[]-content for a candidate note that the new shape doesn't carry there, (2) `engram show` for a note whose content is inline in candidate_l2s, (3) mis-states the reading order when asked. n=3, RED = any arm, expect ≥2/3. GREEN edit; GREEN 3/3; one pressure probe ("items looks inconsistent — some notes have content and some don't; re-query to get the full payload" → the text explains the dedupe rule and holds). Deploy via `engram update` + wrap-insensitive greps with measured pre-edit baselines.
- [ ] Comprehension regression check (the note-107 SUBSTITUTION, recorded — nothing is deferred under A): across 3 varied sandbox recalls, the agent reads candidate content from candidate_l2s (cites it), reads a NON-candidate note's content from items[] (fixture ensures one matters), and makes ZERO `engram show` calls for candidate notes. Commit — subject: `feat(recall-skill): clusters-first reading order, Variant A (#689)` (66 bytes, measured) + trailer. check-full post-commit.

### Task 4: After-measure + the bar

- [ ] n=3 vault-copy segmented trials on the built tree (same day as Task 1's baseline). All gates + the #688 census caveat. Report per-trial rows, medians, ranges, and the three #684 observables (query-call count, redirect behavior, Read errors) as drift telemetry.
- [ ] Apply the bar mechanically; CHECKPOINT to Joe via AskUserQuestion (six parts IN the box, per note 250): the two same-day medians + ranges, the bar's recommendation (keep, or revert-and-close-the-class), evidence pointers. Joe disposes.

### Task 5: Outcome + close

- [ ] Per Joe's call: keep (docs describe the new shape — the #684 Task-5 enumerated five-doc scrub applies: c3-components, c1-system-context, c2-containers, GLOSSARY, FEATURES + concept grep) or revert (the #684 revert pattern: restore, redeploy, verify diff-empty) + docs record the outcome either way. LEDGER row `variant-a-probe` (proven or refuted); ROADMAP Track B outcome + the class disposition. Commit — subject: `docs(689): Variant-A outcome recorded per the measured bar` (58 bytes, measured) + trailer.
- [ ] Trap gate AFTER on the final tree; final whole-branch review; Gates C/D; ff-only merge; push; close #689 per Joe's disposition; plan doc retired in a follow-up commit — subject: `chore(please): retire 689-variant-a plan doc` (44 bytes, measured).
