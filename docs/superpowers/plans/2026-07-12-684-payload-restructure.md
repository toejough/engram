# #684 Payload Restructure Implementation Plan — measure first, build conditionally

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Establish the segmented recall-time baseline (#684's own gate: the post-cuts binary/agent split is unmeasured), size the payload-consumption slice the restructure can actually reach, present the number + recommendation to Joe at a mid-cycle checkpoint, and build the clusters-first/deduped payload ONLY if Joe green-lights on the measured ceiling.

**Architecture:** One instrumentation task extends `dev/eval/traps/recall_time.py` with phase segmentation (transcript-timestamp spans per recall phase) and a payload byte census (bytes per payload section, from the trial's own `engram query` output). A pre-registered checkpoint mapping recommends build/close to Joe. The conditional build task reorders `queryPayload` (clusters before items) and dedupes candidate-note content out of `items[]`, with the recall skill's consumption contract updated under writing-skills TDD and note-107's two-gate lazy-retrieval validation.

**Tech Stack:** Python (instrumentation), Go (`internal/cli/query.go` payload rendering, conditional), skill markdown (conditional), targ gates, trap-gate smoke brackets.

## Global Constraints

- `targ` only for test/lint/check; check-full runs POST-commit (includes check-uncommitted), fix + `git commit --amend` on the unpushed branch until green.
- Every commit subject ≤72 bytes `wc -c`-measured; trailer `AI-Used: [claude]`.
- SKILL.md edits only via superpowers:writing-skills. Production vault/chunks are read-only copy sources; all trials sandboxed (recall_time.py's existing pattern). Diff-scope check before every commit.
- Branch `684-payload-restructure`; review before push; ff-only merge.
- Trap gate (controller-run): BEFORE on the unchanged tree, AFTER on the final tree IF the build proceeds (binary + skill are behavior surface); a measurement-only cycle needs no AFTER (no behavior surface changed) — the BEFORE run then serves as the cycle's gate record. C5b-honored-only RED at n=1 → one same-tree re-run; expensive runs are canary-gated after any degradation signal (note 252).
- **Prior-outcome reconciliation (note 80, surfaced at orientation):** a 2026-06-24 recall restructure (host/haiku split) was ROLLED BACK — quality held but the op-level delta (~14%) didn't justify the complexity. This cycle's lever is different (payload shape, not model split), so the prior does not close it — but its moral is codified here as the checkpoint gate: no build without a measured ceiling that clears the bar.
- **Measured preconditions** (2026-07-12, live tree; executors re-verify):
  - `internal/cli/query.go`: `queryPayload` struct order = Version, Phrases, **Items, Clusters**, Budget (YAML renders in struct order); `matchSetCap = 300`; under `lazyChunks`, `clearChunkContent` strips chunk content but NOTE items keep full content in `items[]` WHILE the same notes' content also rides inline in `candidate_l2s` (O2) — candidate-note content is duplicated in today's payload.
  - Baseline from #657 (LEDGER `recall-time-remeasure`): recall-only vault-copy median 51.9s, range 39.3–63.6s (n=3 directional). The binary/agent split post-cuts is UNMEASURED (the pre-cuts ~3.5s binary / ~43–63% paging figures are superseded vintage). Small-fixture floor 39.7s.
  - `dev/eval/traps/recall_time.py` exists (vault-copy mode, pinned delimiters, caffeinate + span>wall + degraded-cost gates); trial transcripts carry per-message timestamps and tool-call records (the segmentation source).
- **Pre-registered CHECKPOINT mapping** (recommends — Joe disposes, per note 254; fires after Task 1):
  - Phase model (mechanical; handles the OBSERVED multi-query case — 2 of 3 real #657 vault-copy trials issued multiple `engram query` calls: vc-1 three, vc-2 two): (a) **pre-query** = first timestamped record → the FIRST `engram query` tool_use; (b) **query in-flight** = the SUM of each `engram query` call's own tool_use→tool_result span; (c) **payload consumption** = (first query tool_result → the Step-2.5-start marker) MINUS the in-flight spans of any subsequent query calls inside that window (inter-query reading time counts as consumption); (d) **remainder** = Step-2.5-start marker → the END delimiter. Step-2.5-start marker = **min(Arm 1, Arm 2)** — the EARLIER of: Arm 1 = the first assistant tool_use invoking `engram amend`/`engram activate`/`engram learn` after the first query tool_result; Arm 2 = the first assistant record matching the script's END_PRIMARY regex (`Query surfaced ~?[0-9]+\+? items`). (Arm-1-only precedence measured WRONG on real trial vc-2: a post-synthesis `engram activate` landed the marker after END, driving (d) to −11.8s and inflating (c) by ~12s — Gate A code review, mechanical run.) Neither arm found → STOP, escalate the method. **Phase-validity gate (mechanical, per trial):** every phase ≥ 0 AND (a)+(b)+(c)+(d) = START→END span ±1s; violation = segmentation-failed trial, reported and never pooled (same pattern as the sleep gate). Compound-call caveat: a single Bash tool_use chaining `show-chunk | ... | engram query` counts wholly as (b) under the per-call-span rule — this understates (c), consistent with its conservative-lower-bound framing; executors don't split it.
  - The **addressable slice** = phase (c) ONLY. Two bias directions, both stated at the checkpoint: excluding (b)'s binary render share UNDERSTATES the reach; and in Arm-2 trials (no write-side call — 3 of 4 preserved fixtures) the marker coincides with the END delimiter, so (d) is structurally 0 and (c) absorbs cluster-judging AND synthesis composition, OVERSTATING pure payload consumption. (c) is therefore an upper bound on consumption in Arm-2 trials, not a conservative floor — the checkpoint presentation says so next to the number.
  - addressable slice median **< 15s** → recommend **close #684 measured-out** (even total elimination beats the 51.9s median by <30%; the complexity moral of note 80 applies). Constant sourcing: ~12.2s is the measured corpus-scale-attributable gap between the vault-copy median and the small-fixture floor (51.9 − 39.7); the ~2.8s slack covers that indirect estimate's directional noise (its n=3 ranges spanned ~24s and ~10s) and rounds to a clean band edge. A judgment constant, labeled as such.
  - addressable slice median **≥ 15s** → recommend **build** (Tasks 2–4).
  - The checkpoint ALSO puts the SCOPE CHOICE to Joe (Gate A ask-alignment Critical): the issue's lever text says "lazy content across the matched set — fetch-on-demand"; the cheaper variant is dedupe-only (candidate-note content ships once, in `candidate_l2s`, nothing deferred). The census's `duplicated_note_content_bytes` and `items_notes_content_bytes` numbers inform the choice: **Variant A (dedupe-only)** — items[] drops content for candidate notes only; nothing fetch-on-demand; note-107 validation does NOT apply (nothing deferred) and is REPLACED by a comprehension/no-spurious-fetch regression check, with the issue-AC gap recorded in Task 5's disposition. **Variant B (true-lazy)** — ALL matched-note content in items[] goes path-only (the lazy-chunks pattern extended to notes; candidate_l2s keeps content inline); the agent fetches non-candidate note content via `engram show` on demand; note-107's SPARING + CAPABLE gates apply for real. Joe picks A, B, or measured-out.
  - Either way the checkpoint goes to Joe via AskUserQuestion with the segmented numbers + census + evidence pointers BEFORE any build task dispatches. (Ack'd here: the issue's "trap gate before + after" AC reads unconditional; in the measured-out branch no behavior surface changes, so the BEFORE run stands as the cycle's gate record — carried into the disposition comment.)
- Scope guards: the recall skill's Step 2.5 judgment contract (agent judges coverage; binary never decides) is untouched in any branch; no new payload fields beyond the restructure; `--lazy-chunks` semantics for chunks unchanged.

---

## Controller pre-flight (before Task 1)

- [ ] Branch: `git checkout -b 684-payload-restructure` from main.
- [ ] Trap gate BEFORE: canary first if any degradation signal; `python3 dev/eval/traps/gate.py --tier smoke` → GREEN; log to `$CLAUDE_JOB_DIR/tmp/684-gate-before.log`.

### Task 1: Phase segmentation + payload census (the measure)

**Files:**
- Modify: `dev/eval/traps/recall_time.py`

**Interfaces:**
- Consumes: the existing trial/delimiter/validity machinery.
- Produces: per-trial `phases` dict (pre_query_s, query_call_s, payload_consumption_s, remainder_s) + `payload_census` dict (total_bytes, items_notes_content_bytes, items_meta_bytes, clusters_candidate_content_bytes, clusters_meta_bytes, recent_bytes, duplicated_note_content_bytes) in the artifact; the checkpoint's addressable-slice number.

- [ ] **Step 1: Segmentation design, verified against REAL transcripts first** (note 237): the #657 vault-copy trial transcripts are PRESERVED at `$CLAUDE_JOB_DIR/tmp/657-transcripts/` (controller copied them 2026-07-12 — the artifact's own `transcript_path` entries point into `/tmp/recall-time-657/`, which `run_trial`'s `shutil.rmtree` deletes on its next invocation; do NOT rely on those paths). Against **vc-2 specifically** (the trial exhibiting BOTH the multi-query shape AND the late-activate shape that broke Arm-1 precedence) AND the single-query vc-0 (vc-1 optional, third shape: three queries + a compound piped call), locate mechanically per the phase model's exact arms (Global Constraints), print all marker timestamps + derived phase spans, and confirm the phase-validity gate passes on all verified trials. STOP if any marker cannot be located mechanically — escalate the method. recall_time.py's change ALSO copies each trial's transcript into a durable per-run directory alongside the artifact (fixing the ephemerality for future cycles).
- [ ] **Step 2: Implement** the segmentation + census in recall_time.py. Census formulas (computable, per field): save each trial's raw query payload YAML; section bytes are measured by SLICING THE RAW YAML between top-level keys (not by re-dumping parsed structs — markup bytes differ); content bytes by parsing into the payload structs' shapes; `items_notes_content_bytes` = Σ len(content) over items with kind ≠ chunk; `items_meta_bytes` = serialized items-section bytes − Σ len(content) over all items (YAML markup overhead included, stated as such); `clusters_candidate_content_bytes` = Σ len(content) over all candidate_l2s entries; `clusters_meta_bytes` = serialized clusters-section bytes − clusters_candidate_content_bytes; `recent_bytes` = serialized bytes of items whose provenances include "recent"; `duplicated_note_content_bytes` = Σ len(content) over paths whose content appears in BOTH an items[] note and any candidate_l2s entry (live-measured precedent: 8,410 bytes duplicated on one small real query, 5/5 candidate notes byte-identical in both places — Gate A code review); `total_bytes` = len(raw payload).
- [ ] **Step 3: Run** n=3 vault-copy trials (canary-gated). Artifact: append under a `segmented` key in a NEW artifact file `$CLAUDE_JOB_DIR/tmp/684-recall-segments.json`; per-trial phases + census + median/range per phase. Same validity gates; discard-never-pool.
- [ ] **Step 4: Commit** the script change — subject: `feat(eval): phase segmentation + payload census for recall-time (#684)` (70 bytes, `wc -c`-measured) + trailer. check-full post-commit.
- [ ] **Step 5: Report** — the addressable slice median + range, the census (how many KB the duplication and the items-before-clusters ordering actually cost), evidence pointers, and which checkpoint band the median lands in.

### CHECKPOINT (controller): Joe disposes build vs measured-out

- [ ] Present via AskUserQuestion: the segmented spans, the census, the mapping's recommendation, evidence pointers. Joe's call gates Tasks 2–4. If Joe says close: skip to Task 5 with the measured-out disposition.

### Task 2 (conditional, per Joe's checkpoint variant): Binary — clusters-first payload

**Files:**
- Modify: `internal/cli/query.go` (+ tests per repo conventions: imptest/rapid/gomega, t.Parallel, nilaway patterns)
- Payload-shape consumers (dispositions, from Gate A's live grep): `dev/eval/links/replay.py:50-67` + the inline parser in `dev/eval/qa/p1_retrieval_pollution.sh:290-310` hard-code an items→clusters end-sentinel — p1 is a stamped HISTORICAL instrument (annotate only, do not modernize); replay.py gets its sentinel fixed if still a live consumer (verify), else HISTORICAL-annotated; `dev/eval/cumulative/lever_recheck/stub_engram.py:74-93` emits the old shape as a binary stand-in — update to the new shape (it emulates production for skill tests) with a comment tying it to this cycle.

- [ ] RED: table-driven tests asserting the NEW payload shape per the chosen variant. Both variants: `Clusters` renders before `Items` (struct order swap); chunk/lazy semantics unchanged; budget gains `items_content_deduped` count (no-silent-caps rule, `TagNominationsAdded` precedent). Variant A additionally: note items whose path appears in any cluster's `candidate_l2s` render path/score/provenance only; non-candidate notes keep inline content. Variant B additionally: ALL note items render path/score/provenance only (candidate_l2s keeps content inline; `engram show` is the fetch path). Run RED.
- [ ] GREEN: minimal implementation. REFACTOR + Gate B (design-fit reviewer, sonnet).
- [ ] `go install ./cmd/engram` and verify with the real binary from a non-data-dir cwd (note 125): a real query shows clusters first and the variant's items shape.
- [ ] Commit — subject Variant A: `feat(query): clusters-first payload, deduped note content (#684)` (64 bytes, `wc -c`-measured); Variant B: `feat(query): clusters-first payload, lazy matched-note content (#684)` (69 bytes, measured) + trailer. check-full post-commit.

### Task 3 (conditional, per variant): Recall skill consumption contract (writing-skills TDD)

**Files:**
- Modify: `skills/recall/SKILL.md` — the enumerated now-false anchors (Gate A code review): line ~126 "(notes always carry full content inline)"; lines ~158–159 "the same content is also on the matching `items[]` note member"; red-flags row ~315 "NOTE members in `items[]` carry `content` — use it directly"; plus the Step-2 channel description's reading order (clusters lead the payload).

- [ ] RED probes on the OLD text against a NEW-shape payload fixture. Failure criterion (mechanical): the probe FAILS the old text if the agent (1) cites items[]-content that the new shape no longer carries there, OR (2) calls `engram show` for a note whose content is inline in candidate_l2s, OR (3) processes items[] before clusters when asked its reading order. Quantifier: n=3 probe runs (separate `claude -p` processes); a run is RED if it trips ANY arm; expected ≥2 of 3 runs RED. GREEN edit per variant; pressure probe; deploy via `engram update` + wrap-insensitive greps with measured pre-edit baselines.
- [ ] Validation per variant:
  - **Variant B (true-lazy)** — note-107 two gates, for real: (a) SPARING — across 3 varied sandbox recalls, `engram show` fetch count stays well under break-even (measure; the lazy-chunks precedent was 0/13); (b) CAPABLE — one sole-source fixture where a non-candidate matched note's content is load-bearing → the agent fetches it unprompted. Both must hold or the variant rolls back.
  - **Variant A (dedupe-only)** — note-107 does NOT apply (nothing is deferred; content ships once). REPLACEMENT check, honestly named comprehension/no-spurious-fetch regression: across 3 varied sandbox recalls, the agent reads candidate content from candidate_l2s (cites it) and makes ZERO `engram show` calls for candidate notes; the issue-AC substitution is recorded in Task 5's disposition.
- [ ] Commit — subject: `feat(recall-skill): clusters-first payload reading order (#684)` (63 bytes, `wc -c`-measured) + trailer. check-full post-commit.

### Task 4 (conditional): Re-measure + gates

- [ ] n=3 segmented vault-copy trials on the new binary+skill → the win is the measured delta on the addressable slice AND the total, reported directional with n + range. Trap gate AFTER → GREEN.

### Task 5: Docs + disposition

- [ ] LEDGER: `recall-time-split` row (the segmented baseline; plus the post-build delta row if built). ROADMAP Track B: #684 outcome record per what happened (built + delta with variant named, or measured-out).
- [ ] **Enumerated doc scrub (BUILD branch only — the measured-out branch changes no payload shape, so these stay true):** `docs/architecture/c3-components.md` ~137–140 (K6 payload spec: items/clusters order + "notes carry full content inline"); `docs/architecture/c1-system-context.md` ~154–160 (sequence diagram payload order + "content inline from the payload" note); `docs/architecture/c2-containers.md` :42, :50, :119, :121 ("notes keep full content" + sequence note); `docs/GLOSSARY.md` ~273–339 (candidate_l2s / items entries, :316 "notes keep full content inline"); `docs/FEATURES.md` two-channel payload section (order + content rules); `docs/architecture/memory-system-rigor.md` payload references. EVERY hit of the governing grep — enumerated or not (it also fires on adr.md, memory-invariants.md, ROADMAP.md, route/SKILL.md, and the recall test baselines) — gets a disposition: update, or explicit no-op with a word of why. Line numbers are planning-time hints; the live concept grep governs: `grep -rn "full content inline\|items\[\]\|candidate_l2s\|two channels\|two-channel" docs/ skills/ | grep -v superpowers/plans`.
- [ ] Concept-level verification greps both branches; Commit — subject: `docs(684): baseline split recorded; disposition per measurement` (63 bytes, measured) + trailer. check-full post-commit.

## Controller close-out

- Final whole-branch review; Gate C over touched docs; Gate D over outward prose (#684 comment carries: the segmented baseline + census, the checkpoint decision and Joe's call — including the variant or measured-out choice and, in Variant A, the note-107 AC substitution — gate records, and the measured-out branch's trap-gate-conditionality note).
- Rebase if main moved → check-full → ff-only merge → push → #684 disposition per Joe (the checkpoint already captured it; the close comment records it). Plan doc retired in a follow-up commit — subject: `chore(please): retire 684-payload-restructure plan doc` (54 bytes, measured) + trailer.
